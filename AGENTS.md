# Agent 持久指令

## 当前项目

- 项目：`scheduling-system`（格物工坊）
- 正式远程仓库：`gewu`
- 默认推送分支：`gewu/master`

## 当前长期目标与约束

- 将格物工坊改造成“指定本地数据主机 + 移动硬盘题库独立存储 + 多电脑离线编辑后授权同步 + 阿里云服务端中继 + 微信小程序只读/有限写入”的多端数据架构。
- 本地数据主机保存全量权威业务数据；阿里云只承担认证、设备、心跳、中继、快照、小程序 API 和任务队列，不作为最高权威业务数据库。
- 移动硬盘题库由本地数据主机统一管理，其他电脑不得直接写主机移动硬盘题库目录。
- 其他电脑允许离线本地编辑，但联网后必须经用户确认和授权后同步到本地数据主机，不得静默推送离线变更。
- 微信小程序以只读为主，只允许财务个人资产导入、题库选题组卷、Word/PDF 导出等有限写入任务；不得直接修改核心教务表或题库基础数据。
- 工作顺序是先完成并验证底层多端数据架构，再逐步设计和优化微信小程序 UI，最后部署阿里云和小程序。
- 小程序 UI 优化不能只停留在首页；后续每一个小程序页面都必须在原有基础上逐页优化、逐页验证，并留下截图或检查记录。
- 小程序 UI 优化必须同时覆盖管理员端和学生端两套体验；同一路由在不同角色下呈现不同导航、权限、数据范围或写入能力时，必须分别检查。
- 每次声明“小程序 UI 已优化/已完成”前，必须先维护页面清单：覆盖 `miniapp/src/app.config.ts` 的全部注册页面；代码中任何 `navigateTo`/`redirectTo` 可跳转的 UI 状态页也必须注册并纳入清单，不得留下源码存在但运行时打不开的页面；不得只依据首页、tab 首页或少量高频页面下结论。
- 小程序 UI 优化完成条件：页面清单中的每个页面都已有明确角色归属、优化策略、验证状态和截图记录；管理员路径、学生路径、空态/离线/无权限/有限写入提示等关键状态都有检查记录；`miniappUiCoverage` 类检查必须通过。
- 明确禁止“跑完首页截图/测试后转去做别的任务”。首页只能作为其中一个页面，不能代表整套小程序 UI 完成。
- 小程序 UI 中的所有功能入口、按钮、菜单、指标、状态文案和说明文字都必须建立在本软件真实存在的功能之上，且能追溯到现有路由、API、任务类型、权限模块、本地数据字段或已实现流程；不得为了视觉效果虚构功能、虚构模块、虚构指标、虚构承诺或虚构可点击入口。不确定是否真实存在时，必须先查代码和文档，不能凭感觉补文案。
- 后续验证由 Codex 主动执行并留痕；可使用浏览器、Playwright、微信开发者工具、阿里云检查脚本等工具。涉及账号、密钥、真实云部署或真实小程序发布时，仍以安全规则和可用权限为准。

## 每次修改代码后的默认流程

1. 运行与改动相关的测试；风险较高时运行 `npm test`。
2. 执行：
   - `git add -A`
   - `git commit -m "自动发布 YYYY-MM-DD"`
   - `git push gewu master`
3. 每次优化更新完成并推送 `gewu/master` 后，默认更新 OSS 桌面更新包：自动判定并递增版本，执行 `npm run dist:win` 生成安装包，再执行 `npm run publish:desktop-update` 发布 OSS 更新 feed。
4. Electron 打包或 OSS 发布后，必须确认已恢复 Node native 依赖环境（项目脚本通常已在 `npm run dist:win` 末尾执行 `npm run rebuild:node`），并运行相关验证。
5. 不再默认推送 `origin/master`。
6. 不再默认上传夸克网盘。

## 打包和上传

除“每次优化更新后默认更新 OSS 桌面更新包”外，只有在用户明确要求“打包”“生成安装包”“上传夸克网盘”时，才执行其他打包和上传流程。

如果用户明确要求打包：

1. 递增 `package.json` 版本号。
2. 执行项目构建命令，当前项目优先使用：
   - `npm run build && npx electron-builder --win`
3. 找到生成的安装包/构建产物。

如果用户明确要求上传夸克网盘：

- Codex 使用：`node scripts/upload-quark-clean.js`
- Qoder 使用：`node scripts/upload-quark-qoder.js`
- 不使用旧脚本：`node scripts/upload-quark.js`
