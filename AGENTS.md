# Agent 持久指令

## 当前项目

- 项目：`scheduling-system`（格物工坊）
- 正式远程仓库：`gewu`
- 默认推送分支：`gewu/master`

## 当前长期目标与约束

- 将格物工坊改造成“指定本地数据主机 + 移动硬盘题库独立存储 + 多电脑离线编辑后授权同步 + 阿里云服务端中继 + 微信小程序只读/有限写入”的多端数据架构。
- 本地数据主机保存全量权威业务数据；阿里云只承担认证、设备、心跳、中继、快照、小程序 API 和任务队列，不作为最高权威业务数据库。
- 移动硬盘题库由本地数据主机统一管理，其他电脑不得直接写主机移动硬盘题库目录。
- 其他电脑允许离线本地编辑，但联网后必须经用户确认和授权后同步到本地数据主机，不得静默推送离线变更。
- 微信小程序以只读为主，只允许财务个人资产导入、题库选题组卷、Word/PDF 导出等有限写入任务；不得直接修改核心教务表或题库基础数据。
- 工作顺序是先完成并验证底层多端数据架构，再逐步设计和优化微信小程序 UI，最后部署阿里云和小程序。
- 小程序 UI 优化不能只停留在首页；后续每一个小程序页面都必须在原有基础上逐页优化、逐页验证，并留下截图或检查记录。
- 后续验证由 Codex 主动执行并留痕；可使用浏览器、Playwright、微信开发者工具、阿里云检查脚本等工具。涉及账号、密钥、真实云部署或真实小程序发布时，仍以安全规则和可用权限为准。

## 每次修改代码后的默认流程

1. 运行与改动相关的测试；风险较高时运行 `npm test`。
2. 执行：
   - `git add -A`
   - `git commit -m "自动发布 YYYY-MM-DD"`
   - `git push gewu master`
3. 不再默认推送 `origin/master`。
4. 不再默认打包安装包。
5. 不再默认上传夸克网盘。

## 打包和上传

只有在用户明确要求“打包”“生成安装包”“上传夸克网盘”时，才执行打包和上传流程。

如果用户明确要求打包：

1. 递增 `package.json` 版本号。
2. 执行项目构建命令，当前项目优先使用：
   - `npm run build && npx electron-builder --win`
3. 找到生成的安装包/构建产物。

如果用户明确要求上传夸克网盘：

- Codex 使用：`node scripts/upload-quark-clean.js`
- Qoder 使用：`node scripts/upload-quark-qoder.js`
- 不使用旧脚本：`node scripts/upload-quark.js`
