# 云入口恢复记录（2026-09-13，UTF-8）

## 12:25 更新：8.11.11 已部署，真实导出暴露重复转换内存泄漏

- 8.11.11 固定提交 e3ad6bb29648 部署退出 0；部署前数据库备份与隔离恢复、权限校验通过。备份 SHA256 为 8ca5140f954a542df9519e99c8c62afd0b07d6d3d15925d5ee777f3ad4b9b50f，部署日志 SHA256 为 6efc1813007f57689527b220639239017f5c84161e59c369936cd6c11b811686。12:08 独立公网读回版本 8.11.11。
- 已单独处理早先明确指定的旧导出任务（尾号 0feed210c433）：新备份恢复验证后，以任务、账号、原时间、快照校验和、无执行租约等条件严格匹配，只标记中断，不重排队；原快照保持不变，其他旧任务未修改。维护备份 SHA256 为 619382402273ba410b135ee743949bfd20218967de4d1d63bde4dc3f73453034。对应 Python 2 项和隔离 PostgreSQL 成功/拒绝/重复执行测试通过。
- 新建的真实教师会话 108 题 Word 导出（尾号 7b3d6b0b77bc）失败，不能称正式导出通过：图片队列实际交付 25 张后，日志出现 JavaScript heap out of memory，容器重启次数变为 1；任务由执行租约保护标记 CLOUD_PAPER_EXPORT_INTERRUPTED。12:08 主机可用内存 967MiB，内存压力均值为 0；不是短信认证失败，也没有要求更新 NAS 镜像。
- 回归先复现：遇到首张未就绪图片即返回，后续图片未排入交付；此前题目的公式在每轮重试重复转换。另发现 MathJax tagformat 与 mathtools 将每个解析器闭包保留在全局，600 次转换的回收后堆内存从约 22MiB 增至 49MiB。
- 修复为：先顺序准备全部图片，完成后统一等待，全部就绪才转换公式；真正错误仍立即失败，无后台遗留请求。公式转换期间使用同步局部标签注册表并在 finally 恢复，保留所有公式包和每条公式的宏隔离；HTML handler 不再每次新增。600 次转换回归堆内存稳定在 24MiB，原生 Word 公式、PDF 字体与布局、租约、任务归档回归通过。
- 同一当前 108 题、115 张哈希核验原图：5 次等待图片均走完 115 项，堆内存 19–24MiB；随后在同一 256MiB 堆进程内顺序生成 Word/PDF/Word/PDF，耗时约 8/22/8/23 秒，回收后堆约 23/45/26/45MiB，最大 RSS 584356KiB。属于本地产品渲染回归，不是生产或 NAS 下载证据。
- 仅云服务源版本递增为 8.11.12，待部署；其他端未变更。本轮没有修改业务窗口、课程逻辑、历史地址权限或未跟踪 output 工作。

## 已恢复，尚未根治或完成多端发布

- 复查用户指定的“排查云端入口不可达问题”：上次存在内存与磁盘压力、旧容器积累、端口占用，并配置了 2GB swap。只作为本次排查线索，不直接套用结论。
- 用户允许临时使用既有部署密钥向 Workbench 认证；未保存凭据，认证失败后已清空输入。Workbench 的“禁用密钥认证”提示不等于真实 SSH 设置证据；恢复后既有密钥正常登录，未改密码、安全组或 SSH 权限。
- 用户完成阿里云短信安全验证后，控制台从停止中返回运行中，系统启动时间为 09:17。只提交一次正常重启，未提交强制重启。PostgreSQL 日志显示上次关闭不干净，随后完成 WAL 自动恢复并接受连接；不能声称关机过程完全优雅或据此保证所有业务数据已全面核对。
- `0c5050`：SSH banner 恢复，HTTPS TLSv1.3 握手恢复。早先 `/cloud-business/health` 的 404 是路径错误，不是健康接口失败。正确 `/cloud-business/api/health` 在 `bdd7c2` 返回 `ok=true`、PostgreSQL、版本 8.11.9；`7e2fb6` 内网同样通过。
- `71acf6` / `2ccb8e`：SSH、Nginx、Docker、云助手 active；正式 8.11.9 容器监听本机 3002，和 PostgreSQL 均连接 `gewu-cloud` 网络。系统盘 77%，约 8.8GB 可用；2GB swap 存在，正式云业务容器约 134MB。未重建 NAS、清理旧容器或部署新版本。

## 故障证据与仍需验证的因果关系

- `7e2fb6`：05:24:50 起反复出现 `Under memory pressure, flushing caches`；当前检索窗口无 OOM-kill 记录，不能称为已确认 OOM。
- `908feb` / `058aab`：05:20 尚正常；05:42 磁盘等待占 CPU 90.42%，负载约 34，阻塞任务 32 个，后续 41 个。读入约 108,535 KB/s，主要缺页约 126 次/秒，2GB swap 使用始终为零。运行值 `vm.swappiness=0` 来自 `/etc/sysctl.d/99-apsara-sysctl.conf`。这些证据支持内存回收和磁盘读取抖动，不证明某个单独进程是唯一触发源。
- 原任务 `paper_task_0fc7a87e-58f8-4e63-9326-0feed210c433` 创建于 05:22:54，最后更新于 05:24:43，仍是 `processing/rendering`。任务记录未改写、未重建；当前任务领取逻辑不会自动恢复卡在 rendering 的任务，后续需要带版本/执行所有权约束的中断恢复方案，不能直接 SQL 改回 queued。
- `a9ccb3` 只读取得该任务不可变快照：108 题、115 个图片引用、659465 字节，SHA256 `3d11fd9f225918e4e373c8336e7666d0ac0ec3245957ac5e17ecdd9fb5e185a8`。所涉题目全部资产 121 项共 12,478,381 字节，单项最大 1,221,614 字节；不是凭空认定超大原图。
- 本地隔离复现只替代图片字节为 1px，不能代替真实媒体测试。原实现 `54301c` 发起 115 次图片请求后因已有公式内容错误失败，错误时 RSS 约 166MB；未复现服务器整个故障，也没有生成通过验收的整卷。

## 已执行的可恢复内存策略调整

- `72e015` 新 PostgreSQL 备份 `/root/scheduling-backups/postgres/20260913-012902`，SHA256 `ad8ab1e25ddf800bf66be7da98e6ead47ca58278da86394b47c985d8f87afb1f`；隔离恢复通过，权限指纹 `046cf160303ba80ae0835241a3ccc659` 一致。
- `019301` 首次配置备份因 SFTP 独占打开方式失败，未写入生效配置；该失败目录保留，不冒称首次成功。
- `ebad38`：保存原厂配置和回滚说明至 `/root/scheduling-backups/20260913-memory-reclaim-013019`，独占新建 `/etc/sysctl.d/99-gewu-memory-reclaim.conf`，仅设置 `vm.swappiness=60` 并回读生效值。原厂文件不改，无重启、数据库写入或 swapoff。
- 回滚：仅移除本次创建的 `99-gewu-memory-reclaim.conf`，将运行值恢复为 0；原厂文件及备份保留。不能删除整个 sysctl 目录或备份目录。
- 60 是 Linux 文档默认值；值为 0 会延后启动交换。参见 [Linux VM 文档](https://docs.kernel.org/admin-guide/sysctl/vm.html#swappiness)。调整是缓解措施，不代替后续受控负载测试、容器资源预算或任务隔离。
- 调整后独立检查 `735269` 回读运行值与配置文件均为 60，原厂文件与备份逐字节比较一致；内存压力最近 10/60/300 秒均为零。`0569a9` 公网健康 HTTP 200，桌面题库与小程序云上下文无令牌请求均 HTTP 403，未放宽认证。空闲状态结果不等于受控负载验收。

## 本轮代码修复，尚未部署

- `124006` 新回归先失败：6 个题目同时启动 6 个资源请求；第一个返回 media_pending 时，另外 5 个仍在后台执行。外层提前失败后，下一轮任务可能与尚未结束的请求重叠。
- `paperExportRenderer` 按原有题目/资源/内容顺序逐一加载，失败时没有后台遗留工作，不更改纸张内容、公式策略、排序、API 或存储协议。取舍是图片等待逐项进行；真实整卷耗时仍待测，不能把每次重试的累计等待当作已解决。
- `335765`：资源并发/失败收尾、Word/PDF renderer、原生公式结构、任务处理器、worker 和媒体解析专项通过。`52710a`：产物存储和 PDF 校验专项通过。此前完整根回归不冒充本提交的全量结果。
- `ae17e5` 相同快照隔离检查仍按预期报告原公式错误，但仅发起 5 个图片请求，错误时 RSS 约 118MB。不能将采样 RSS 称为进程峰值，也不能称整卷导出成功。
- 自动规则 `8e3e5f` 判定 patch，云业务源版本 8.11.9 → 8.11.10，gateway 同属云组件联动；版本规则及各端独立版本测试通过。桌面、NAS、小程序版本不变。生产实际仍为 8.11.9。

## 后续固定顺序

### 10:20 导出执行保护和真实媒体诊断（未部署）

- 新增兼容性迁移，仅为导出任务添加 claim_token 和 lease_expires_at，不改请求协议、权限、题目快照或旧任务。每次领取生成独立执行标识，15 秒续期，5 分钟无续期后将已带租约的 rendering 任务记录为 failed/interrupted，不自动重新生成。
- defer、fail、renew 均校验当前执行标识和有效期；失去资格的执行进程不能覆盖新状态。产物入库、加密中转、存储任务和任务 attachment 在一个带行锁的数据库语句中提交，避免保存产物后崩溃留下永远 rendering 的状态。已进入 storage_pending 的任务不受 rendering 过期回收影响。
- `322131` / `516a89` 新回归先失败；`7e4655` / `d2ee97` 专项通过。真实隔离 PostgreSQL 验证租约过期、重领后旧令牌拒绝、无遗留存储任务、原子产物 attachment 和旧版本无租约任务不被误回收；后续增加两个独立数据库连接竞争同一任务的检查，完整回归 pretest 中也通过（`2638de`）。运行时续期、失败收尾和产物仓库专项 `21cfa1` 通过。
- 云完整测试 `40751` 的 pretest 与 test 已完成，posttest 中途退出 1；独立复查教师排课作用域 `21847e` 通过。第三次 posttest `42241` 最终在 `1c3766` 退出 0，覆盖教师/学生保留历史操作、课程和排课保真、机构、学校、出勤费用、权限与版本冲突；其间未改产品代码。可确认三个阶段分别通过，不能声称原整条 npm test 退出 0；先前中途退出原因尚未确证。
- `226fdc` 再次只读取得旧任务所涉 108 题的当前云内容，只有已复核三题从版本 2 变 3。诊断快照 SHA256 `6dfc75704e472a6654e7d580d9ba59d67f854cee89e0ebf9eca2891b982b5303`。两份原件哈希与导入时一致；从原件取得全部 115 张匹配哈希图片，合计 12,467,183 字节，无缺图，没有生成 NAS 下载任务。
- `7df65d` 实际产品 Word 导出器在本地 256MB V8 堆上限的独立子进程完成：108 题、115 次真实图片读取，10.451 秒，11,288,660 字节，SHA256 `987b9d7c6f14f5097cd945e12d0b90aaf33480350f3842d2e2c92b0a1538c625`，操作系统报告 maxRSS 354148KB。结构检查有 473 个原生公式和 115 个行内图片；这不能替代 Word 中的公式编辑和逐页验收。
- `e7e145` 同样条件的实际 PDF 导出器完成：38.595 秒，18,031,001 字节，SHA256 `c2f68aceb400903dd49f4f304e4efeb5dd1cf5eb9f8a954a019856ee4eae62e6`，maxRSS 614772KB。`7c3b93` 严格 PDF 解析成功，未加密，76 页；第 1、2、5 页已转图实看，文本提取为空的第 5 页实际是两张图，不是空白页。图片占幅和图文跨页仍需逐页核对，不称排版已通过。
- 文档规范要求渲染实看；`a7ca89` 捆绑 Word 渲染器缺少 LibreOffice，已记录失败，不反复重试同一路径。改用已安装 Word 的独立只读实例，窗口标题确认打开了完整 DOCX，但自动分页耗时异常，尚无完成回执；原先用户打开的 Word 实例不受影响。
- `27b420` 在核对 PID 与创建时间后结束本次长时间未完成的只读 Word 检查进程 68604，原文件及此前 Word 进程 56888 保留；自动分页未通过，不宣称已经逐页检查了 Word。版本规则与各端独立版本测试均通过。
- 上述是真实云内容加本地原件媒体的受控渲染诊断，不是正式云导出、NAS 成品下载、小程序交互或发布验收。源版本仍为已准备但未部署的 8.11.10，不重复递增其他端版本。

### 10:55 实际 Linux 镜像资源验证与部署保护（未部署）

- 使用固定提交 `9a3f877571a8` 导出干净构建输入，没有创建工作树或带入暂停的权限文件。本地实际 Dockerfile 构建镜像成功，镜像 ID `sha256:91a1aac65fe6c651f88d5738a7b191a6ec1c03a4e67582915f6e1f8f47f5fbb0`；基础层首次下载较慢，过程中没有重复构建。
- `fb45aa`：隔离容器禁网、未连接数据库或 NAS，只读挂载上述当前 108 题及 115 张原图；实际产品渲染器与产物仓库完成 Word/PDF 生成、加密和解密 SHA 校验。768MiB 内存、1GiB 内存与交换总上限、1.5 CPU、192 PID 限制下，Word 21.131 秒，PDF 40.982 秒；最终退出 0、OOMKilled=false，cgroup 内存峰值 608821248 字节。测试仓库使用内存查询替身，真实 PostgreSQL 原子归档由独立数据库测试覆盖，不将本项写成云/NAS下载验收。
- 另用部署默认 Node 堆配置重复同一受限镜像测试（不额外传 256MB 堆参数），`ac8995` / `31aff6`：Word 18.746 秒、PDF 42.377 秒，加密解密一致，cgroup 内存峰值 594886656 字节、oom/oom_kill 均为 0。不能将本机 Docker 宿主机结果当作小规格生产服务器的并发负载验收。两轮均有 Fontconfig 配置警告，尚未确认影响，不隐藏该警告或称排版全部通过。
- 部署候选容器禁止启动正式导出 worker，限定 256MiB/0.5 CPU；旧正式容器停止后，提升的新正式容器才启用 worker，限定 768MiB/1.5 CPU。脚本核对 Docker 实际内存、交换、CPU、PID 配置及 worker 开关，失败不能进入后续健康或成功回执。
- 发布权限检查增加执行租约列类型和有效索引门禁；`37bac9` 实际隔离 PostgreSQL 测试退出 0，验证旧结构拒绝、迁移后通过、删除隔离测试索引后再次拒绝。`57359d` 部署测试 50 项、`dce35f` 发布检查 4 项通过；实际 Git Bash 检查生成脚本语法及缺失/重复 worker、错误资源配置的失败退出。
- `fea525` 版本规则、各端独立版本与 diff 检查通过。沿用已准备但未发布的云 8.11.10，不再递增版本，也不更新未变更的 NAS。生产仍为 8.11.9，旧导出任务尚未处理；这些结果不等于新版本已发布。
- 部署源检查发现现有上传遍历会带入暂停的工作区权限改动；正式部署前必须让构建和迁移均固定为已审查提交，不能仅给未提交代码套一个提交版本号。保留这些文件，不恢复、覆盖或暗中发布。

### 11:15 固定部署输入与再次只读复查

- `a384c9` 集成回归先失败：原迁移入口不接受固定源码，部署也没有生成已提交快照。现构建上传和全部控制面/业务迁移均读取同一份固定 HEAD 的归档，版本不匹配或提交变化在连接生产前拒绝；只传既有 dotenv 路径，凭据不复制进快照。没有注册工作树或修改原工作区。
- `6194c9` 真实快照检查发现缺少版本清单，已补齐归档依赖；`776cfb` 复验可以在没有原工作区文件的快照中导入全部迁移脚本、生成并校验 M20–M28 SQL。不会把导入模块成功当作已经应用生产迁移。
- `cc329e` 部署常规入口现包含源归档回归，共 56 项退出 0：脏文件与未跟踪迁移不进入快照，原文件保留；旧提交、路径穿越、符号链接拒绝；构建与迁移路径一致，备份和失败回滚顺序保持。`01cb12` 业务迁移 8 项、发布检查 4 项、独立版本检查通过。
- `5c5279` 11:11 只读生产复查：内网健康仍为 8.11.9，启动时间 09:17:22；可用内存 517MiB，近期内存压力均为 0，swap 尚未使用，swappiness 60，磁盘可用 8.8GiB。没有通过扩大权限或重建 NAS 恢复服务。
- 原目标任务仍为 processing/rendering，进度 10，原更新时间及快照校验摘要不变，result_artifact_id 为空；同类当前共有 4 条。尚未确认另外三条的来源，不批量更改、不重复创建替代任务。正式源码 app.js 哈希与工作区暂停权限版本不同，不能将工作区状态当作已部署状态。
- 前一资源保护提交 `bb05b053` 推送时首次 GitHub TLS 握手失败，重试成功；`65f3f9` 独立回读 gewu/master 精确匹配。没有关闭 TLS 验证或改变远端。

### 11:24 云 8.11.10 已实际部署，其他端未发布

- `7d5d95` 正常部署入口退出 0，固定提交 `1260d14dc41755edc4c180fbc3cfbd72b6ddb363`；本次独立回执目录后缀 `gewu-cloud-deploy-20260913-8col6z_1`，日志 SHA256 `0bcc43c84c124a28d037504ef6a8feb9210473e6fffd4fb2d760178e5fb60b5b`。原受保护 output 未改写。
- 新 PostgreSQL 备份 `/root/scheduling-backups/postgres/20260913-032102`，SHA256 `914f69c5107bc24dad6b4f57f677c601cda88a244452048c7f771370a72c9d1c`；隔离恢复及权限一致性检查通过后才应用迁移。旧服务保留为 `gewu-cloud-business-api-rollback-8.11.10-1260d14dc417`，原镜像 `8.11.9-11c58e342e13`，退出 0；网关旧代码另按原部署入口备份。
- 候选实际微信登录码、离线信任公钥、内网健康和资源配置核验通过后提升；正式公网健康、权限契约、旧网关四个权威接口 410 及两条 websocket 拒绝检查通过。云回执 healthContractSha256 `a1ae202e7f963dd8d65235e1ddb6aaca35638cd1ce3e1067b1e34f8ac171f33f`、authorityContractSha256 `4b231700ed44a3a015e4e36302bbe254fcc9001d6d603879ea928d601a9ee492`。
- `5b724e` 部署后独立读回：内网/公网均为 8.11.10；实际镜像 `8.11.10-1260d14dc417`，内存 805306368、内存与交换总上限 1073741824、NanoCpus 1500000000、PID 192，OOMKilled=false。执行租约列/索引门禁返回 true；正式 app.js SHA256 `8a3f5405f67283d6ad1b2bc4ef7363902e20b9db2e889e76f4cb39481cbf5308` 精确匹配提交，不等于工作区暂停的权限版本。
- 部署后可用内存 793MiB，swap 实际使用 313MiB，最近 10 秒内存压力为零，60/300 秒仍有少量历史压力，磁盘余 8.1GiB。不能据空闲状态宣称满载或根因完全解决。
- 原任务仍是原 rendering/原快照/无产物，四条旧任务均未自动回收；这符合无租约旧任务不擅自重跑的保护，但仍需单独中断处理。NAS 没有重建或升级；桌面 OSS、小程序上传未执行，整体为部分发布。

### 11:30 PDF 字体污染已定位修复，本地验收中

- 按 PDF 规范追加实看旧完整 PDF 第 3、4、6 页，第 6 页解析末尾出现乱码；对应当前云题目分析文本正常，不通过改题目内容掩盖导出错误。
- `3e0d67` 最小真实 PDFKit/SVG 实验显示正文原为 NotoSansCJKsc-Regular，公式 SVG 文本 fallback 后变为 Times-Roman，后续中文均沿用该西文字体。`116e61` 加入产品回归后按预期失败。
- 只在 inline SVG 绘制后恢复 PDFKit 原字体来源、字体族及字号，成功和异常路径均恢复，不改变题目、业务、权限、图片或 Word 原生公式策略。`a9d521` SVG 实际字体回归、异常收尾、原 renderer/资源加载/原生 Word 结构、任务及租约处理专项退出 0；`009b9a` 独立复查正文前后字体相同。
- `45ba74` 相同当前 108 题与 115 张真实原图重生成本地 PDF，29.557 秒、18,151,047 字节，SHA256 `1ae8bf785c6fe19411c6797fd7b363ee38f91c53aa0f44bc4cf6b4df3d0d17d3`，maxRSS 580852KB。`1636e8` 严格解析 83 页、未加密；页数从 76 增加是恢复正文正常字宽后的重排，不据此宣称完成排版验收。
- 本轮实际查看修正 PDF 第 6、7 页，先前乱码位置恢复中文，解析跨页继续可读。大图占幅、公式细节及其余页面仍须核对；这不是云任务或 NAS 下载的验收产物。文件为诊断目录中的 `full-product-font-fixed.pdf`，原 PDF 保留。
- 版本规则 `57de3a` 将这项独立后续修复判为 patch：云源码 8.11.10 → 8.11.11，版本和各端独立版本测试通过。8.11.11 尚未部署，当前生产仍为已核验的 8.11.10，其他端不递增。

1. 确认恢复后的公网健康、未授权请求拦截及内存配置；保留故障证据，不清除日志或删除旧任务。
2. 完成真实图片条件下的受限本地渲染、公式修正及 rendering 中断恢复门禁，给云服务明确的资源保护，避免再次用生产机做无约束压力试验。
3. 09:45 已通过新备份、当前版本核验、正常 REST 和逐题回读完成三题公式修正（版本均为 3），临时会话撤销，见真实导入记录。旧任务快照没有改变，不能据此冒称旧整卷导出成功。
4. 按兼容矩阵部署实际需要变更的端，再回到既定桌面业务保真、小程序逐页面多角色和真实 Word/PDF 验收。当前不构成任何一端的新软件发布，更不构成多端发布完成。
