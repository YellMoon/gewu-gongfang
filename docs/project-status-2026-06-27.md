# 格物工坊阶段状态记录（2026-06-27）

本文件记录当前阶段的真实状态，避免继续执行时被早期 `TASK.md` 复选框误导。当前阶段完成后仍按项目持久规则只提交并推送 `gewu/master`；除非再次明确要求，否则不打包、不上传夸克网盘。

## 已完成并验证

### 阿里云后端

- 线上 API 基址：`https://physicsedu.xyz/scheduling`
- 生产服务：`scheduling-backend-prod`
- 生产端口：`3002`
- 当前版本：`5.0.34`
- 已验证：
  - `/api/health` 返回 `ok: true` 和 `version: 5.0.34`
  - 云中继 smoke 通过
  - 主机云中继任务 smoke 通过

### 微信小程序

- AppID：`wx3d570539bbe6ba1b`
- 当前上传开发版：`5.0.34`
- 当前状态：已提交审核，等待审核。
- 审核说明文档：`docs/miniapp-review-guide.md`
- 审核通过后的下一步：进入微信公众平台发布线上版。发布线上版属于对外生效动作，执行前需要再次确认。

### 本地数据主机与存储

- 当前电脑角色：本地数据主机。
- 移动硬盘题库路径：`I:/GewuQuestionBank`
- 本地缓存路径：`D:/GewuQuestionBankCache`
- NAS 备份路径：`\\192.168.124.2\lin1225_存储空间1\GewuBackup\GewuQuestionBankBackup`
- 已验证：
  - 题库 store 可识别
  - 移动硬盘题库目录可用
  - 本地缓存可用
  - NAS 备份可用

### 权限边界

- 小程序区分管理员与学生。
- 管理员使用管理入口。
- 学生不展示管理快捷入口，只展示本人相关课表，并保留题库选题组卷和导出能力。
- 小程序页面文案避免出现不存在或已删除模块，例如“教学工具”“学籍管理”“分班”“档案”等。

## 仍需继续推进

### 微信小程序审核后动作

1. 若审核通过：
   - 确认后发布线上版。
   - 发布后检查首页、角色入口、课表、题库组卷导出、财务资产导入入口。
2. 若被驳回：
   - 保存驳回截图和原因。
   - 按驳回类型修复代码、配置或审核说明。
   - 重新执行测试、上传开发版、提交审核。

### 离线同步稳定性

当前已有本地优先同步、云中继、任务队列和权限限制基础；后续仍应继续做更完整的离线同步验证：

- 客户端断网时编辑教务、题库、财务等数据是否进入 pending 队列。
- 恢复联网后是否先提示获取同步权限，而不是静默覆盖主机。
- 主机是否能展示待同步、冲突和最近同步日志。
- 冲突数据是否能被阻止并给出可理解原因。

### 存储与备份增强

- 继续验证移动硬盘热插拔后的题库读写提示。
- 继续验证 NAS 备份断开、恢复后的备份状态。
- 后续如 SSD 空间不足，支持扩展到新硬盘或迁移题库目录。

## 当前默认执行规则

- 阶段性代码改动后运行相关测试；风险较高时运行 `npm test`。
- 提交信息使用：`自动发布 YYYY-MM-DD`
- 只推送：`gewu/master`
- 不默认打包。
- 不默认上传夸克网盘。
