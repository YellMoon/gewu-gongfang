# 投入使用收口清单（2026-09-13，UTF-8）

## 09:45 三题公式已经正式接口修正

新备份通过隔离恢复与权限校验后，三题正式更新并逐题完整回读，版本均为 3；本次临时会话及设备登记已撤销。详见 [真实导入记录](real-question-import-completion-2026-09-13.md)。整卷旧导出任务仍需中断处理，云 8.11.10 尚未部署；没有将本次数据修正称为多端发布。

## 09:30 云入口已恢复，导出资源缺陷已补测试修复

用户短信验证后正常重启完成，公网及内网 `/api/health` 已回读正式 8.11.9；新数据库备份通过隔离恢复。历史监控确认内存压力及严重磁盘等待，原 swap 策略为 0，已备份并可恢复地调整为 60。导出器的无界并发及失败遗留请求已复现并修复，云源版本准备为 8.11.10，尚未部署。原 108 题任务仍是中断的 rendering，未重复创建。证据、边界和后续门禁见 [云入口恢复记录](cloud-recovery-2026-09-13.md)。以下旧状态保留为排查历史，不代表当前入口仍不可达。

目标日期为 9 月 14 日；这是排期目标，不是尚未验证的完成承诺。完整架构、原业务保真和多端验收标准不缩减。不追加界面改版，不启用子 agent，不重复盘点或导入已经确认的数据。

## 已取得的证据

- `9c940516` 已推送 gewu/master：两份真实原件对应 108 道题全部在生产云提交并发布，逐题五个内容字段回读零差异。两次生产备份均在独立数据库恢复验证。详见 `real-question-import-completion-2026-09-13.md`。
- 当前 Node v24.15.0 / ABI 137 的内存 SQLite 打开检查成功。没有为测试重新安装桌面或切换原生依赖。
- 完整根测试第一次在课程更新的旧源码字符串断言失败（`96194/440934`）；该断言未涵盖已恢复的结课状态专用草稿。改为执行实际缓存方法，覆盖普通编辑、结课、开课与三种版本基线。专项 `dbac45` 退出 0，原课程状态 12 组保真继续通过。
- 第二次完整根测试在小程序课表旧源码断言失败（`39466/aaae88`）：当前保留历史课次名称的投影类型与旧测试不符。新增实际 loadData 执行检查，覆盖正常课程名称、已删除课程的课次名称、缺失课程、访客不读取业务缓存、源缓存不被改写。专项 `1d0b2a` 已通过；没有改动产品页面。
- 小程序四轮全角色检查未全部通过；诊断截图返回 terminated。控制台有微信运行时 timeout。随后当前页与最小 evaluate 可读，不等于完整自动化恢复或视觉验收通过。
- `72224/2fde76` 架构整段测试进入云服务 posttest 后退出 1，末条成功记录为 teacher schedule scope，没有后续断言诊断。紧接的课时计费快照专项单独执行 `33ddf5` 退出 0；不以该结果替代完整入口。完整回归仍未通过。
- 教师限定重跑 `35668/afa013` 同样在刷新后的启动状态读取超时。启动等待现在沿用已有刷新瞬态错误分类，最多三次桥接失败，权限等其他错误立即终止；不是无限等待或改变产品启动逻辑。新增回归 `b6effb` 先失败，修复后 `33ddf5` 的启动 3 项及原角色脚本 30 项通过；另一次真实教师检查进行中。
- 后续 `55140/5f9dfb` 退出 0：真实云测试教师会话的课程、课表和题库页面路由及身份正确；当前题库 40 张题卡、40 个答案开关、40 个试题篮操作入口。脚本完成会话清理及原会话恢复，没有新增截图，未据此宣称按钮交互、108 题逐一显示、全部角色或全页面验收通过。

## 固定执行顺序

### 05:34 当前状态更新（UTF-8）

- 完整根入口 `npm test` 本次退出 **0**，643.54 秒。日志 SHA256：`4ac813daec0c942f161d4cb8929bf6226d3b88c913edca1008e9ef58bf2b0114`；本机记录目录后缀 `gewu-full-regression-20260913-jy5m9b3l`，含 `result.json` 和完整日志。运行基于 HEAD `787a3206` 及原有未提交工作区，不等同于纯提交或生产环境验收。
- 本次为 Node 子进程增加只记录启动/退出的诊断，454 次启动均取得退出记录；不改变断言或命令退出码。之前两次 `0xC0000409` 的原因仍未确认，不能说已修复本机进程崩溃。本条更新早先“完整回归尚未通过”的当前状态，保留失败历史。
- 已复用生产发布的 108 题创建一份完整 Word 导出任务；最后成功回读状态为 `processing/media_pending`。其后公网 HTTPS 连接超时，SSH 在认证前未收到协议握手；没有下载到本次整卷 Word/PDF，不能计为整卷验收通过。原任务保留，重连只查询同一任务，不重做导入。
- 没有变更业务窗口、导航布局或权限范围，没有部署、NAS 切换、桌面 OSS 发布或小程序上传。云端元数据及本地显示修复仍须进入实际发布门禁。

以下顺序不变；第 1 项已取得一次完整通过，后续只在代码变更风险需要时重跑，不因续转重复跑全套。

1. 完整回归门禁：修正失真的测试时必须补实际行为断言，不能删除验收条件；定位真正的业务回归后只恢复原行为。未出现完整入口退出 0 前不宣称全量测试通过。
2. 真实使用门禁：教师桌面登录、静默设备登记、学生/课程/排课/调课、离线不提交、确认后云端读回；小程序全部注册页面及适用角色、受限与离线状态。已有成功证据复用，失败只重测对应缺口。
3. 题库门禁：对本次完整导入题目检查来源/知识点、答案开关、试题篮、组卷编辑、Word/PDF 下载与实际打开；重点复验五道修正题。公式须为可编辑内容，不以公式图片兜底。
4. 发布门禁：确认生产数据库备份及恢复方案，再按统一兼容性矩阵部署确有变更的端、核验云端健康与权限、小程序开发版上传、桌面 OSS 安装包和 feed。各端独立版本；NAS 不随其他端陪同升级。

最终独立审计仍属总目标；用户当前禁止子 agent，不擅自启动。小程序正式发布仍需微信平台审核，开发版可测试但不等于正式发布。任一适用端缺少证据，只能记录部分发布，不能宣布全部投入使用。

## 06:08 云连接与控制台核验（UTF-8）

- 本轮公网 SSH/TLS 再检查仍在握手阶段超时。已确认本机存在 OneLite 隧道，业务域名解析为 198.18.0.0/15 范围的虚拟地址；指定外部 DNS 的查询也返回同一虚拟地址。这证明当前 DNS 路径受隧道影响，不单独证明超时由隧道引起。
- 使用已配置服务器公网 IP、仅为诊断连接指定 WLAN 出站接口的对照仍超时；没有关闭 VPN、改系统路由、改 DNS、弱化证书验证或修改服务器。该逐连接方法依据 [Microsoft IP_UNICAST_IF 文档](https://learn.microsoft.com/en-us/windows/win32/winsock/ipproto-ip-socket-options)。
- 已复用 Chrome 中原有阿里云 ECS 控制台。刷新后明确提示登录超时，因此之前页面缓存的“运行中/正常”不作为当前健康证据。恢复登录页已打开并保留，已请用户扫码恢复；不是要求用户替格物工坊做测试登录。没有盲目重启服务器、改安全组或安装监控插件。
- 108 题原 Word 导出任务继续保留；没有为超时创建替代任务或重复导入。恢复入口后先只读核验该任务、云进程和资源状态，再决定下一步。
- 同期已完成财务相关三页的直接进入拦截及未登录实测，详见页面审计记录；本轮仍未部署或发布任何端。

## 06:16 NAS 交叉核验（UTF-8）

- 复用 Edge 中已登录的 NAS 管理页，实际打开 Docker 容器列表和详情。唯一运行中的项目容器为 `gewu-storage-agent-8.8.2-candidate`，镜像标签 `gewu-storage-agent:8.8.2`，页面显示已运行 8 天；这是管理页运行证据，不等于镜像内容哈希或端到端健康验证。
- 该容器日志在 2026-09-13 05:24:58 至 06:16:34 持续出现 `storage-agent retrying: STORAGE_CLOUD_UNAVAILABLE`。同一日志中 02:35:57、02:36:19 尚有任务 verified 记录。结合本机 SSH/TLS 握手超时，确认云连接问题同时影响 NAS；不能将其归咎于浏览器控制，也不能据此确定服务器故障或具体网络根因。
- 源码 `storage-agent/src/cloudClient.js` 中该错误用于请求异常或不可用响应，真实 HTTP 4xx/5xx另有 `STORAGE_CLOUD_HTTP_*` 分类。因此没有依据通过升级镜像、重建容器或更换凭据解决本次问题。本轮没有执行上述操作，亦未查看环境变量中的凭据。
- 云端入口恢复后先核验原 108 题 Word 任务与进程/资源状态，接续下载和实际打开验证；不重新导入试题、不重复建任务、不以本地测试替代生产验收。

## 控制台登录恢复后的新证据（UTF-8）

- 用户恢复阿里云登录后，原控制台已能读取实时数据：实例运行中，CPU 读数约 11.7%～15.9%。公网 SSH/TLS 对照 `3b4bf6` 仍分别在 banner 与 TLS 握手超时，登录恢复不等于业务服务已恢复。
- 官方健康诊断明确返回“云助手状态异常，暂不支持诊断”；内存/磁盘监控显示插件安装失败。没有因此安装插件或修改实例配置。
- Workbench 现有密码凭据登录尝试返回“无法使用密码登录方式”；只说明该方式失败，不能据此断言密码错误或修改 SSH 安全配置。项目部署配置检查显示使用 SSH 密钥、未配置密码；Workbench 的已保存密钥凭据列表为空。
- VNC 页面成功连接实例，实际画面显示 Ubuntu 24.04.4 LTS 的 tty1 登录提示。没有重启、重置密码、开启会话管理或放宽安全组。临时使用现有私钥向 Workbench 认证属于新的凭据传输，已单独说明用途并请求确认；不保存凭据、不新建密钥或授权。
