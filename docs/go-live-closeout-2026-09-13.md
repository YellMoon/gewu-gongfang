# 投入使用收口清单（2026-09-13，UTF-8）

目标日期为 9 月 14 日；这是排期目标，不是尚未验证的完成承诺。完整架构、原业务保真和多端验收标准不缩减。不追加界面改版，不启用子 agent，不重复盘点或导入已经确认的数据。

## 已取得的证据

- `9c940516` 已推送 gewu/master：两份真实原件对应 108 道题全部在生产云提交并发布，逐题五个内容字段回读零差异。两次生产备份均在独立数据库恢复验证。详见 `real-question-import-completion-2026-09-13.md`。
- 当前 Node v24.15.0 / ABI 137 的内存 SQLite 打开检查成功。没有为测试重新安装桌面或切换原生依赖。
- 完整根测试第一次在课程更新的旧源码字符串断言失败（`96194/440934`）；该断言未涵盖已恢复的结课状态专用草稿。改为执行实际缓存方法，覆盖普通编辑、结课、开课与三种版本基线。专项 `dbac45` 退出 0，原课程状态 12 组保真继续通过。
- 第二次完整根测试在小程序课表旧源码断言失败（`39466/aaae88`）：当前保留历史课次名称的投影类型与旧测试不符。新增实际 loadData 执行检查，覆盖正常课程名称、已删除课程的课次名称、缺失课程、访客不读取业务缓存、源缓存不被改写。专项 `1d0b2a` 已通过；没有改动产品页面。
- 小程序四轮全角色检查未全部通过；诊断截图返回 terminated。控制台有微信运行时 timeout。随后当前页与最小 evaluate 可读，不等于完整自动化恢复或视觉验收通过。
- `72224/2fde76` 架构整段测试进入云服务 posttest 后退出 1，末条成功记录为 teacher schedule scope，没有后续断言诊断。紧接的课时计费快照专项单独执行 `33ddf5` 退出 0；不以该结果替代完整入口。完整回归仍未通过。
- 教师限定重跑 `35668/afa013` 同样在刷新后的启动状态读取超时。启动等待现在沿用已有刷新瞬态错误分类，最多三次桥接失败，权限等其他错误立即终止；不是无限等待或改变产品启动逻辑。新增回归 `b6effb` 先失败，修复后 `33ddf5` 的启动 3 项及原角色脚本 30 项通过；另一次真实教师检查进行中。
- 后续 `55140/5f9dfb` 退出 0：真实云测试教师会话的课程、课表和题库页面路由及身份正确；当前题库 40 张题卡、40 个答案开关、40 个试题篮操作入口。脚本完成会话清理及原会话恢复，没有新增截图，未据此宣称按钮交互、108 题逐一显示、全部角色或全页面验收通过。

## 固定执行顺序

1. 完整回归门禁：修正失真的测试时必须补实际行为断言，不能删除验收条件；定位真正的业务回归后只恢复原行为。未出现完整入口退出 0 前不宣称全量测试通过。
2. 真实使用门禁：教师桌面登录、静默设备登记、学生/课程/排课/调课、离线不提交、确认后云端读回；小程序全部注册页面及适用角色、受限与离线状态。已有成功证据复用，失败只重测对应缺口。
3. 题库门禁：对本次完整导入题目检查来源/知识点、答案开关、试题篮、组卷编辑、Word/PDF 下载与实际打开；重点复验五道修正题。公式须为可编辑内容，不以公式图片兜底。
4. 发布门禁：确认生产数据库备份及恢复方案，再按统一兼容性矩阵部署确有变更的端、核验云端健康与权限、小程序开发版上传、桌面 OSS 安装包和 feed。各端独立版本；NAS 不随其他端陪同升级。

最终独立审计仍属总目标；用户当前禁止子 agent，不擅自启动。小程序正式发布仍需微信平台审核，开发版可测试但不等于正式发布。任一适用端缺少证据，只能记录部分发布，不能宣布全部投入使用。
