# 完整试卷、讲义导入与后续验收

## 15:20 小问源结构去重与生产修正（UTF-8）

- 解析器三个入口停止同时把小问内容追加到 stem；后续文字、公式、插图只进入原小问。先失败回归后修正；`998ddb` 重新运行完整 Python 解析套件 54 项通过，`3ae45c` 存储代理 12 组测试通过。新增修正准备器和正式接口执行回归 `c6df93` 覆盖冲突、备份失败、持久日志、断线后读回续做、旧公式修正保持有效。
- D 盘两份已导入原件只读重解析：试卷仍 20 题，讲义仍 88 题；试卷索引 13–19 的题干需去重，其他内容字段、子题、公式清单和资产无差异。索引 13 的两张图仍跟随原小问。`872d4a` 确认 7 个正式修正题干逐字等于原件重解析结果。
- 正式计划原始文件 SHA256 `9629d2c6e87654293bce01cf7d872c69169e8452bd48c683e00a0ae19bfb71a2`，规范化计划哈希 `739c733a77d858763eb551725f11353a7e93e2043db132b6582cce6b54fd204e`。仅删除逐节点核对相同的小问副本；独立小问、答案、解析、选项和元数据均不改。未知位置的额外公式保留，后续需原件定位，不把“保留”说成布局正确。
- 备份 `/root/scheduling-backups/postgres/20260913-071431`，SHA256 `aabfb1233e19d8e36fa718efc3bbd2f9021e19c18aca2ab8df2293b14234f505`；隔离恢复及所有权/权限校验均 true。恢复时使用该 custom dump 到新空库，不直接覆盖生产库；执行日志还保留逐题完整前值供限定恢复复核。
- `090136` 正式接口执行退出 0，7 题全部 committed-and-readback：索引 18 版本 3→4，其余六题 2→3。临时会话和设备登记已撤销，activeSessions=0。没有 SQL 直接更新题目，没有重导 108 题，没有创建新 Word/PDF 任务。
- `8d85e3` 独立生产读回核对 7 题、25 个小问：答案、解析、选项、所有非 stem 结构段、元数据、全部不同公式/图片节点均保持不变。只读快照 SHA256 `34a1c964da174803602779550e644774cc2f2628fd03ad52fa29872d13d178a1`；脱敏核对报告 SHA256 `ff71f5db32899f12d78842628313f858eb757354d765a7b964ec02e09893bee7`。
- NAS 的 Dockerfile 确实包含此 Python 解析器，因此下一次必要镜像更新使用已准备源版本 8.8.3，当前容器仍是 8.8.2。独立版本规则测试 `618d84` 通过，没有递增桌面/小程序/云版本。当前云已为 8.11.14，下方 13:16 的“尚未部署”仅是历史状态。各端实际显示和重新导出的分页布局尚待复验，仍为部分发布。

## 13:16 导出验收更新

没有重新导入，沿用修正后的 108 题。生产云 8.11.13 与 NAS 8.8.2 已完成真实 Word/PDF 生成、归档及下载；Word 473 个原生公式、115 张原件图片哈希核对通过，Microsoft Word 实际打开显示 64 页；PDF 严格解析及全部 83 页渲染通过。产物哈希见云恢复记录 13:16。全页布局与桌面/小程序多角色实际操作仍未全部验收，不用本次 API 测试冒充 UI 测试。随后 Word 图片比例修复在云源版本 8.11.14，尚未部署。

## 12:25 导出验收更新

沿用已修正的 108 题和 115 张原图，没有重新导入。8.11.11 真实教师会话整卷任务因公式解析内存泄漏失败，未取得正式云/NAS Word/PDF 成品。8.11.12 源码修复已通过 5 次图片等待与两轮完整 Word/PDF 本地生成，尚待部署、真实下载和逐页验收；不能将本地回归写成小程序或桌面操作通过。详见云恢复记录同时间段。

## 09:45 三题公式已正式修正并回读（更新状态）

- 云入口恢复后重新取得在线测试会话，登记和会话检查均 HTTP 200；没有使用真实用户密码，不能当作密码或微信手机号登录验收。
- 正式提交前重新完整分页预检三题基线版本，并创建备份 `/root/scheduling-backups/postgres/20260913-014453`。隔离恢复、所有权和权限验证通过，SHA256 `d67ca36ed3599347184498c6ac125794f4324f6ea9830da6eecf3465dd082c82`，权限指纹 `046cf160303ba80ae0835241a3ccc659`。
- `607ff9` 实际执行退出 0：试卷索引 6、8、18 的三个 `question.update.v1` 均 committed，逐题完整回读版本均为 3，verifiedCount=3。稳定计划哈希 `8e0d62825e5774cfa37d0da1500022afa588cd970f5d311e7cfff3220131ef4f`，结果文件 SHA256 `434609a22578d47ef5bc80bc4a672f59a1ad9a16ec2cf16cc61abe36b31138f6`。
- 没有改其他题目、标签、来源或旧导出任务快照；本次临时测试会话、安装实例、设备和关联已按精确 fixture 范围撤销，activeSessions=0，审计记录保留。撤销在维护连接的独立受限事务执行，没有给业务 writer 添加 owner 权限。
- 实际执行暴露备份命令的普通日志混入 JSON 回执问题。先补两项失败回归，再增加 `--json`：诊断写 stderr，stdout 仅包含完整验证后的回执；失败不输出成功回执。`13ba77` 的 7 项 Python 测试和正式修正入口测试通过，随后上述真实备份与提交验证通过。
- 本节替代下方历史“尚未提交三题”的状态，不代表整卷 Word/PDF 已导出成功。原 108 题导出任务仍保存旧版本快照，须单独完成中断处理，再以修正后内容生成和检查新产物。

## 已完成的真实数据操作

2026-09-13 对此前两个导入任务继续提交，没有重复上传原件或重建导入任务：试卷 20 道、讲义 88 道。原已发布各 1 道；本次补齐 106 道，当前共 108 道已提交并发布。这是题目发布，不是多端软件发布完成。

- 只读候选快照 `ad5a90`：20/88 项，分别剩余 19/87 项；快照 SHA256 `290d2f0035666bef53a36e3410bc6f8fab6e122bd5dbbd4e850b4cbd925f32ac`。
- 提交前云库备份：`20260912-192544`（UTC），SHA256 `f664c65235c2d43f884dd74c5803ea5ef198fe6c175c584c9130d891ecbb2305`。隔离恢复成功，权限指纹 `046cf160303ba80ae0835241a3ccc659` 一致。备份位于云服务器 `/root/scheduling-backups/postgres/20260912-192544`；恢复时先恢复到新空库，不覆盖生产库。
- 实际提交 `61034/56fb2b`：106 次正式云端 `question.create.v1` 均 committed；随后发布前核对失败，因此该次进程退出 1，不冒称整次通过。独立只读检查 `34f89f` 确认两项任务均 submitted，新题 106 道均 draft，原 2 道 published，数据库 writer 无 owner 成员资格。
- 发布前备份：`20260912-192946`（UTC），SHA256 `8e0f94e86ddcee61a43dfdcbc9c3d00420d3b7b81857a6d5142fe17a70ccc078`，同样通过隔离恢复及权限验证。备份位于云服务器 `/root/scheduling-backups/postgres/20260912-192946`。
- 继续执行 `83704/bd58cd` 退出 0：本次新增提交 0、发布 106，回读本次所有 108 个题目 ID 均 published。没有再次创建题目，也没有修改其他题目。提交计划 SHA256 `e1bda393cd3117bd2610c0f7dcfd1c9d13a94489b09d63be99539b4b261cc82c`。
- 独立云库回读 `8ad137` 退出 0：试卷 20、讲义 88；题干、选项、答案、解析及 rich_content 与复核后的提交内容逐项比较，差异为 0。回读 SHA256 `6a7d280a2191fae49d35a1d669c8cab94411a334e740114e2f5f4d6739eadeb5`。

## 原文件实际暴露的解析问题及修复

1. 讲义第 4、6、50 项（索引 3、5、49）题干含“实验”或“综合”，旧规则误将其选择题选项并入题干。现在明确选择题括号优先识别为选择题；含真实子问的实验题仍保留子问内选项。
2. 第 24 项（索引 23）把来源批注 `2019全国I卷` 与答案合并，导致选项验证失败。现在识别卷号在“卷”前后的来源格式，仅分离完整来源行，不截断正文解释批注。实际答案恢复为 C。
3. 第 56 项（索引 55）的旧 MathType 公式为 preview_only。当前本地解析得到可编辑 `a-x`；完整转换后不再将该公式的预览图片重复放入题干，原预览资产仍保留。未完成转换时仍保留预览，不丢内容。
4. 质量报告现在对选择题缺少全部选项产生 missing_options 警告，不再把此类结构错误当作干净结果。

TDD 首次 `3af17d` 复现选择题/来源/质量报告问题；`3da697` 复现公式与预览图重复。修改后 `ee8d15` 的全部 46 项解析测试通过。真实讲义重跑 `4588fe` 退出 0：88 道、153 个公式全部 complete、无解析警告；结果 SHA256 `bd5338a829a35dfe6a9aad660e242e14ccc29b5675eb2f66cf83c9b63f98c842`。

只对上述五个候选做可追溯复核修正；云端原始候选及其旧警告没有伪改为 accepted。正式提交内容绑定原 task/item/contentHash，并先核对原候选哈希、原文件哈希和全部媒体已校验状态。没有把本地 data URL 写入云端题目内容。

## 验收脚本问题

正式题目列表存在多页。旧验收只读前 200 条，导致第一次提交后错误地认为本次部分题目不存在。真实预检 `806751` 返回第一页 200 条、匹配本次 44 条且 nextCursor 非空，证明应继续翻页。

新增完整分页读取，拒绝重复记录、循环游标及错误页结构；首次 `f432ad` 失败，修改后 `2486a3` 的分页及发布 helper 回归通过。最终 `579a39` 的提交、分页、发布 helper 及 Python 提交入口 6 项均通过。原仅发布两个样本的旧入口没有被直接执行，本次发布范围来自已校验的 108 个 ID。

## 权限、清理与尚未完成事项

- 旧在线会话不存在，预检 `20981d` 在任何题目写入前失败。本次在备份后使用既有在线登记接口取得测试会话，并通过其会话上下文检查；没有临时提升数据库 writer 权限。此处是已授权测试入口，不作为真实密码/微信手机号登录的通过证据。
- 本次新增测试安装实例 `acceptance-registration-7d09cc7d-0aa7-4f5e-ad93-93c6d61b93a3`，设备 `desktop-device-281b8d7a7804e906a1e25b147f428a39`。后续测试结束需按正常设备撤销流程收尾，不使用 owner 直改库绕过自设备撤销限制。未记录会话令牌。
- 第一次临时脚本清理因容器文件归属失败，随后针对本次四个精确临时文件清理成功（`68d8c3`）。后续执行的本次临时脚本均正常清理；本地原件、备份、用户改动及 output 未删除。
- 仍需多角色桌面/小程序题库与组卷实际交互、五个修正题目导出的 Word/PDF 渲染核验，以及来源/知识点等标签与原业务显示的完整核查。本次逐字段验证仅证明保存符合提交内容，不证明所有原始标签已经结构化映射完备。
- 解析器修复尚未纳入适用端部署矩阵；没有更新 NAS 容器、桌面 OSS 或小程序开发版。后续只部署确实变化的端，保留各端独立版本号和统一兼容性管理。

## 追加回归与真实小程序检查状态

### 完整原生公式检查发现的新问题（UTF-8）

- 对 SHA256 `6a7d280a2191fae49d35a1d669c8cab94411a334e740114e2f5f4d6739eadeb5` 的生产回读逐个调用实际 Word 原生公式转换器：108 题、473 处公式、347 种不同表达式，其中 3 处失败（`ffa343`）。位于试卷索引 6 的解析，以及索引 18 第五个子问答案中的两处公式。此前“回读零差异”证明存储一致，并不证明全部公式语法有效。
- 重新检查哈希为 `cc32c9804373a906f6799522da77f24882c85fdec447701b0f09002894a132ad` 的同一原始试卷，确认来源为原生 OMML：原件明确使用空白右定界符，旧转换错误输出空白右定界符命令；嵌套上标的基底未加分组，错误产生双上标。
- TDD `9e8601` 的三类新用例先失败；修复仅位于 OMML 导入转换器，保留空白定界符、嵌套脚本分组及定界符属性的所属层级，不在导出器猜测修复，也不转成图片。最终 `d2db0e` 的全部 49 项解析测试、Word/PDF renderer 测试通过。
- 用原始 OMML 重新转换得到 7 处表达式规范化变化（3 处原转换失败，另外 4 处同类带撇号变量恢复嵌套分组）。仅在本地检查副本应用这些来源明确的变化后，全部 473 处原生公式转换通过、失败数为 0（`d2db0e`）。原生产回读文件未改写，没有更改云题目、重新导入或新建导出任务。
- 尚未完成：云端连接恢复后，先备份并核对记录版本，再经正式题目更新接口修正对应两题及核对回读；旧导出任务若已保存题目快照，须核实快照规则，不能假定它自动使用修正数据。随后完成真实 Word/PDF 下载、完整页面渲染与原生公式视觉检查。本地转换通过不计为整卷导出或上线成功。

### 两题修正准备与快照契约（UTF-8，范围已由下节三题复核更新）

- `paperExportTaskRepository.js` 在创建任务时写入 `question_snapshot_json`，工作进程读取同一快照。旧 108 题任务不会因题库更新而自动使用修正公式；恢复后应先查它的实际状态，保留失败或取消记录，再对修正后内容执行新的验收任务，不能直接改旧任务快照或把重试结果当作新内容验证。
- 新增纯准备函数 `prepare-question-formula-correction.js`：复用现有完整题目更新字段及 `question.update.v1`，携带原 `expectedVersion`，由完整负载哈希生成独立幂等命令 ID；无网络、数据库或自动提交代码。原 ID、版本、发布状态、题干、选项、答案、解析或结构化内容变化时拒绝生成，不覆盖他人编辑。
- 修正限定到已复核路径上的 `formula.attrs.canonicalLatex`。同步更新具有同一公式 ID 和旧表达式的旧 HTML `data-latex` 属性，不重写周围文字、标签、分类或题目布局。TDD `ff4996` 先复现旧显示字段不一致，修复后 `8290eb` 通过；还覆盖重复路径、越界字段、原型路径、旧值不符、非法新公式、缺少验证器和确定性幂等。
- 离线演练 `fe5b79` 通过：试卷索引 6 为 1 处、索引 18 为 6 处；只有索引 6 的旧解析 HTML 需同步一处公式属性。两题基线版本均为 2。计划 SHA256 `0007660ae06ffdea00cb58b1ba6fb1d1b4129a2bd299da0d06d56c1522e4bfe2`，明确 `submissionReady=false`，须重新获取完整云记录并验证备份后才能提交。没有更改原生产回读文件、云题目或任务。
- 复查仍为 SSH/TLS 握手超时（`943a74`）；已有阿里云登录页尚未登录且二维码过期。本轮未重复要求授权、未重启服务器或 NAS。

- `da8e34`：完整题库解析/导入来源/删除保护/存储迁移回归退出 0；`54752d`：38 项公式导出检查退出 0。
- 微信开发者工具检查 `905d2c`：已登录、skill 0.3.2 equal、无需 CLI token。
- 全角色页面首轮 `59894/9c036e` 在首个页面被登录页异步回首页覆盖，退出 1。测试现在在页面操作前检查真实首页及当前账号，而非仅等待 1 秒；不改变产品导航。`cba317`/`7a43e7` 先失败，修复后 `e95111` 的启动等待 2 项及原角色脚本 30 项通过。
- 第二轮 `43007/d5abc7` 发现刷新初期 getCurrentPages 尚不存在，已增加可用性判断；第三轮 `45053/58f545` 已通过启动与首个资产页路由检查，但在 automator 截图超时、未生成图片，退出 1。不能将这些失败轮次当作五角色通过证据，也不能宣称完整小程序视觉验收已完成。
- 第四轮去掉截图，`23011/e84451` 仍在启动状态读取时收到 automator response timeout，退出 1。说明不能仅归因于截图；本轮没有取得五角色全部通过的结果，后续须恢复工具连接再验收。独立诊断截图会话仍未返回，不重复启动相同检查。
- 后续诊断截图 `68830/d9d494` 返回 terminated，无图像证据。启动瞬态错误的有限恢复修复后，教师限定运行 `55140/5f9dfb` 退出 0：课程、课表、题库路由与身份通过，当前 40 张题卡均有答案开关和试题篮入口。仅页面/元素存在性检查，不代替视觉及按钮操作验收；全部角色仍待完成。见 `go-live-closeout-2026-09-13.md`。

### 实际 Word 复看：撇号空基底修正（UTF-8）

- 之前 473 处转换成功不代表视觉合格。使用产品 `renderPaperExport` 生成诊断文件并在实际 Word 中以 200% 打开后，发现撇号旁仍有虚线空方框。原因是原件 OMML 上标中的撇号被转换为 `u^{'}`，TeX 将其当作空基底上的简写上标；应保留为显式符号 `u^{\prime}`。不在导出器猜测原题或转图片兜底。
- 新断言先失败（`0e4cba`）；仅修正 OMML 上标符号转换，覆盖单撇号、双撇号及嵌套脚本。`dd6599` 的全部 50 项解析测试、Word/PDF renderer 及原生公式结构测试退出 0；`7e2ff1` 的有版本保护修正准备测试通过。增加 Word XML 中无空公式基底、有实际 prime 字符的断言。
- 原件对照范围由此前两题七处更新为三题八处：试卷索引 6（1 处）、8（新增 1 处 `OO^{\prime}`）、18（6 处）。仅本地应用来源明确的修正后，`8d8705` 对试卷和讲义合计 108 题、473 处、347 种公式检查为零转换失败、零撇号空基底语法；不表示 473 处均已逐个视觉验收。
- 新诊断文件 `source-formula-prime-fixed.docx` 为 9019 字节、8 个原生公式、0 个绘图和媒体文件，SHA256 `d59961674ddf3a9530a6156e86766571ebe4963a0f208ccd06fe04506edfcfb6`。实际 Word 200% 截图已逐行查看八处公式，无原有虚线空方框；随后实点第四行，显示原生公式边框与“公式”上下文选项卡。没有修改或保存该公式。旧失败文件保留，未覆盖。
- 文档验证流程要求实际渲染；捆绑渲染器因缺少 LibreOffice 未能运行，改用已安装 Word 打开核验，未宣称渲染器成功或完整生产整卷通过。原始诊断文件的空方框是实际 Word 发现的缺陷，不掩盖为成功。
- 三题离线准备计划 SHA256 `99c0f796b07d2c2fef8f377b92d32bd4d722224c70523169b4f0976d6326973e`，仍为 `submissionReady=false`，三题基线版本均为 2。索引 6 的解析 HTML、索引 8 的题干 HTML 只同步对应公式属性，索引 18 保留其原有富文本子问结构。先前两题计划作为历史证据保留，不再用于提交。
- 本轮没有修改云题目、创建生产导出任务、更新 NAS、部署云端或发布桌面/小程序。下一步仍须新备份、完整云记录版本核验、正常更新命令及回读，再验证修正后内容的真实 Word/PDF 产物。

### 三题修正的正式 REST 执行入口（UTF-8）

- 新增 `scripts/apply-question-formula-corrections.js`，只调用既有桌面题目分页读取和 `question.update.v1` 命令接口；不引入数据库写权限、不用旧 SQL 修复入口。默认 dry-run，只读且不生成执行记录；显式 `--execute` 才执行现有 `backup_cloud_postgres.py` 创建并恢复验证新备份。
- 命令行要求复核计划文件及其 SHA256。仅接受固定生产业务 URL，认证令牌和设备编号从进程环境读取，拒绝 HTTP 重定向，不将令牌写入日志或执行记录。会话仍须从原有测试账号正常登记流程获得，不替代真实用户登录验收。
- 正式提交前完整分页读取当前题目，逐项核对全批基线并准备命令；随后以独占新建、同步落盘方式保存同一批完整命令、原记录和备份凭据，不覆盖旧文件。每题提交前再次核对版本，返回必须是匹配命令编号和负载哈希的 committed 回执，之后逐字段读回确认；非公式内容和来源标签保持不变。
- `--resume` 复用既有执行记录及原命令编号，并重新完成备份检查。已等于预期更新结果的题目只核对、不重复写；未知版本、其他人编辑、伪造执行记录或读回不符即停止。网络结果不明时不自动重发，保留执行记录供同一逻辑操作恢复。
- TDD `258e54` 首先因尚无执行模块失败；实现后 `74b505`、输入保护补充后 `03fb88` 退出 0。覆盖默认只读、分页、全批预检、备份失败、落盘失败、响应丢失后恢复、拒绝回执、异常读回、篡改命令、目标 URL 错误、重复题目、计划字段不能替代云端当前值、保留当前来源元数据，以及真实命令行缺参/未知参数失败。已接入现有导入验收测试入口。
- 本节是执行器的隔离回归证据，尚未向生产提交三题修正。实际执行须待云端健康恢复后进行；不以替身 HTTP 测试宣称数据库备份、真实修正、整卷导出或发布成功。
