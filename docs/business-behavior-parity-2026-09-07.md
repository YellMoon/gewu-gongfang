# 业务行为保真核查（未完成，禁止据此发布）

## 2026-09-12 教学用教师档案的云端范围闭环（UTF-8）

- 根据原账户/权限约定，教师可以建立无账号、可排课的教学档案。本批只补云端创建者范围，未改任何桌面表单、导航、窗口、排课或计费交互；不创建登录账号或角色授权，不推断历史档案归属。
- 新迁移新增同租户创建者关联及已被账号认领标记；已有授权仅回填认领事实。认领与写入同记录互斥，撤销或删除授权不把档案重新交给原创建者。业务 reader 不必读取账号授权目录，也未增加直接改表权限。Supabase PostgreSQL 检查用于最小权限、并发锁及真实受限读写验证。
- 实际 desktopIdentityClient → REST → writer 创建/修改及版本冲突通过；原创建者可读取、建课、排课、结课再启用。课表列表与主投影统一范围；曾发现独立课表接口漏课（2baf5a 退出 1），修复后通过。读权限使用真实 reader 验证（0d141b 首次因账号表权限不足失败，改为业务认领标记后 ff0ccd 通过）。其他教师、跨租户、未绑定、学生及认领后的原创建者被拒绝。
- 新教师删除实际触发旧函数字段名歧义，已保留课程引用保护并限定字段修复。真实教师档案专项 93daaf 退出 0：HTTP/数据库、实际查询、账号认领与撤销、旧版本、先编辑后绑定/先绑定后编辑、事务回滚；原本人修改、课程范围、课次范围、学生范围、课程删除历史及学生账目查询专项均通过。
- 原课次操作回归 06a9ed 退出 0：保留课程 192 次操作/48 次确认恢复，保留学生 384 次操作/96 次确认恢复，均经原处理函数、草稿、REST 和受限 PostgreSQL，出勤/价格/快照及锁保护保持。专项已接入 cloud-business-api 的 posttest。
- 完整云服务测试 85138 前置和主体通过，posttest 已跑过课次及课程专项，但在后续 managed-teacher 入口退出 1，无新增断言诊断；不宣称完整套件成功。独立专项已通过，仍需真实云库桌面实操和最终统一回归。用户暂停的地址历史、NAS 脚本及版本文件不纳入本批；版本文件 SHA256 保持 BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173。
- 尚未部署、打包、递增版本或更新 NAS/OSS。上线顺序必须先备份并应用本迁移，再切换云服务；桌面 REST 结构未改，小程序仍不能直接修改核心业务。完整业务保真、Word 可编辑公式、小程序全页多角色及真实发布门禁仍未关闭。

## 2026-09-12 六个原业务窗口的输入保留（UTF-8）

- 同一迁移重建副作用还影响学生、教师、上课地址、学校、机构、缴费六页。实际 App 状态回归 e800c9 先退出 1，学生窗口状态在云投影刷新后由 1 变 0。现仅将这六页改为原地读取缓存并清理事件监听，原表单、按钮、宽度及提交函数均未改。实际六页读函数/effect 验证只读全部关联数据，卸载后不再响应；App 状态保留与原表单 AST/尺寸对照 5c43bb 退出 0。React/Playwright 检查用于状态保留，不做重设计。
- business-parity 专项 26030 退出 0；源码构建 59420 退出 0，main.3d12c6f3.js、249.903c5f2e.chunk.js，样式 main.5792781d.css 不变。新增只读 resource-editor-only 隔离范围，禁止混合其他写入场景；入口单测先因范围未实现失败，补实现后 fa162c 退出 0。一次误用短提交号在连云前被 EXACT_SOURCE_COMMIT_REQUIRED 拒绝，改用完整已核实 SHA，未削弱门禁。
- 真实源码桌面 **51256 退出 0**，云源码 `eea7fe89becd48fde00da8a9b71e18723b293a93`、阿里云备份副本 `gewu_ui_shadow_49ee4eeeaa61270c`。通过原菜单分别打开六个新建窗口，实际输入后各进行两次真实云投影读取，输入保持；取消后完整业务投影及 outbox 均与基线相同。宽度依次 700/600/520/520/600/600，均为原窗口尺寸；没有执行保存，不据此声称六类新增/编辑/删除完整写入流程已验收。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-_xmvqgbu`；六页各有截图和控件树，desktop-receipt.json SHA256 `0a17306ec0df826d3a7b3f3a1005c54283b27cf274f386678a3e8174718e1767`。学生窗口截图因检查底部取消按钮而已滚动，不将其当作完整窗口布局截图；缴费备注输入可见。ok/cleanupComplete=true，临时副本和服务已清理，原备份及证据保留；productionWrite/uiVerified/businessFlowComplete=false。
- Electron ABI 准备 88185 退出 0（119），收尾 63254 退出 0，root/backend Node ABI 137。无生产部署、安装包、版本递增或 NAS/OSS 更新；受保护版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。
- 用户要求争取次日投入使用：冻结新增功能、外观改动；已验证项只在受改动影响时重测。接下来按上线阻断项合并推进：原教务权限/确认提交与恢复、真实 Word/PDF、桌面和小程序真实多角色流程、备份迁移及兼容发布。不能以本轮六窗口通过替代这些门禁，独立审计仍须遵守当前不开子 agent 的限制。

## 2026-09-12 课程状态实操发现的筛选重置回归（UTF-8）

- 接续会话 27021 的句柄已不存在；核对本机进程及 `gewu-business-parity-ihyq0ke1` 终态证据，未盲目重复启动。副本 `gewu_ui_shadow_883d5b3f60456850` 已清理，原备份保留。该轮在线结课、课表待排消失已走过；重新启用后课程显示未结课，但筛选也变成未结课，测试在等待行消失时失败，不能记为完整通过。
- 原版 `8118419f` 更新课程后只重读列表；当前 App 在 `authority-projection-refreshed` 后改变页面 key，导致课程页重建并把原筛选和编辑状态清空。先前将该轮失败归为纯测试等待问题的判断不准确。实际 App React 重建测试 5413b7 退出 1，明确复现状态从 1 重置为 0。
- 修复仅使课程信息页原地读取已确认云端缓存，并在离开时移除监听；不重建页面，不重置筛选，不改按钮、窗口、排课或课程规则。课程表原地刷新及撤销历史保留不变；用户主动刷新、离开页面及账号分区切换仍保留原重置边界。React 检查用于保持组件状态，没有重设计。bd8270 退出 0，覆盖真实 App 重建行为和真实课程/课表读取 effect。
- 原按钮实操增加筛选不变断言、在线及离线结课/启用、继续保留草稿、确认后读回、待排列表及原课次文本、重开和数据库全字段核对。副本保护单测拒绝非隔离库、混合运行范围以及历史字段漂移。7aec47 退出 0（12 组状态对照、实际 outbox 合并与确认、3 项历史检查、10 项副本入口检查）；34370 退出 0（完整 business-parity 专项）。源码构建 51746 退出 0，main.55820f17.js、249.132ecfa7.chunk.js，样式 main.5792781d.css 未变。实际重跑结果另记，不能以这些测试替代实操完成。
- 无生产写入、无 NAS/OSS 更新、无安装包或版本递增；保护版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。完整业务保真、其他页面、多角色、Word 可编辑公式和统一发布门禁仍未关闭。
- 最终实际桌面会话 **78854 退出 0**，固定云源码 `c111acebe8b83140d0090e8bccf6da9114804442`、阿里云副本 `gewu_ui_shadow_fec6883393c616a6`。通过原按钮完成在线结课/启用及两次离线状态切换；两方向均保持操作前的原筛选，目标行从当前筛选消失，再切换筛选显示正确状态。重连、查看及继续保留草稿均未提交；分别点击确认后才写云端。每一步完整云投影仅目标课程 active/updated_at 可变。重开后待排课程显示恢复，原课次三项内容一致。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-inuvknth`，`course-active-database-readback.json` SHA256 `696212a5cca5e1489baee4120b841e48c1a2efbb4b693e77634da064b0e31e25`。最终数据库比对覆盖课程 1、已归档学生 1、报名定价 1、课次 1、出勤覆盖 1、缴费 2、课消 1；全部原值恢复，仅课程版本前进。已查看确认窗口及重开课表截图；截图中的 Cloud account 文案仍按先前记录留给账户页面审计，本项不是整页 UI 合格结论。
- receipt 的 ok/cleanupComplete=true；sourceDesktop=true、installed=false、productionWrite=false、uiVerified/businessFlowComplete=false。只删除本轮临时副本/服务，原备份与本地证据保留。Electron 依赖准备 49611 退出 0（ABI 119），结束后 39951 退出 0，root/backend 已恢复 Node ABI 137；恢复后 App 状态与 course-active 专项再次通过。一次追加命令误填不存在的 browserAuthorityCache 测试路径而退出 1，属命令错误，不算产品失败或测试通过。

## 2026-09-09 恢复原课程结课／重新启用的单字段写入（UTF-8）

- 原版 `8118419f` 的 handleToggleActive 只调用 updateCourse(id,{active})。本轮先执行实际原版/当前处理函数，23bcf5 明确复现当前在线分支重提交整份课程，缺失年份变为 0、地址变为空，携带历史报名单价。追加的原服务 `800b4fdf` 实际 HTTP 对照使用合法毫秒版本，确认仅状态请求返回 400；不是把缺失字段补成猜测值来绕过校验。
- 原按钮、窗口、导航、结课筛选及课次显示均未改。在线状态切换现在只发送 active 和 expectedUpdatedAt；离线仍更新本地派生缓存，但只记录 active 意图并保留基线版本。实际 outbox 验证新增后结课、已有完整编辑后结课、反向次序及连续切换均正确合并，不丢此前表单改动；仅状态草稿展示原课程名及课程状态，无会话或未确认时不发请求，确认后经既有 PUT /api/business/courses/:courseId 提交。测试 safeStorage 为替身，不作为真实加密验收。
- 同一课程更新接口增加严格的二字段状态请求，原完整课程更新契约继续保留；额外字段、伪造权限、不合法布尔值/版本拒绝。新前向迁移 `20260909-course-active-original-behavior.sql` 复用原教师范围检查、教师及课程锁，只更新 legacy_active 和版本，不重验或重写旧报名名单、价格、课次。版本至少增加一毫秒，旧版本不能覆盖；PUBLIC 无执行权限，业务 writer 不获直接 UPDATE 权限。PostgreSQL 检查流程用于最小权限和并发证据锁定。
- `course-active-parity.postgres.test.js` 通过实际身份客户端、HTTP、真实 writer 服务和隔离 PostgreSQL，验证缺少年份/学期/地址编号、含已删除学生报名的课程在教师与超级管理员下结课/恢复；逐字段核对课程其余字段、学生、报名、课次和出勤不变。跨教师、跨租户、未绑定身份、学生、已删除课程、只读用户及旧版本均拒绝；事务期间并发改归属或删除教师被锁阻止，回滚后无漂移。重复应用迁移通过。1efac6 退出 0。页面原版对照 12 组、草稿确认与合并、原表单/地址联动、78 组成功写入后读失败分离及原 runtime 回归均通过，2432 退出 0。
- Renderer 构建 74326 退出 0：main.9fbebad6.js、249.1056ec00.chunk.js；样式仍为 main.5792781d.css。未打包、未安装、未更新 OSS/NAS，root/backend Node ABI 137，受保护版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。Electron 检查流程用于确认构建没有遗留错误的原生依赖环境。
- 完整云测试 97468、38524 均退出 1，分别在 parser-binding 和 supplemental lifecycle 附近停止且无断言诊断；对应 parser-binding 专项 1a8c77 单独退出 0，但不能替代全量通过，也不认定中断原因。前期新增测试夹具的毫秒时间格式、创建/更新先后及会话字段错误已修正；不将其算作产品故障。完整测试与实际桌面窗口结课/待排消失/重新启用回归仍待完成。
- 收尾专项 43791 退出 0：教师课程范围、实际地址服务及客户端、保留课次展示与原课程删除、状态 REST/真实 PostgreSQL、派生缓存、原课程写入、12 组请求/草稿同编号、78 组成功写入后读失败、90 组待确认课程及 180 组资源确认边界均通过。云端 app.js 只暂存状态更新段，历史地址可见性的原未提交行保持在工作区。（UTF-8）
- 发布兼容门禁：先应用本迁移并发布云服务，再投放支持状态单字段的桌面版本；旧桌面完整更新仍兼容新云服务，新桌面不能提前配旧云服务上线。小程序不因此新增课程写权限，NAS 无变更需求。当前只提交代码与验证记录，不关闭 BP-05/BP-07、其他原业务、多角色全页、Word 可编辑公式或统一发布门禁；暂停的地址权限改动与用户文件不纳入提交。

## 2026-09-09 原桌面刷新课程信息及确认撤销（UTF-8）

- 本轮只新增原版对照、隔离实操和读回验证；产品页面、窗口、导航、业务规则及云接口均未修改。直接提取 `8118419f` 与当前真实刷新处理函数及计费函数，48 组对照覆盖两种计费单位、教师计费方式、学生出勤组合、正反日期范围、教师名称有值/空值；首日零点、截止日深夜包含在内，范围外及缺失课程不改，取消/日期不完整无写入。故障注入确认可以识别截止日被排除的回归。
- 最终实际桌面会话 **94329 退出 0**，阿里云备份副本 `gewu_ui_shadow_d060685275f7b051`，固定云源码 `85632ab32b54db2ec91a72ab76b3967174f8cd65`，排除暂停的未提交权限代码。原日期输入选择 9 月 8 日，实际点击 Windows 原生取消及确定按钮，未使用 CDP 代答。取消不写；离线刷新只生成当天 09:00 和 23:00 两条待确认草稿；重连与查看确认窗不推送，逐条点击确认后才更新云端。
- 刷新后两课次按课程默认值为 220/160、正常出勤、按次计费；原 Ctrl+Z 生成两条恢复草稿，再逐条确认恢复原 180/120 单价、请假状态、0/0 总费用、按小时计费和原教师/地址快照，重开后的云投影完全一致。数据库逐字段比对确认范围外前一日/后一日课次、课程默认值、原报名、学生、缴费和课消均不变；只允许两条目标记录的 updated_at 更新。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-ssjv2jp4`，`refresh-database-readback.json` SHA256 `8eb2aa5406d91b535d81952afbec006b80d24effaf7cc0cc5f7d200e0c01c039`。已查看恢复确认窗口和重开课表截图；原课表显示优先取课程地址，与存储的课次地址快照分开核对，不以页面文字代替数据库检查。重开后账户区域仍有 Cloud account 文案，沿用此前待审计记录，不将本项当作整页 UI 验收。
- receipt 的 ok/cleanupComplete=true；sourceDesktop=true、installed=false、productionWrite=false，uiVerified/businessFlowComplete=false。仅清理本轮副本及临时服务，原备份和本地证据保留。未更新 NAS、未部署、未打包或递增版本。Computer Use 用于原生确认框实操，PostgreSQL 检查用于限定副本和核验账目，Electron 检查用于测试前后原生依赖恢复。
- 首轮会话 20823 退出 1，副本 `gewu_ui_shadow_1484cf61e4f31024` 已清理。失败是测试把教师名称误设为保留值：夹具课程的名称快照为 null，而原版刷新明确使用课程名称覆盖课次。确认原版与当前同样处理后，仅修正测试期望，并补名称为空的原版对照；没有改变产品逻辑或删减全字段比对。该次证据保留在 `gewu-business-parity-a7btnhqy`。
- 依赖恢复会话 17219 退出 0，root/backend Node ABI 137；恢复后原业务保真总入口 1480 退出 0，包含 144 组批量、108 组出勤、48 组刷新对照，以及备份 5、部署保护 46、隔离范围 10、刷新读回 3、恢复验证 2 项 Python 测试。新增对照已接入根 pretest；没有重新构建 renderer，沿用 main.f665d165.js。用户版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。（UTF-8）
- 本项不关闭其他原业务组合、小程序全页多角色、Word 可编辑公式、历史快照处理及多端发布门禁。后续继续原流程对照，不以此转入界面重设计或发布。

## 2026-09-09 原桌面删除学生后课次及出勤实操（UTF-8）

- 最终会话 **88517 退出 0**：阿里云备份隔离副本 `gewu_ui_shadow_194daab66260ab77`，实际 reader 与固定云端 `54e037f62abefba00242f76892469ebebd71d510`。原学生窗口取消/离线删除/重连不提交/查看不提交/确认删除/重开通过；原课表鼠标移动、拉伸、撤销重做、Ctrl 复制及撤销复制后的确认删除通过；原独立出勤窗口取消不写，选择请假后费用为 0，确认再原 Ctrl+Z 恢复并确认，重开后为周三 09:00–11:00、360/240，学生仍不在档案列表。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-uu7fl3ff`，`retained-student-database-readback.json` SHA256 `b30e4d4a0f1bee01f51c5cd908e8108af684e75007ccea9c35dfb36a81874ba7`；数据库逐字段比对确认原名单、单价、课程、缴费及课消保留，只有明确操作的课次时间/总费用、复制墓碑及学生删除/版本改变。已查看最终恢复出勤确认窗和重开课表截图；原出勤中间状态另有完整云投影对照文件。
- receipt 的 ok/cleanupComplete=true；sourceDesktop=true、installed=false、productionWrite=false，uiVerified/businessFlowComplete 保持 false。仅清理本轮隔离副本及临时服务，保留原备份和本地证据；本项通过不关闭其他原业务、全部页面、Word 可编辑公式或生产发布门禁。Electron 检查流程用于实际源码桌面启动与结束后恢复依赖：21634 退出 0，root/backend Node ABI 已恢复为 137。
- 恢复后 19398 退出 0：原业务保真、144 组批量/108 组出勤、备份与隔离保护、108 组保留学生课次/12 组出勤处理函数、原三字段卡片/待排六状态及 273 组渲染对照和三个故障注入通过；再次验证 root/backend Node ABI 137。版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。没有新构建、完整云端测试、NAS 更新或发布声明。（UTF-8）
- 本轮只扩展原桌面自动化及读回验证，没有改动产品界面、业务规则或云接口。固定云端 `54e037f62abefba00242f76892469ebebd71d510`，排除暂停的未提交权限代码；源桌面沿用已验证 renderer main.f665d165.js、249.4cf45e28.chunk.js 和 main.5792781d.css，无安装和版本递增。
- 校验器新增学生删除/课程保留模式，99d382 首先因未实现参数退出 1，属于测试能力缺口而非产品故障。补齐后 973899 退出 0，九项安全/读回测试拒绝混合目标、学生复活、关联记录及账目漂移；同时原版/当前 108 组课次和 12 组出勤开窗/保存处理函数通过，覆盖两种计费单位、教师计费模式及正常/请假/取消，不改原计算规则。
- Electron 准备 16839 退出 0，root/backend ABI 119。实操 10656 在阿里云隔离副本 `gewu_ui_shadow_b019f97749eab2ef` 已完成原学生删除/重开、课次移动拉伸、复制及撤销复制后的确认删除，最后在出勤下拉选择超时而退出 1；证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-3btq0e0_`，复制删除回执 completed，副本清理完成，未写生产。
- 已查看该次 failure 截图：原 700px 独立出勤窗口完整，保留 180/120 单价；脚本点击内部 combobox 输入层被正常选中项遮挡，不是产品控件故障。仅改为点击可见选择区域，不用强制点击或修改样式。截图中的“未知学生”也是 `8118419f` 原 JSX 对已删除学生的回退文字，本轮保持原行为，不将其冒称新设计或整页视觉验收通过。
- 会话 31275 在副本 `gewu_ui_shadow_55479882711d2463` 完成出勤请假确认、回执 completed 后，测试把云端 attendance_status 错写为本地 status，且误以为请假值为 3，导致最终断言退出 1；源码枚举为 CANCELLED=3、LEAVE=4，实际草稿 status=4，云读回 attendance_status=4。已查看 0515u9qa 目录的确认截图，学费/教师费均为 0；仅修正测试期望字段及枚举值，不修改业务映射或放宽逐字段比较。副本已清理，未写生产。
- 会话 20377 在恢复副本迁移阶段退出 1，错误为无详细 stderr 的 CLOUD_MIGRATION_PSQL_FAILED，随后 dropdb 也失败；未打开桌面，不推断具体网络或数据库根因。只读检查确认唯一遗留副本 `gewu_ui_shadow_20e67789a14b9bc4` 的最后迁移与当前 02:34–02:35 创建时段相符，残留 psql 连接 118699 于 18:35:37 UTC 建立、idle 且 query 为空。先重新核验原备份 SHA256，再定向清除该副本及其残留连接；a2c56d 退出 0，数据库计数为 0，原备份保留。临时目录 f3wix4kq 保留，本次没有存储容器创建或生产写入。
- 测试 runner 现于迁移前写入明确副本名/范围和 cleanupComplete=false 的起始回执；清理函数仅接受精确 `gewu_ui_shadow_` 加 16 位十六进制名称，允许清除此自建副本的残留连接。新增校验拒绝生产库、postgres、空前缀及注入字符串，dc6639 十项单测通过；不增加自动重试生产迁移、全库清理或扩大删除权限。

## 2026-09-09 已删除学生的保留课次写入（UTF-8）

- 直接执行 `8118419f` 的原 deleteStudent，确认仅删除学生列表项，不移除课程报名、课次或逐学生价格；108 组原版/当前处理函数对照覆盖学生已删除、课程存在/已删除、两种计费单位和教师计费模式及三种出勤状态。移动、复制、拉伸、删除和取消/冲突边界一致；课程已删除时原出勤入口仍不开窗。课程存在时的出勤编辑完整流程尚未在此项核验，不以这 108 组冒称覆盖。
- 红灯 75212 退出 1，原操作输出进入真实 scoped PostgreSQL 时触发 `VNEXT_TEACHER_SCHEDULE_SCOPE_DENIED`；actor 的关联校验和底层新增/更新均强制学生未删除，阻断原保留课次操作。新增前向迁移 `20260909-retained-student-schedule-write.sql`，仅允许目标课程已有报名或现存课次名单中的学生墓碑作为历史关联；没有复活学生选择项、改动学生 CRUD 函数、放开其他课程或直接表写入。
- 新辅助函数不授予 PUBLIC 或 writer 执行权，只由现有受控课次函数内部调用；保留租户、教师归属、版本、教师事务锁，按学生编号锁定学生及关联行。数据库技能用于最小权限与锁序检查。未修改旧迁移、业务页面、表单、窗口、布局或计费公式，没有新增 REST 字段。
- 第一轮 73115 退出 0：384 组教师/超级管理员操作及 96 次明确确认后的删除恢复，经过原函数输出、真实草稿队列、REST HTTP 和隔离 PostgreSQL；确认前无请求/无写入，确认后原名单、价格、费用及其他记录一致，学生始终保持删除。覆盖课程存在/已删除、报名/单次课次两种关联和并发锁；认证及系统加密为明确测试替身，不是生产或原生桌面实操证据。
- 最终完整课次专项 95235 退出 0：应用新迁移后，原教师权限、财务快照、确认撤销、192 组保留课程操作/48 次恢复，以及加强后的 384 组保留学生操作/96 次恢复全部通过。新增验证逐字比较学生 CRUD 函数定义与权限不变；另一课程的已删除学生即便属于同一教师，也不能被夹带到当前课次；教师与超级管理员均受该历史关联校验约束。新内部辅助函数不可由 writer 直接执行，拒绝操作后数据库不变。eaa780 再次确认 root/backend Node ABI 137；没有把专项当作完整云端 npm test 或真实桌面验收。
- 收尾 67092 退出 0：原业务保真（144 组批量、108 组出勤、原实例保留）、备份/部署/隔离保护、108 组本项原行为以及独立版本和发布矩阵检查通过。兼容清单只新增桌面/云端的前向迁移要求，不要求 NAS 或小程序跟随升级；未递增组件版本、构建安装包或部署生产。学生删除后的实际桌面操作和完整多端验收仍待办。

## 2026-09-09 原桌面保留课次实操与确认名称（UTF-8）

- 最终实操会话 52314 退出 0：固定云端 `5ec3339609b3283ff5709f8ea09850790289f9dd`，阿里云完整备份隔离副本 `gewu_ui_shadow_229b06fbbfccdf1b`；原课程删除后移动、拉伸、撤销/重做、Ctrl 拖动复制、确认复制及撤销后确认删除全部通过。离线只存草稿，重连及仅打开确认均不提交；重开后沿原导航进入课程表，周三 09:00–11:00 原课次仍在，周四复制课次已删除。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-p_mjcz9f`；数据库读回文件 `retained-course-database-readback.json` SHA256 `bc3467bc3f60f1da1ed7f40eb12de70ba4230599909af68f4c8c1c4d8381eb32`。逐字段验证原课次时间和费用、复制/删除记录、原学生关系和费率，缴费及课消未变；已查看 retained-06-undo-copy-confirm 与 retained-07-reloaded 截图，目标课次和确认名称可读。没有将其他测试名称、账号文案或整页视觉签为完成。
- 回执 ok/cleanupComplete=true，副本已清理；sourceDesktop=true、installed=false、productionWrite=false，uiVerified/businessFlowComplete 保持 false。收尾检查 6c7111 退出 0，root/backend Node ABI 均为 137；38677 退出 0，确认展示/实际确认事件、题库提交边界、八项隔离校验、原课表三字段/撤销重做/待排同数据渲染及 273 组课表对照和三个故障注入均通过。受保护版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。未打包、更新 NAS 或部署生产；完整业务和多端验收仍未完成。
- 实操会话 79578 退出 1：固定云端 `5ec3339609b3283ff5709f8ea09850790289f9dd`，阿里云完整备份隔离副本 `gewu_ui_shadow_4c87ca7c394e89d9`，实际 reader 启动。原桌面完成课程删除、移动课次到周三、拉伸至 09:00–11:00、原撤销/重做、确认调课及 Ctrl 拖动复制到周四并确认。移动/复制都已有成功回执；撤销复制产生待确认删除后，点击原“查看并确认”未打开窗口，因此整项未通过。失败后副本清理完成，未写生产。
- 失败证据目录 `gewu-business-parity-pro7ltr0`（系统临时目录）；已查看 retained-03-resized、retained-04-resize-confirm 与 failure 截图：目标课次仍显示课程名、地址和时间，原确认窗显示 360 元学费/240 元教师课时费；删除副本草稿保留，不能将无窗口当成已删除。用户原业务布局未改，其他长测试名称、Cloud account 文案和整页视觉未签为完成。
- 定位为确认展示函数忽略课次自身的 `course_name`，只查已不存在于选择列表的父课程，触发 `AUTHORITY_DRAFT_TARGET_UNAVAILABLE`。d48bec 退出 1 复现；修复仅补回课次已有名称，仍拒绝没有真实名称的删除目标。新增预留记录/云端读回两条路径测试，并执行实际确认事件：打开窗口不提交，点击确认才提交；不改删除权限、草稿版本、课表或窗口布局。
- 另发现旧 cloudQuestion 源码检查仍要求五秒前的 items 参数，afac0c 退出 1；已更新为检查读取最新持久化 current 清单，保留原实际事件回归，不回退产品的防陈旧确认逻辑。确认/展示/题库提交边界检查通过后才开始 renderer 构建，第一次调用没有走到构建阶段。隔离最终数据库校验新增八项单测，拒绝费用、教师名、原档案、缴费/课消、出勤关系或复制记录不符；只补齐新建测试样本的教师名称和服务类型，不修改既有业务数据。
- Electron 准备 23803 退出 0（ABI 119），失败后恢复 76603 退出 0（root/backend ABI 137）。renderer 构建 37524 退出 0：main.f665d165.js、249.4cf45e28.chunk.js，CSS 仍为 main.5792781d.css；仅重新构建渲染端，没有改写受保护版本文件。第二次 Electron 准备 47825 退出 0；没有安装、NAS 更新、生产部署或整体验收声明。
- 会话 68566 在上传固定提交源码时发生 SFTP EOF，尚未打开桌面；副本 `gewu_ui_shadow_1763624d1db3fa63` 已清理，目录 `gewu-business-parity-6mlbs_nj` 留存失败回执。确认其已退出并清理后才重试，没有因观察超时重启活进程。
- 会话 6106 在 `gewu_ui_shadow_8334d9ee6c62c14d` 已完成原调课、复制及撤销复制后的确认删除；已查看 `gewu-business-parity-3s5gvto8` 的确认截图，课程名、地址、时间和费用完整，删除回执 completed。最终重开时原应用回到“今日工作台”，脚本误等课表卡片而退出 1。此为测试导航遗漏，不改产品启动行为；已补原导航进入课表，未把该失败当作整项通过。副本已清理。

## 2026-09-09 已删除课程的原课次操作（UTF-8）

- 对照并执行 `8118419f` 与当前课表的真实函数，60 组案例覆盖移动、复制、调整时长、删除和出勤入口，两种计费单位、两种教师计费模式及三种出勤状态；取消删除与时间冲突仍不写。课程不在列表时原出勤编辑窗不会打开，本轮保持不变，没有重设计窗口、导航、卡片或表单。
- 首次数据库测试 3a1860 因测试迁移时间未含毫秒而失败，只是测试搭建错误。修正后 83e939 退出 1，原本允许的课次更新被 `VNEXT_TEACHER_SCHEDULE_SCOPE_DENIED` 拒绝。原因是最新 scoped actor 与底层新增/更新函数都把父课程删除当成操作禁令；并非原函数禁止调课。
- 新增前向迁移 `20260909-retained-course-schedule-write.sql`：课程墓碑仍须属于同租户及当前教师，教师有效性、教师事务锁、课次版本与行锁保留；只在课次写入中使用该已核验课程的原报名或单次课次学生关系，不改变学生 CRUD 权限函数。新增/更新仍校验课程存在，未复活课程选择项、未放开直接表写入、未改旧迁移或快照入口。数据库技能用于保留最小权限及锁定证据。
- 专项会话 20065 退出 0：192 组教师/超级管理员操作，经原函数输出、真实草稿队列、用户确认接口、既有 REST 客户端/HTTP 路由和 PostgreSQL writer 完成；另有 48 次已确认删除后的明确确认恢复。覆盖原报名与仅单次课次覆盖两种关系。确认前零请求、云库不变；确认后时间、原计费快照、逐学生价格和出勤一致，其他课次、学生及父课程不变。旧版本、其他教师、学生/访客/家庭角色、跨租户、无关联学生及缺失课程被拒绝；真实并发撤销教师归属、教师/学生有效性和出勤关系均因锁等待被阻止，回滚后全量读回一致。
- 认证与系统加密在上述集成测试中是明确的测试替身；数据库为自动销毁的隔离 PostgreSQL，未连接生产。本轮没有新的原生桌面拖动/右键实操或微信开发工具多角色实操，不能将 192 组接口链路测试称为这两类验收。学生自身已删除后的课次操作仍须另行原版核查，本节不覆盖这一状态。
- 教师课次完整专项 82810 退出 0，包含应用新迁移后的原权限并发、财务快照及确认撤销回归；该次新增专项尚为 96 组，后来扩充的 192 组及 48 次恢复由 20065 单独证明。根业务保真及课表写入会话 97177 退出 0：144 组批量、108 组出勤、273 组渲染及三个故障注入、原课表实例保留和备份/隔离安全检查通过。独立版本/发布矩阵和 root/backend Node ABI 137 检查 8c5572 退出 0；受保护版本文件校验和未变。
- 完整云端会话 12224 退出 1：pretest、test 及 posttest 中完整课次专项（含 192 组和 48 次恢复）均已输出通过，进入课程专项后退出。npm 的本次主日志及课程子日志记录实际退出码 `-1073740791`，没有测试断言详情；限定时间段的系统事件查询未返回匹配记录，不能据此推断具体原因。单独后半段会话 21908 最终退出 0，覆盖课次、课程、学生、学校、机构账单学生及余额的全部 posttest；不将分段输出冒称整套 npm test 成功。工作区仍含暂停权限文件；本轮未递增组件版本、打包、更新 NAS 或生产部署。

## 2026-09-09 原课程删除与保留课表读取（UTF-8）

- 真实原桌面专项会话 **46426 退出 0**：固定云端提交 `aa9794be85d07d6fc7379d0d8f945bc1ee95e429`，未加载暂停的地址权限文件；阿里云完整备份副本 `gewu_ui_shadow_771ae56adf3971fc`，实际 reader 启动。通过原取消、离线删除、重连不提交、仅查看确认不提交、确认后删除及重开；原课次文字“历史课程核验 / 原上课地址 / 09:00-10:30”保持一致，课程从列表和待排中消失，课次仍在。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-endqes1z`；`course-history-readback.json` SHA256 `a65eee3f3b4f9a7f8cf99f760624b22043d8d3781755444954405f23185c5c6d`。已查看 course-history-01-before、02-confirm、03-reloaded 三张截图。数据库逐字段对比仅课程软删除标记/版本变化；学生、排课、定价、出勤、缴费和课消全部保留，云端费用投影不变；原课次文字前后相同。色彩随原课程颜色分配发生变化，不据此宣称整页视觉不变或完成全部业务。
- 最终 ok/cleanupComplete=true，隔离副本、临时服务及远端辅助目录已清理，原备份和本地证据保留；sourceDesktop=true、installed=false、productionWrite=false，uiVerified/businessFlowComplete 仍为 false。没有安装、NAS 更新或生产部署。小程序目前只有原加载/缓存回归和类型检查，尚无该场景的开发者工具多角色实操。
- 直接执行 `8118419f` 的原 deleteCourse，确认只移除课程，不删除学生、课次、出勤、定价或费用。新增真实 PostgreSQL 对照：b389f0 退出 1，新增引用限制拒绝删除；加入 `20260909-course-delete-original-behavior.sql` 后，eee2bb 退出 1，教师课表读取把保留课次过滤为空。原 scoped actor 校验、教师/课程锁、租户和 expectedUpdatedAt 保留，旧迁移不改写。
- 两个现行读取入口（完整投影与 schedules REST）只移除“父课程必须未删除”条件，仍要求课次未删除、同租户及教师/学生关联范围。课程选择列表仍过滤已删除课程，不将其重新放回待排。schedules REST 的独立读回先在 57551a 退出 1 复现空列表；更早 f4c62f 是测试库缺少现行快照迁移的搭建错误，不计为产品故障。
- 同一次云读取查询仅取已获准课次所属课程的名称、类型、年份、学期，用原桌面显示名/纯课程名规则补齐课次显示字段；不提供新课次、教师单价或联系人，内部上下文在响应前移除。18 组原函数对照覆盖空显示名、系统前缀、换行和 Unicode 空白；不改持久化历史快照。原课表 JSX 对照增加删除课程的 21 组合，总计 273 组合和三个故障注入通过。
- 小程序既有 loadData 在课程缺失时把有效名称改成“未知课程”；a4e7b2 退出 1 复现，修复只保留已有 course_name/course_type，不改页面布局、按钮或新增状态。React 技能用于沿用派生值处理；原页面加载、缓存不变及访客边界回归通过，类型检查 49910 退出 0。不冒称微信开发工具/手机多角色实操已完成。
- 6407de 退出 0：教师/超级管理员删除现存或已删除课次所引用的课程，数据库除课程软删除标记/版本外逐字段一致；授权教师/学生/管理员课表与 schedules 查询保留现存课次，学生查询仍不返回教师费，无关联角色/异租户/陈旧版本/verifier 均拒绝或为空，直接表 UPDATE 拒绝。52704 退出 0：根业务保真、课程原表单、地址联动、草稿确认和小程序缓存回归；版本兼容检查通过。
- 完整云端 41720 退出 0 发生在新增 schedules REST 对照前；第二轮 45857 退出 1，最后已见到 teacher-schedule-scope 通过但无后续错误详情，不能称为当前完整云测试通过。原桌面删除/重开实操见本节开头；后续关联课次修改及全业务验收仍须单独证明。数据库技能只用于保留最小权限和历史数据；没有生产写入、NAS 更新、安装包或发布。
- 单独 posttest 会话 45794 已退出 0，完整覆盖金融快照、撤销恢复、教师课程/学生权限、本轮新增课程删除/直接列表读取、余额和学校回归。未复现上次终止；没有据此猜测上次退出原因，也不抹去失败记录。真实 UI 测试加载固定提交以排除暂停权限文件。
- Electron 准备 14702 退出 0（root/backend ABI 119），实操后恢复 83539 退出 0。收尾回归会话 38765 退出 0：真实 PostgreSQL 课程删除及授权课表读取、18 组原名称规则、小程序原加载及访客边界、原课表草稿/撤销/重做、273 组渲染对照和三个故障注入、七项副本安全检查均通过；root/backend Node ABI 均为 137。没有将结果缺失的前一次收尾调用算作通过。

## 2026-09-09 原桌面现存引用删除实操（UTF-8）

- 会话 11388 退出 0，实际源码桌面加载已提交云端 `058fce74154d05f1f95c74576cb413aaa7eeeb1e`，未加载暂停的权限改动。阿里云完整备份隔离副本 `gewu_ui_shadow_82ffd0f9ff4e26df` 使用实际 `gewu_cloud_schedule_reader` 启动；仅在副本新建学生及未删除的课程/排课、定价、出勤、两笔缴费和一笔课消。
- 在原学生窗口完成取消、离线删除、重连、打开确认、确认提交、重开列表。取消/重连/仅打开确认均不写云端；确认后草稿 completed、列表与云端投影不再返回学生。云库逐字段比对六类关联记录全保留，学生只改变软删除标记和版本；`referencesArchived=false` 明确证明此次不是仅测已删除课程/排课。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-c6kinf1u`；`student-history-readback.json` SHA256 `65016f0934f3cf6b510c2e255c134e4445cbdeb1d0ca2aed9f4d3ef49adfd8d5`。已查看 student-history-01-confirm 与 student-history-02-reloaded 两张截图，确认对象名称、两个确认选项和重开列表可读；未将旧测试数据长名称或整页其他状态签为视觉完成。
- 回执 ok/cleanupComplete=true，测试副本、容器及远端临时目录已清理；备份和本机证据保留，该 profile 残留 Electron 数为 0。sourceDesktop=true、installed=false、productionWrite=false，uiVerified/businessFlowComplete 仍为 false。本轮没有改动业务 UI、生产库、NAS 或发布版本。
- Electron 准备会话 6333 退出 0，收尾会话 92432 退出 0，root/backend 恢复 Node ABI 137；恢复后会话 72300 退出 0，原学生页面写入检查、210 组原年级计算、16 个真实 PostgreSQL 删除案例及六项隔离防误写测试再次通过。用户版本文件校验和仍未变。未重跑完整根测试或生成新构建，不把上轮完整云测试算作恢复依赖后的新运行。
- 后续原版检查已定位下一项：原 browserDatabase.deleteCourse 也只删除课程项，当前 vnext_soft_delete_course 却新增排课引用禁删。此项尚未修复；必须连同原课表读取、历史费用与教师范围对照，不能仅取消报错便宣称完整恢复。

## 2026-09-08 恢复原学生删除行为（UTF-8）

- 重新读取 `8118419f` 的原 StudentList.handleDelete 和 browserDatabase.deleteStudent：只删除学生项，保留课程、排课、定价、出勤与费用记录，没有课程/排课引用禁删规则。下文仅恢复“已删除父记录不阻止删除”的阶段结论不是完整保真目标；本轮继续移除现存引用带来的新增业务限制，不改原窗口、按钮、字段和布局。
- 测试直接提取并运行原版 deleteStudent，再对比真实 PostgreSQL scoped writer 的结果。会话 2d4229 退出 1，原版允许的课程引用被 VNEXT_BUSINESS_STUDENT_REFERENCED 拒绝。新增前向迁移 `20260908-student-delete-original-behavior.sql` 后，原账号范围与锁、expectedUpdatedAt、软删除保留；没有删除任何关联表、放开直接写表或改写旧迁移。
- 会话 54645 退出 0：教师与超级管理员共 16 个案例覆盖已删除课程/排课、仅结课、现存课程/排课、混合引用、关联其他教师的课程和无创建者的既有在读学生；课程、排课、定价和出勤整表读回一致。无关联教师、学生/访客/家庭角色、跨租户、陈旧版本和 verifier 均拒绝/无结果，拒绝不改版本；原创建者、名称、创建时间保留。共享记录仍遵循既有教师范围，不由本轮增加另一套角色规则。
- 根 `test:business-parity` 会话 95558 退出 0，包括 144 批量排课与 108 出勤组合、原课表实例保留及隔离备份/部署安全检查；Python 六项隔离测试也通过。完整云端 npm test 会话 30627 退出 0，包含 pretest/test/posttest 和本轮 16 个删除案例；工作区仍含暂停权限文件，完整工作区测试不冒称干净部署验证。原桌面现存引用删除实操须记录独立最终结果，不以数据库案例替代。
- 已核对此前真实原桌面会话 44252 的落盘证据：云端源码为 `2300f34f2edb743c0d1a3a1d3682eaa419c1854f`，隔离副本 `gewu_ui_shadow_4bd486a027a8956a`，目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-fell681y`。它只覆盖已删除父记录的学生删除；原取消不写、离线草稿、重连不提交、打开确认不提交、确认成功和重开消失均通过。六类历史数据逐字段不变，cleanupComplete=true；本轮已查看确认和重开两张截图，名称及按钮可读。该旧轮证据不能冒称新修复的现存引用实操。
- 当前 root/backend Node ABI 137，用户版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。Electron 技能用于原生依赖收尾，PostgreSQL 技能用于保留最小权限；无生产写入、业务 UI 修改、NAS 更新、打包或发布。BP-06 其他操作及全部原业务仍未关闭。

## 2026-09-08 学生删除的历史引用回归（UTF-8）

- 原始学生删除与当前引用限制的整体差异仍未关闭。本轮先确认一个独立回归：`20260827-lifecycle-reference-active.sql` 已仅检查未删除的课程/排课；`20260907-z-teacher-student-write-scope.sql` 接入教师范围后却直接检查关系行，导致课程/排课已删除仍拒绝删除学生。没有把“结课”当成“删除”，也没有决定放开有效引用。
- 在既有真实 PostgreSQL writer 用例追加教师/超级管理员两套五种情况：已删除课程、已删除排课、仅结课、现存排课、混合历史与现存引用。会话 d57f8d 退出 1，首个已删除课程案例仍抛 `VNEXT_BUSINESS_STUDENT_REFERENCED`；新增前向迁移 `20260908-student-delete-active-references.sql` 后，会话 40491 退出 0，连同现有教师学生会话权限和原入学年级 210 组合通过。旧迁移文件不改写。
- 修复只恢复父记录未删除条件；保留原 actor 校验、学生排他锁、教师锁、expectedUpdatedAt 和软删除更新。其他教师、跨租户、陈旧版本及验证账号均被拒绝/返回空结果，拒绝不改学生版本。所有课程、排课、学生定价和出勤关系在学生删除前后整表比对一致，不删除或重写历史；学生原名称、创建者和创建时间保留。迁移重复执行通过，未扩大表写入权限。
- 兼容声明 `studentDeletionReferences` 只涉及云服务与现有桌面调用，不新增 REST 字段，不要求小程序或 NAS 更新。本轮未改业务窗口、原确认入口或页面代码，未操作生产数据。有效引用是否应允许删除、其他资源维护权限和原业务全部闭环仍须继续，不以这十个数据库案例冒充完整原版恢复或多端发布。
- 完整云端 npm test 会话 79049 退出 0，包含新增案例与原教师课程、排课、学生及余额回归；会话 67014 退出 0，包含验证账号拒绝、独立版本/兼容矩阵与八项迁移执行器检查。工作区仍有暂停权限文件，不冒称干净部署验收。本轮没有重跑真实桌面删除、完整根测试或构建；Node ABI 为 137，受保护版本文件校验和未变。数据库技能仅用于保留既有最小权限和锁定顺序。

## 2026-09-08 原布局复核与学生余额读取（UTF-8）

- 后续真实源码桌面专项会话 **51103 退出 0**：加载已提交云端 `c6a1303226e5b379f2515f3089482c8a9ed15285`，排除暂停/未提交的云端权限文件；阿里云完整备份副本 `gewu_ui_shadow_a1cd5687d6ca740e` 校验原权限指纹后迁移，以实际 `gewu_cloud_schedule_reader` 启动服务。测试在副本新增一名有 12 课时/1200 元缴费、1.5 课时/180 元课消的学生及一名无关联学生；通过原登录、原学生列表、700px 独立编辑弹窗、取消、刷新重进及 1200×800 窗口验证，均显示 10.5 课时和 1020 元。无关联学生不返回；云端记录与版本、桌面草稿清单前后不变。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-tcahum4l`，已查看 balance-02 原弹窗、balance-03 重进列表、balance-04 小窗口截图；读回文件 `balance-03-reloaded-list-readback.json` SHA256 `3701595256ff62ce21319c21b3c8edd8a0e9c2e1aea8d41a3ab8550f170590b5`。小窗口保留原表格横向滚动，右侧删除按钮需要横向滚动才完整可见；弹窗截图未覆盖下半部全部联系方式和底部按钮，不能据此宣称整页视觉完成。账户仍有 Cloud account 文案，留在账户检查项，不借本轮改业务界面。
- 最终回执 scope=student-balance-only、sourceDesktop=true、installed=false、productionWrite=false；result 六项通过，但 uiVerified/businessFlowComplete 均保持 false。测试数据库、容器和远端临时目录已清理，原备份保留。本轮只新增测试脚本和证据，不改产品界面。Electron 准备 36296 退出 0；收尾 14034 退出 0，root/backend 恢复 Node ABI 137，未发现该测试 profile 残留 Electron。没有安装包、NAS 更新或正式发布。
- 恢复依赖后，会话 20925 退出 0：隔离测试库防误写的五项单测、学生余额 HTTP/缓存/原公式、真实范围 SQL 和 `test:business-parity` 全部通过。没有重跑完整根测试或完整云端测试，不把上轮结果重复算成本轮；本轮没有产品代码更改。
- 本轮先复核用户指出的原业务差异，不重新设计界面。会话 54818 退出 0：实际 AppShell 在 Chrome 1200/1536 宽度下完成悬停、锁定、释放；144/143 个动画帧均保持主区位置和宽度不变，注入旧挤压样式能被检测。六资源页原表单和 Modal 宽度、待排 Sidebar 六状态、课表 252 组同数据渲染及三个故障注入、原三个字段卡片均通过。浏览器仅挂载真实组件，不等于重新验收完整安装版。
- 随后定位原学生余额的迁移缺口：`scopedDesktopProjection` 将教师的 payments/consumptions 固定置空，而原 StudentList 根据这两组明细计算余额。新增真实 HTTP → 原桌面客户端 → 派生缓存 → 原 StudentList 函数测试，在会话 58a87c 退出 1，明确复现应为 10.5 课时/1020 元却为零；最初测试调用错客户端导出产生的 TypeError 只是测试搭建错误，不计作产品故障证据。
- 修复仅在云端教师桌面读取：同一 SQL 语句内复用现有学生可见范围，读取这些学生同租户且未删除的缴费/课消明细。共享学生余额仍按完整学生明细计算，不擅自按某教师排课切掉部分扣费。原页面函数与 `8118419f` 语法树一致；未改业务字段、窗口、布局、缴费计算或数据库写入权限。缺失明细数组明确读取失败，不再伪装成零余额。
- 会话 70eaea 的 HTTP/客户端/缓存/原公式链路退出 0；会话 264fe9 的隔离 PostgreSQL 范围/明细、桌面活动角色及小程序投影回归退出 0。数据库覆盖创建者、课程关联、单次排课学生覆盖、共享学生、无关联教师、跨租户、删除学生/明细/课程；受限读取角色不能更新缴费。测试使用真实学生范围 CTE 和新增明细 SQL，但省略资产/地址等无关投影字段、认证使用替身，不能据此声称完整云部署或真人登录通过。
- 复用既有 tenant/student 索引，不新增迁移。兼容声明仅涉及桌面/云服务的既有字段，云端修正即可；小程序投影不查询这两张明细表，NAS 无更新要求。测试搭建期间缺少读取角色、遗漏排课必填金额均在临时数据库中修正，未修改生产配置。
- 完整云端 npm test 会话 6948 退出 0，含 pretest/test/posttest 和新明细专项；原业务回归、版本兼容/发布边界回归、桌面缓存及原 REST 客户端会话 92052 退出 0。仍是包含暂停权限文件的工作区回归，不等于干净生产版本验收。本轮没有改 renderer，无需重复构建桌面；Node ABI 保持 137，受保护版本文件 SHA256 仍为 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`。
- 待核查而未擅改：旧学生删除只移除学生，本地仍如此，但现云端拒绝删除被课程/排课引用的学生；这涉及共享学生及历史记录，仍列为业务差异，不通过删除引用保护或抹除历史来制造通过。本轮没有新桌面完整实操、Word 导出或生产发布，原业务整体与多端验收门禁仍未关闭。

## 2026-09-08 已确认删除的云端恢复（UTF-8）

- 承接 `gewu-business-parity-rng0ldti` 的真实同编号冲突，本轮不改课表窗口、布局、菜单、撤销/重做或计费规则。原恢复在本地仍是新增覆盖缓存，runtime 仅从同账号、同云端范围的最近成功删除回执取得 expectedUpdatedAt，并标记为待确认恢复；普通新增不借用历史删除回执。恢复后再编辑保留该基线，确认前重做删除只取消恢复草稿。
- 沿用 `/api/business/schedules/:scheduleId` PUT，新增严格可选 `restoreDeleted: true`，要求完整课程生命周期和学生价格字段；普通 PUT/POST 不恢复软删除。云端独立受限函数在同一事务中锁定准确删除版本、核对原课程及目标课程权限，复用已有出勤、费用与快照校验，再恢复记录；创建时间不重写。非法关联、越权、校验失败均回滚，读账号无函数执行权、writer 无直接表更新权。
- 新增实际 outbox → REST 客户端 → HTTP 路由 → PostgreSQL writer 测试。首轮退出 1（恢复草稿缺少明确标记）；runtime 补齐后第二轮退出 1（旧路由拒绝恢复输入）；完整接入后退出 0。加测同账号/不同账号、不同云端、确认前重做、已恢复后不能借旧删除回执、两请求争用同一删除版本（仅一个 200、另一个 409）、跨租户、学生/小程序票据拒绝及失败全量读回不变，均通过。认证上下文和系统加密在该集成测试中是测试替身，不冒称真实登录/UI 验收。
- 兼容声明增加 `desktopScheduleRestoration`，只涉及桌面与云服务；发布时必须先应用 `20260908-schedule-confirmed-restore.sql` 及云端支持，再发布桌面消费者。旧云拒绝该请求，绝不退回旧中继或改成覆盖式 POST。未触动 NAS、小程序功能或任何组件的安装版本；独立版本/发布矩阵回归通过。
- 完整根 npm test 会话 2683 退出 0（含新增集成测试）；仍是包含受保护暂停文件的工作区回归，不是干净发布矩阵。渲染构建会话 75561 退出 0，main.f7d2cc8d.js、249.17b700cb.chunk.js，CSS 仍为 main.5792781d.css。Electron 准备会话 33375 退出 0，root/backend 为 ABI 119。
- 原源码桌面专项会话 33699 **退出 0**：云源码 `6df09d5565a09804ffb28bf47f37bbd8db9791cf`、隔离库 `gewu_ui_shadow_279b609e0a0df0ca`，未带入暂停的历史地址权限改动。通过实际 Windows 原确认按钮删除，再在原确认入口提交；Ctrl+Z 后仅出现本地恢复草稿，确认前云端仍为删除状态；再次确认恢复成功，最终云读回及重开课表检查通过。原创建时间、课程、时段、地址、出勤、学费和教师课时费一致，其他课次和课程不变。已查看原三字段卡片截图，未修改窗口或布局。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-hpampnyx`；`confirmed-delete-undo-readback.json` SHA256 为 `d4a1108dadf8b9a9212cf35d06b1b3fda0ecec096629e27f20a9515c9d797a8d`。最终恢复草稿 completed、restoreDeleted=true、submitError=null；回执 `confirmedDeletionUndoRestored=true`，同时保持 `scope=confirmed-delete-undo-only`、installed=false、productionWrite=false、uiVerified=false、businessFlowComplete=false。测试副本已清理，原备份及本地截图读回保留。
- 收尾恢复会话 31976 退出 0，root/backend 已回到 Node ABI 137；随后会话 3036 再次通过实际 runtime、outbox/REST/PostgreSQL 恢复及 App 挂载测试。当前测试 profile 无残留 Electron 进程，受保护版本文件 SHA256 仍为 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`；暂停的云权限改动及用户 NAS/output 文件未纳入提交。（UTF-8）
- 本节只关闭“确认删除后的同编号云端恢复”这个缺口，不关闭 BP-07 全部动作组合、原业务整体审计、真实 Word 可编辑公式或多端发布门禁。PostgreSQL 技能用于短事务、锁定版本及失败整体回滚；Computer Use 技能用于实际原生确认按钮观察/操作。没有组件版本递增、安装包、NAS 更新或正式部署。

## 2026-09-08 确认后的课表原历史保留（UTF-8）

- `6fc91d1c2` 加入的云投影事件无条件递增 App 的 refreshKey，导致业务页面重建。对照 `8118419f`，原版只有主动刷新会递增该 key。本轮仅对课表区分确认后数据刷新与原主动刷新，不改窗口、按钮、数据写入规则或其他业务页刷新方式。
- 新增实际 App React 挂载测试，页面探针只用于观察状态与卸载，不写业务数据。先失败：投影刷新后原历史探针由 1 变 0；修正 App 不重建课表后，实际课表 loadData 效果测试仍失败（读取次数 1 而非 2）。随后让原 loadData 监听投影更新并在卸载时移除监听，保留原 30 秒刷新与原数据映射。
- 定向测试通过：连续三次投影更新不丢课表实例和历史；原主动刷新、离开再进入仍重置；实际课表读取效果立即更新日历及批量操作缓存，卸载后不再响应。另核验原 DesktopIdentityGate 的 partitionKey 边界及 React 替换分区，换账号/角色不会保留上一个分区的历史。React 技能用于稳定页面实例及清理事件监听，没有新增依赖。
- 完整根测试会话 94260 退出 0；这是包含受保护暂停文件的当前工作区回归，不是干净发布矩阵。渲染端构建会话 36593 退出 0，main.d39c0cf4.js、249.17b700cb.chunk.js，CSS 仍为 main.5792781d.css。未执行会改写用户版本文件的根 build 前置步骤，版本文件校验和未变。
- 真实专项先前会话 32230（`gewu-business-parity-ptxlaziq`）在 Windows 删除确认框未被及时处理时超时；取消该框后进程退出 1，隔离副本清理完成。没有把超时当作撤销验收，也没有替换原确认窗。
- 本轮会话 37267 使用云源码 `bde444816215acc502b2643571743830b57cda70`、当前渲染构建、隔离库 `gewu_ui_shadow_86e7ca715ebf9cdd`。实际 Windows 点击确定后，原课次删除进入待确认；显式提交后云读回确认删除。随后 Ctrl+Z 恢复原卡片及一条待确认草稿，恢复确认前云端仍无该课次，证明没有静默推送。已查看 `39-confirmed-delete-undone-locally.png`，仍只有课程名、地址、时间。
- **专项整体退出 1，云端恢复尚未完成**：恢复草稿仍为 `schedule.create.v1`，同编号新增返回 `CLOUD_BUSINESS_SCHEDULE_CONFLICT`，最终状态 conflict、云读回 restored=null。证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-rng0ldti`；读回文件 SHA256 `0528f104a93c6c08853ae7a33cf35f85c0765734d6d010bfb415b420492cdd83`。本轮只证实历史保留修复，不能称已完成“撤销已确认删除”。
- 下一步边界已由真实失败确定：恢复必须使用已成功删除回执的版本、同编号、同账户及云端范围，通过现有 REST 确认提交；云端恢复需保留创建时间、原出勤和费用快照，并拒绝旧版本、越权及失效关联。不得把 POST 同编号冲突改成无条件覆盖、删除原撤销功能或恢复旧中继来规避失败。
- 本轮副本清理完成；Node 恢复会话 2567 退出 0，root/backend 均为 ABI 137。App 挂载及实际 loadData 测试再次退出 0；差异空白检查通过，受保护版本文件校验和仍为 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`。没有新权限设计、版本递增、安装包、NAS 更新或生产发布。

## 2026-09-08 原生确认框实操纠偏（UTF-8）

- 本轮未修改产品布局、原确认框或业务规则。窗口等待辅助测试通过并接入 `test:business-parity`；该入口会话 73464 退出 0。真实完整会话 35819 仍在末尾失败：`PARITY_WINDOW_UNMAXIMIZE_TIMEOUT`，证据位于 `gewu-business-parity-e7p0krny`，不是产品窗口修复成功。
- 单独启动原源码桌面、不登录、不连业务端点，复现窗口状态：未处理确认框时可以还原和设为 1280×800；用 Playwright `dialog.accept/dismiss` 后原生窗口仍禁用，取消最大化无事件，`setEnabled(true)` 也无效。失败的强行启用方案已删除，没有加入产品或最终测试。对应诊断目录 `gewu-business-parity-pRsKA2`、`gewu-business-parity-6Zsbc2`。
- 改为 Windows 原生操作后，会话 26262 退出 0（`gewu-business-parity-D3TW2y`）：从实际截图点击无业务数据诊断框的“取消”与“确定”，分别返回 false/true，窗口均恢复 enabled=true，1280/1200 宽度验证通过。第一次无障碍索引点击未生效，重新观察后用截图坐标点击；没有把无效点击记为通过。Computer Use 技能限定了真实窗口选择、观察后操作和即时复核；没有替换产品确认框。
- Playwright 官方 Electron 文档说明原生 dialog API 不由其网页弹窗接口拦截：https://playwright.dev/docs/api/class-electron 。因此之前删除实操中 `dialog.accept/dismiss` 只能证明脚本响应后的业务变化，不能证明真实系统按钮操作完成。现有删除验收已改为等待实际 Windows 按钮操作，并检查原生窗口恢复可用；不再用 CDP 回答该弹窗。
- 完整会话 79880（`gewu-business-parity-wsc8zfdj`）中，实际 Windows 点击取消/确认后原窗口 enabled=true；单节/批量删除及未提交撤销重做检查走完，两个宽度的学生、老师、学校、上课地址、机构、缴费共 12 项原弹窗均检查完并保存 `resource-modal-checks.json`，取消不产生草稿。学生长表单上下截图均保存，底部取消/确定可见；本轮查看了 1200 学生底部截图。该会话在最后新增撤销专项开始时被原 600 秒上限终止，整体退出 1，不能称全流程通过。测试副本已清理；残留本地测试窗口经原生 Alt+F4 关闭，随后检查该 profile 的 Electron 进程已不存在。（UTF-8）
- 新增已确认删除后的原 Ctrl+Z 验收及 `--confirmed-delete-undo-only` 专项入口，完整实操上限调整为 900 秒，所有入口仍保持明确 scope/fullSignoff=false。专项会话 63230（`gewu-business-parity-cg7eqxjo`，隔离数据库 `gewu_ui_shadow_91a0157ecfce8fce`）实际确认删除后，云端课次已消失，但 Ctrl+Z 无效、卡片没有恢复；失败快照中撤销/重做均 disabled，outbox 只有已 completed 的新增与删除，没有恢复草稿。该专项明确退出 1，副本清理完成。尚未执行到恢复 REST，不能将潜在同编号新增冲突冒称本次已复现。
- 定位到 `src/App.tsx` 的 `authority-projection-refreshed` 事件递增 refreshKey；整个业务页被带有该 key 的 div 重新挂载。确认窗口刷新云投影会触发该事件，因此课表组件内 past/future 历史丢失。这是下一处应修复的原业务差异：区分原手动刷新与确认后的数据刷新，保持课表实例及原历史，再继续验证同编号恢复的云端契约。不要靠改按钮、删除原撤销功能或放宽云端冲突检查掩盖问题。
- 本轮没有产品修复、版本递增、安装包或发布。Electron 准备会话 2005 退出 0（119）；最终恢复会话 25213 退出 0，root/backend 均回到 Node ABI 137。用户版本文件 SHA256 仍为 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`，暂停的权限改动及用户 NAS/output 工作未动。
- 最终 Node 环境下 `test:business-parity` 会话 65575 退出 0，新增/改动实操脚本语法检查及定向差异检查通过，UTF-8 中文已读回。该回归入口不包含真实桌面专项，不能冲销上述真实业务失败；没有重新运行完整根测试或构建。

## 2026-09-08 原删除撤销的相反草稿修复与实操（UTF-8）

- BP-07 专项先复现明确错误：原课表删除后撤销由 replaceSchedules 捕获为同 ID 新增，runtime 没有 delete→create 合并分支，实际留下删除/新增两条草稿。新增真实 runtime＋实际草稿适配器测试退出 1（2 !== 0）。没有改原右键菜单、原确认窗、历史按钮、计费或云端权限。
- 修复只在加密本地草稿保存排课删除前的白名单字段，用于匹配撤销。未提交删除完整恢复时取消该删除；删除前已有修改时恢复原修改及最初 expectedVersion。恢复内容不同或旧草稿缺少快照时转为同 ID、原版本的待确认更新，不自动写云端、不覆盖服务器。已确认/冲突草稿和不同用户/云端范围不参与本合并。删除草稿 payload 依旧只有 id/expectedVersion，REST 继续沿用原编号路径和 expectedUpdatedAt 契约，本地撤销资料不进入云端命令。（UTF-8）
- 新增 `business-parity-schedule-deletion.cjs`，通过原右键“删除课程”、原生确认对话框、Ctrl+Z/Ctrl+Y，以及原矩形选择/“全部删除”/批量确认窗完成测试。涵盖取消不产生草稿、离线删除待确认、撤销去除相反草稿、重做保留原云版本、重连无自动提交、显式确认只删指定课次、课程默认设置和其他课次不变、刷新重开不复现已删除课次。
- 首次实操 48292 退出 1（`gewu-business-parity-32grngov`）：尚未进入学生添加，画面停留首页，没有执行删除。原因未证实，不能记成删除回归失败或登录完整验收通过。测试随后增加等待页面实际业务缓存出现本测试教师，不再只根据独立 REST 已成功判断页面就绪。最初误用短 SHA 被脚本在连接云端前拒绝，改用完整 SHA 后才执行；两项均不是产品修复。
- 修复后真实会话 95943 使用云源码 `7abdec2fc1e0c9cb54e184a1e9b2dce1dbeecfc3`、当前桌面源码及新渲染构建，隔离库 `gewu_ui_shadow_698a0f8ffe1757f5`。证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-ngbwytp8`。原学生/课程/调课、手势、多人出勤先完成，随后单节及两节批量删除的上述全链路断言通过；已查看撤销后的三字段课程卡、原批量删除确认窗和重开后的剩余课表。
- **该真实会话整体仍退出 1**：删除 helper 返回后，末尾资源弹窗测试在将 Electron 缩为 1280 时等待 innerWidth 超时（business-parity-desktop.cjs:574），未完成本轮 12 组小窗口复查。不能把删除专项通过冒称整个 UI 流程成功，也未因缩放超时修改产品布局。该缩放测试问题及首次导航停在首页的问题仍待核验。隔离库/容器和本轮远端临时源码已清理；原备份、本地截图和读回文件保留。
- 单节删除读回 SHA256 `c8f3d044745db29ae369301ff0d23da5213ee7792fc1be82ab547a6e7bab9e5b`；批量删除读回 `8f9b4d99c3e58853ae39b88bfd3aa0626e64e9017cb49a40f9fdd3bc291ea3ff`。本次没有生产业务写入，不能称为安装版或小程序验收。
- runtime/适配器及原缓存捕获 64611 退出 0；扩展恢复不同内容/旧草稿兼容专项及类型检查 70338 退出 0。渲染构建 38388 退出 0（main.1807a1a9.js、249.d14dd5d8.chunk.js，CSS 仍 main.5792781d.css）。完整根 npm test 49665 退出 0，基于受保护当前工作区，含暂停的未提交云文件，不是干净发布矩阵。第二次 Electron 准备 32747 退出 0（ABI119），结束恢复 39561 退出 0（root/backend ABI137）。版本文件哈希保持原值，未打包或发布。
- BP-07 与总目标仍不关闭：本轮撤销只验证删除尚未提交的情况，云端已确认删除后的恢复还没有完成；首次导航和小窗口检查、历史快照差异、丢回执恢复、真实 Word 原生公式及多端逐页/权限/发布门禁仍待办。不据此转去扩展无关业务或更新 NAS。

## 2026-09-08 已提交云端源码的原课程表真实实操（UTF-8）

- 本轮未修改产品页面、窗口、业务规则或权限。为排除暂停的历史地址改动，隔离测试脚本新增 `--cloud-source-commit`：仅从精确提交导出云源码、shared 契约及 SQL 到临时目录，不创建 Git 工作树、不改变当前目录。测试先因缺失导出函数退出 1，最终四项通过，覆盖已修改/未跟踪文件排除、禁止覆盖目录、非法引用及越界/链接归档拒绝。
- 实操会话 16740 退出 0：使用云源码 `21d3e96ca62a263a84220b184cbf77dfeb1ba811`，阿里云隔离库 `gewu_ui_shadow_ee0dd9cb9f3adb7b`，证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-6pyhd68g`。源备份校验和及权限指纹复核成功，迁移清单不含暂停的 teacher-room-history；当前已构建渲染端运行在源码 Electron，真实密码登录、独立测试安装实例和原控件操作。不是安装包或生产库验收。
- 原学生弹窗创建、学校登记、确认和重开，原课程地址关联、待确认课程不被在线结课夹带，以及原窗口排课/调课均通过。1.5 小时改 2 小时的学费 270→360、教师课时费 180→240；地址、学生、计费方式未变。时间重叠仍拒绝且不生成额外草稿。导航展开前后主区 x=0、width=1536，原排课窗口独立居中。
- 实际鼠标跨日拖动、底部拉伸、键盘撤销/重做、矩形选择与 Ctrl 拖动复制、两节课批量移动均经确认后读回并刷新核对。拉伸到 2.5 小时后学费 450、课时费 300；复制不改源排课，未选择排课不变。两名学生、按次计费的课程三日期批量排课通过；五组出勤组合的学费/课时费依次为 130/80、130/80、180/120、0/0、310/200，课程默认价格和其他课次不变。
- 实际检查六个资源弹窗在 1280/1200 两种宽度的打开/居中/取消（12 组），不新增草稿。已查看调课后重开、重载课程卡片、撤销重做卡片、1200 宽学生表单上下部、双人出勤重开截图：卡片只有课程名、地址、时间；长学生表单保持原滚动方式，底部取消/确定可达。出勤 form-4 截图捕获下拉收起过渡，不能用于完整静态视觉验收，已另看 reopened-4 的稳定状态。尚未逐张审查全部资源窗口截图，不冒称全部 UI 已完成。
- 桌面回执 SHA256 `075ad38a6473784b65b27907d88ba81b89c3f672c04a45acc9dcccd8bb5277f9`；调课读回 SHA256 `092c6c44c0ea92f125a323316caca059a27622779ece2bc81525512cbef91cfe`。回执仍 `businessFlowComplete=false/uiVerified=false/productionWrite=false`；隔离容器、库和本轮远端临时源码已清理，原备份与本地证据保留。
- Electron 准备 49598 退出 0（root/backend ABI119）；结束恢复 30234 退出 0（两者 ABI137）。最终脚本四项、原撤销/重做及 144 批量+108 出勤源码对照退出 0。未重跑完整根 npm test；产品源码未变，未再次构建、递增版本或发布。
- BP-07 仍不关闭：原单节/批量删除、相关撤销恢复及更多费用/机构组合尚待真实验证。本轮也不覆盖丢回执恢复、原数据快照缺项、小程序全部角色页面或 Word 真实可编辑公式；继续按原业务对照推进，不夹带新权限设计、NAS 更新或 OSS 发布。

## 2026-09-08 新增请求与断线草稿沿用同一编号（UTF-8）

- 原因定位到四页新增处理：云端请求已经分配记录编号，网络异常后本地 create 方法却重新生成编号。现在学生、教师、课程和上课地址只在本次函数调用内保留已尝试的编号，交给原本地草稿方法；尚未发起请求的离线入口仍按原方式生成编号。没有改表单、窗口、布局、提交协议、自动同步或角色规则。
- 本地方法拒绝空白及已存在编号，不覆盖缓存或追加错误草稿。地址的普通离线同名复用保持原逻辑；已有明确新增编号的请求不会被转成另一条同名地址的修改。真实页面函数、真实本地新增方法及投影适配器共 12 组测试覆盖四类型的离线、发送前失败和丢回执，后两种客户端故障由测试注入；实际 HTTP/数据库证据限于下述学生链路。
- 学生集成测试使用真实页面保存函数、REST 客户端、HTTP 路由和受限 PostgreSQL writer。分别在发送前抛错，以及服务端已返回成功后丢弃回执：确认前无自动写入；前者确认后只有一条记录，后者确认得到冲突且仍只有原编号的一条记录。测试认证上下文及部分表单辅助函数是测试桩，不代表真实登录或桌面 UI 验收。
- 集成测试发现已存在学生编号的数据库 23505 被误报为服务不可用。学生新增服务现将该错误转为路由已有的 409 冲突结果；权限和连接错误保持原传递，不新增接口、不覆盖已有记录、不假报保存成功。相关服务单测及真实 PostgreSQL 复跑会话 28080 退出 0；同时原 90 组课程、180 组资源确认和 78 组成功写入后读取失败检查通过。
- 完整根 npm test 会话 95592 本轮取得最终退出 0。它基于当前受保护工作区，仍包含此前暂停的历史地址文件，因此不能作为干净发布矩阵证据；这些暂停文件、用户 NAS 脚本、版本文件和 output 均不纳入本项提交。用户版本 SHA256 保持 bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173。
- 明确未完成：丢失成功回执后的草稿还需要与云端状态核对并恢复，当前保留 conflict，不冒称完整恢复；修改/删除的不确定结果及全部原业务实操继续待验。本项只防止重复新增并纠正冲突误报，BP-02～BP-07、可编辑 Word 公式和多端发布门禁均未关闭。没有生产写入、部署、版本递增或 NAS/OSS 更新。
- 原表单/独立弹窗和 210 组学生年级对照会话 62065 退出 0。此前构建的终端回执未保留，检查确认已无 build 进程后重新取证：渲染构建 25712 退出 0，main.69e679cc.js、249.a8e413a1.chunk.js；主 CSS 仍为 main.5792781d.css。未运行根 build 的版本生成步骤、Electron 打包或原生 ABI 切换。

## 2026-09-08 已成功写入与读取失败分离（UTF-8）

- BP-06 原保存流程的故障注入明确复现：学生新增方法已返回成功，仅 refreshAuthorityProjection 抛 TypeError，原函数仍调用 createStudent 再生成新增草稿。新增 `BusinessWriteReadback.test.js` 先退出 1，重复草稿实际 1、期望 0。
- 相同错误边界涉及学生、教师、上课地址和课程四页。本轮保留原保存入口、字段、校验、版本参数及待确认检查，只用函数内标志记录云端方法是否已经成功返回；成功返回后的列表错误不再进入离线草稿分支，提示已保存/已删除但列表尚未更新，并结束原表单提交。没有添加 React 状态、监听、自动重试、接口或角色规则。
- 课程还保留原全可见课程的排课地址联动。仅当写入已确认而新投影读取失败，联动对本次课程使用已提交成功的地址及同一个 courseId，其余课程继续来自原可见缓存；不覆盖缓存课程、不新建课程草稿，排课仍由原 replaceSchedulesInPrimaryStore 保存为待确认变更。正常刷新与原离线分支不变。
- 78 组真实页面处理函数测试通过：13 个新建/修改/删除/结课操作 × 读取网络失败、读取会话失败、投影错误、写入前离线、写入拒绝、正常成功；仅写入前离线保留原草稿行为。课程原全范围联动、刷新失败时旧缓存地址替换、其他课程及时间/出勤/费用/版本不变均通过；原 90 组课程确认与 180 组资源确认检查保持通过。
- 集成测试首次退出 1 是测试错误地把 authority outbox 的 sessionToken 直接传给身份客户端；此时根本没发出 HTTP，不能作为已落库证据。按客户端既有 currentSession 契约修正后，会话 94237 退出 0：实际学生页面保存函数→实际 REST 客户端/HTTP 路由→受限 PostgreSQL writer，随后注入读取失败。断言仅一次写请求、零重复草稿，数据库仅一条学生记录，年份/年级/学校保持正确。认证上下文、联系人和计算函数在该集成场景使用测试桩；这不是安装版 UI 或真实登录验收。
- 类型检查及原表单/弹窗和 uiRegression 会话 52956 退出 0，210 组学生原年级对照保持通过。用户版本文件 SHA256 仍与本轮开始前一致，暂停的历史地址权限文件未纳入修复。
- 渲染端构建 19378 退出 0，main.c6bccaf4.js、249.707dea11.chunk.js；CSS 保持 main.5792781d.css。直接执行渲染构建，未触发会覆盖用户版本文件的根 build 前置步骤，也未打包 Electron 或切换原生依赖。
- 最终完整根 npm test 会话 49170 退出 0，包含云端 pretest/test/posttest 与集成场景。本次结果基于当前受保护的工作区（其中仍有此前暂停的未提交文件），不是干净发布矩阵或真实多端 UI 验收。定向暂存差异检查通过，只提交本轮九个文件。
- 边界未冒领：本轮处理“客户端已经收到成功回执”后的读取失败，不覆盖写请求本身丢失回执的恢复；后者、完整业务实操及多端发布仍待核验。未写生产数据、未改布局、未更新 NAS 或 OSS。

## 2026-09-08 学生入学年份保存恢复原计算（UTF-8）

- BP-06 逐行为核验发现明确差异：`8118419f` 的学生新建及修改在保存入学年份时计算 grade_current；当前原表单只编辑 grade_year，不注册 grade_current，但在线修改映射把缺失字段作为 null 提交，在线新建还可能沿用过期 grade_current。本轮没有修改原年级算法、表单字段、窗口或角色权限。
- 新增 `StudentList.business-parity.test.js`，提取历史数据库实际 createStudent/updateStudent、历史和当前 calculateGrade、当前页面在线提交函数及当前本地草稿方法，用同数据比较，而非另造一套期望年级规则。先运行退出 1：在线编辑 2021 级实际 null、原版为“高复生”。
- 修复仅两处 gradeCurrent 映射：新增按原入学年份计算；修改有入学年份时按原逻辑重算。按 React 规范在保存事件中直接计算，没有增加副作用、状态或新的组件。学校、机构、联系人、版本和其他提交字段不变。
- 修复后 210 组通过：8 月 31 日/9 月 1 日、7 个入学年份、缺失/空/陈旧年级值、新建与编辑、在线/离线/已有待确认草稿；在线与原结果一致，草稿仍不绕过确认提交，表单值不被提交逻辑改写。已挂入 StudentList.cloud-write 原测试入口。
- 会话 91188 退出 0，包含上述回归、六页原表单/弹窗对照及 uiRegression；类型检查 46921 退出 0。隔离 PostgreSQL 会话 66308 退出 0，学生更新字段往返、学校登记原子性/并发/权限测试通过；后两项是既有数据库契约回归，并非新年级流程的桌面 UI 端到端验证。
- 直接渲染端构建会话 13497 退出 0（未运行会覆盖用户版本文件的根 build 前置脚本），产物 main.ddd937fd.js、249.a7313c4c.chunk.js；CSS 仍为 main.5792781d.css。用户版本文件 SHA256 仍为 bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173。未运行根完整测试，构建成功不代表发布。
- BP-06 仍未关闭：学生保存后的列表读取失败、删除引用、费用余额与完整操作链仍须核验。不把这项字段修复扩大为“学生管理已全部完成”。未做生产写入、安装版实操、版本递增或多端发布；此前暂停的地址权限文件与用户工作保持不动。

## 2026-09-08 收回扩展范围并核验导航原交互（UTF-8）

- 按用户要求暂停历史地址关联表/访问规则的新改动，保留其未提交文件，不继续部署或扩展；未覆盖用户已有 NAS 脚本、版本文件或 output 工作。当前任务优先保持原业务窗口、导航和课程显示。
- 定位到 `946a1ac1` 已删除导航锁定时主区左移 236px、宽度减少 236px 的样式；本轮未重复修改产品 CSS。原源码桌面证据 `gewu-business-parity-jrt01uyf/navigation-geometry.json` 展开前后均 x=0、width=1536，是此前真实桌面运行记录，不冒称本轮重新运行安装版。
- 新增 `AppShell.interaction.test.js`：将当前 AppShell、PageHeaderBar 和实际导航配置编译后，在真实 Chrome 内使用现有 React/Ant Design 运行。只替换联网状态查询与同步面板，禁止网络请求；不使用业务会话、不操作业务记录、不重新造导航 DOM。测试不代表整应用或云端验收。
- 在 1200/1536 两种宽度实际悬停展开、进入导航后移出收起、按钮锁定、移出后仍保持展开、释放收起；逐动画帧检查主区位置与宽度。另在隔离测试页注入已删除的挤压样式，证明检测能识别原错误，不修改产品样式。
- 最终 `npm run test:desktop-layout` 会话 47670 退出 0，四项入口全部通过；两种宽度分别检查 147/139 个动画帧。定向 diff 检查通过，中文内容已读回，用户版本文件 SHA256 与本轮开始前一致。本轮未运行根完整测试。
- 本轮课表检查通过：待排课程同数据六状态、卡片 252 组对照及三种故障注入；课程原表单/地址输入/90 组待确认处理和排课列表原业务入口检查通过。上述源码基线仍是历史对照，不等同于用户确认的全部原版。
- 不因这几项通过关闭所有业务差异；地址范围、其他完整业务操作、可编辑 Word 公式及多端验收仍未完成。没有打包、更新 NAS、部署云端或发布 OSS。

## 2026-09-08 原课程地址联动真实故障与修复（UTF-8）

- 新增 `business-parity-course-address.cjs`，复用原桌面学生/课程/地址创建、确认、排课流程，随后仅用原控件完成课程地址离线修改、保留草稿、课程确认、排课确认、选回原地址与重开；云读回只用于断言。新增 `--course-address-only` 明确限定回执，始终 `productionWrite/uiVerified/businessFlowComplete=false`。Playwright Interactive 所需 js_repl 当前不可用，复用项目脚本；Electron 技能用于测试前后切换和验证 119/137 原生依赖。
- 首轮 32293 退出 1，`gewu-business-parity-j3nlorkn` / `gewu_ui_shadow_e11b80d816732d48`：实际生成课程、地址及排课草稿，但脚本错误读取顶层 baseVersion；既有草稿契约实际是 payload.expectedVersion，和云端基线为同一时刻。仅修正测试字段及等价时区比较，不是产品修复。副本已清理。
- 第二轮 77117 退出 1，`gewu-business-parity-6_3fdglz` / `gewu_ui_shadow_5ae446fe271dcc0d`：确认课程及新增地址后，云排课地址和版本已变，而排课草稿仍待确认。确认窗口未列出这条排课变更。定位旧 `vnext_update_course_record_v1` 对 schedules 的隐式 UPDATE，直接数据库回归同样复现。副本已清理。
- `20260908-course-address-confirmation.sql` 前向替换课程更新函数，仅移除提前写排课的语句。测试将旧函数去掉该语句后与新函数去注释/空白比较，确保课程校验、字段、学生定价和版本检查均未改；原全可见课程地址联动仍生成草稿，经原排课接口确认提交。真实 writer 测试覆盖教师/超级管理员、同地址保存不刷新排课版本、陈旧课程版本、使用原排课基线确认及其他排课/费用快照不变；直接写表仍被拒绝。
- 第三轮 12773 退出 1，`gewu-business-parity-5gk0_wvp` / `gewu_ui_shadow_dd1176e4b5e7d404`：课程确认不夹带排课、排课确认及时间/出勤/费用读回已通过；选回原地址时发现旧地址因不再被课程引用而从教师投影消失，桌面因此创建同名新草稿。失败截图和三条新增草稿已保留，副本已清理，不能把该轮称为完整循环通过。
- `20260908-created-room-visibility.sql` 从服务器已有会话记录新地址的创建教师，增加同租户 FK 和查询索引；教师读投影保留其本人创建的地址，仍保留原课程关联可见规则。实际投影表达式的 PostgreSQL 回归先复现空列表，修复后覆盖无引用仍可读、课程迁移地址后仍可读、其他教师/学生不得额外读取、租户/删除过滤、重名与伪造归属拒绝、旧记录归属仍为空。没有猜测或回填历史地址创建者，也没有赋予额外角色或地址修改/删除权限。PostgreSQL 最小权限及索引规范用于此修复。
- 根 `npm test` 会话 39499 退出 0，发生在第二个地址可见性修复之前；当前两项修复后的云端完整 pretest/test/posttest 会话 69575 退出 0。专项数据库测试已通过。
- 最终桌面会话 92492 退出 0，`C:/Users/83423/AppData/Local/Temp/gewu-business-parity-jrt01uyf`，副本 `gewu_ui_shadow_687cc8fc2297bc88` 已清理，ok/cleanupComplete=true。实际完成原窗口新增地址/课程、创建排课、离线改地址、重连不写、保留不写、课程确认不夹带排课、排课确认后仅地址/版本改变、在线选回原地址不重复创建、排课确认恢复、重新打开课表。`course-address-readback.json` SHA256 `c8ed5e63840b07ca4088b20850da900c7de4cbe4db917c0f32a6cf988f83c841`。
- 已查看最终 address-03/04 的课程与排课确认窗口、address-05 新地址课表、address-07 恢复并重开课表：地址、课程名、时间及确认按钮均可读；长测试教师名称与其他原有 E2E 课程仍在，不据此签署整页视觉或所有业务验收。最终仍 uiVerified/businessFlowComplete=false。恢复 Node 会话 40459 退出 0，root/backend 均为 137；课程原表单/全范围地址联动/90 组合、资源 180 组合及课表 252 组渲染等专项再次通过。用户版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。
- 范围仍未关闭 BP-02/BP-03/BP-05：跨设备同名地址冲突恢复、历史无归属地址可见性、其他教师的全范围关联及正式数据迁移/发布仍待核验。必须先应用两项前向迁移再部署新云读投影。未改原业务窗口/表单/布局、未新增 REST 协议、未修改生产库、未更新 NAS/OSS；用户原有改动保持不动。

## 2026-09-08 课表原版同数据渲染对照（UTF-8）

- 本轮仅补回归测试和记录，没有修改产品窗口、布局、业务规则、云接口或角色权限。未继续未完成的教师新建档案权限改动，原未跟踪测试文件保持不动。
- 新增 `ScheduleCalendar.render-parity.test.js`，从 `8118419f` 和当前源码分别提取实际课程加载映射、课程名处理、课表卡片 JSX、地址表达式和拖动时间换算。相同业务时刻分别用旧本地时间及云端 UTC/带偏移时间表达，再经过当前真实云投影缓存适配器比较完整卡片 HTML（含内部样式与高亮）。这是历史源码对照，不是用户确认的最终原版或整窗截图验收。
- 252 组对照通过：3 类名称 × 4 类地址字段 × 3 种时间表达 × 7 种静止/移动/复制/上下拉伸状态；云投影与缓存输入均未被显示计算改写。另故意添加内部编号、把云时间退回字符串截取、恢复旧排课地址优先显示，三种错误均被对照识别。首次测试退出 1 是故障注入移除了 JSX 提取锚点，修正为保留锚点并额外插入编号后通过；不是产品修复前失败，不能称为修复了一个新产品缺陷。
- 当前实跑 `ScheduleCalendar.cloud-write.test.js` 退出 0，含新对照、原三字段卡片、待排 Sidebar 同数据六状态及撤销重做；`test:desktop-layout` 退出 0，含六页原 Form/Modal 宽度检查和实际浏览器导航遮挡检查；`test:business-parity` 退出 0，含 144 组批量与 108 组出勤等原行为回归。本轮未运行完整根测试，没有新桌面完整业务实操或多端验收。
- D 盘所指目录仍是应用数据目录，无 `src/pages/ScheduleCalendar.tsx`、不是 Git 工作区；未读取/修改其业务库，未把它当旧源码。对照提交的基线限制仍保留。
- 后续仍按 BP-02/BP-03 等原流程核验课程内新增地址、地址联动和确认后云端读回，不将这次显示对照当作全部业务完成。用户三处已跟踪改动和其他未跟踪文件保持不动；仅测试/记录更新，不触发产品版本递增、NAS 更新或 OSS 发布。

## 2026-09-08 上课地址更新函数真实数据库修复（UTF-8）

- 承接教师函数故障检查上课地址，未改任何窗口、字段或角色权限。新增真实受限 writer 测试先复现 PostgreSQL 42702：`vnext_update_room_v1` 在重名检查中的 id 与输出变量同名；UPDATE 的 id/updated_at 同样未限定来源。
- 前向迁移 `20260908-room-update-qualified.sql` 仅补明确表别名。原重名检查、更新名称与地址、租户/版本/未删除条件、writer 会话限制与执行权限均保留；旧迁移不重写，生产数据库未应用本次迁移。PostgreSQL 最小权限规范用于保持受限 writer 和禁止直接表 UPDATE。
- `room-lifecycle.postgres.test.js` 使用实际桌面 REST 客户端、真实 HTTP 路由、原业务服务和可销毁 PostgreSQL；会话上下文为隔离测试桩，不是实际登录或桌面 UI。已通过新增→修改→读回→删除，验证重名 409、旧版本 409、被现有课程引用不可删除、异租户不可改、拒绝后记录不变、writer 不可直接改表；迁移重复执行通过。
- 新测试已接入原 room service 测试入口；RoomManager 云端写入检查、原弹窗宽度检查、Node 根/backend ABI=137 检查通过。完整云端 `npm test` 会话 59364（含 pre/test/post）退出 0；补充同记录原名称可再次保存、重名/旧版本拒绝后立即读回未变的测试亦退出 0。没有重跑桌面窗口实操、根总测试或构建，不沿用上个提交证据冒称本轮多端验收；本轮发布仍暂停。
- 仍须处理的权限差异未隐去：教师创建未绑定教学档案、创建后数据可见/可排课的范围，以及教师对上课地址和机构的维护范围。原方案的教师默认能力包含 profile.unclaimed.create、resource.location.write 和 resource.institution.write；当前不能永久用 super_admin-only 代替，也不能只开放创建却令新档案不进入数据范围。本次先移除阻止合法地址修改的数据库故障，不关闭 BP-05。

## 2026-09-08 教师本人档案读取与修改权限恢复（UTF-8）

- 已实际读取指定会话“分析角色账户绑定与学校统一方案”（019fc06f-4cad-7af3-93b6-e232bdbe29b8）的用户要求，并核对其最终方案：已绑定教师可创建教学用教师/学生档案；正常注册自动建立自身教师档案，随后补充学科、课时费等资料。该会话旧数据主机架构已被当前云端唯一权威目标替代，不能恢复旧主机中继或设备审批。
- 确认两项当前差异：教师 PUT 被一律限制为 super_admin；教师读投影只按已有课程包含教师，尚无课程的自身档案缺失。新增真实 REST 客户端测试先复现 ACCESS_DENIED，真实 PostgreSQL 执行当前投影表达式先复现无课程本人结果为空。
- 修复仅在云端原 REST 路由和已有投影中：以有效桌面会话及当前活动角色确定绑定教师 ID，允许修改本人；读投影在原租户、未删除条件内包含绑定本人。不改原教师表单、窗口、字段、布局；不增加 REST 字段或数据库权限，不修改生产记录。
- 专项验证已通过：本人修改、旧绑定上下文、活动角色收窄、超级管理员；未绑定、其他教师、学生/家庭/访客、小程序/失效/缺失票据均拒绝；伪造租户、actorScope 和角色请求被拒绝；微秒冲突仍为 409。PostgreSQL 检查无课程本人、删除档案、异租户档案、学生范围和既有相关教师读取。
- 首轮真实桌面会话 21878 未通过：学生修改/删除已 completed，教师确认后脚本捕获到 submitted/receipt=null。证据目录 `gewu-business-parity-sa6y7e78`，隔离副本 `gewu_ui_shadow_d3be748af451c853` 已清理。最初怀疑等待时序，但不能据此判断云端已成功。
- 后续 7403（`gewu-business-parity-n1yqlx09`）和 58057（`gewu-business-parity-69l7hxe8`）同样失败，各自副本已清理。最后一轮按同一条 IPC 草稿实际回执持续观察 45 秒，未重发。新增真实受限 writer PostgreSQL 测试随后明确复现 42702：旧 `vnext_update_teacher_v1` 的未限定 id/updated_at 与返回变量歧义。这是实际写入故障，不能归为单纯截图/等待问题。
- 新增前向迁移 `20260908-teacher-update-qualified.sql`，仅限定 UPDATE 列来源；原参数、更新字段、租户/未删除/版本条件、writer 限制与显式执行权限保持不变。真实 writer 调用原服务完成新建→修改→字段读回；陈旧版本和错误租户不能写，直接 UPDATE 仍拒绝；迁移重复执行通过。测试与迁移发现检查均退出 0。
- 迁移后会话 16746 的教师更新已 completed/committed，随后因空备注 ''/null 的严格比较失败（`gewu-business-parity-zg30sqn0`）。经核对 TeacherList 原有 payload 和草稿适配器均将空备注归一化 null，仅修正测试的空值比较；非空备注与其余字段继续严格比较，没有改业务字段。
- 最终真实桌面会话 87891 退出 0：`gewu-business-parity-n_j4sgwp`，副本 `gewu_ui_shadow_167d3870c4f524d0` 已清理。学生修改/删除 completed；教师本人修改 completed，电话/科目/课时费和备注含义保留，删除仍拒绝且原草稿保留；地址修改/删除仍按现有权限拒绝。重连、再次编辑和删除前均验证云端没有自动接收草稿。读回记录 SHA256 `60C7A4C5D4D86428FDC079AF479EF52445F90AFD041EA7E4EF96BE7A2E98B97B`。
- 已实际查看本轮教师修改确认、修改结果、删除确认、拒绝结果四张截图：确认对象名称和两按钮完整可见，修改后的名称及完成提示可见，拒绝后保留草稿提示可见。顶部旧离线通知和同步徽标仍有过渡显示，不能宣称整页 UI 已完成。确认截图 SHA256 `F8F9D2535F331C6064B4AE880D32D267FB03CE340A0444AFB09E513BF499068B`。发布标记仍为 productionWrite/uiVerified/businessFlowComplete=false。
- Electron 测试前 70883 退出 0，原生 ABI=119；结束后 83830 恢复 Node，根及 backend ABI=137，退出 0。原表单宽度、导航覆盖、TeacherList 云端写入检查亦已退出 0。本轮未改任何 src/pages、src/layout、CSS 或用户版本文件，未打包和发布。
- 本轮云端完整测试会话 96374、48864 退出 1；对应 foundationLifecycleRoutes 和 foundation-lifecycle.postgres 单项复跑通过。第二轮 npm 日志记录 Windows 进程异常退出码 -1073740791，不是可认定的全套成功。恢复 Node 后，根 `npm test` 会话 18615 完整退出 0，包含新增教师 REST/真实 PostgreSQL 检查、云端完整 pre/test/post、桌面确认与原业务对比及后端/发布脚本测试，最后为 miniapp release config / backend runtime port / deploy env checks passed。此为当前总测试证据，不表示小程序真实上传、全页面实操或生产部署已完成。
- 尚未完成：教师创建教学用新档案及其维护范围、共享地址维护、其他原业务差异、逐页面/多角色和生产发布验收。本项自身档案修复不是将全部角色方案缩减为“只允许本人”。

## 2026-09-08 确认内容实时读取与删除对象标识（UTF-8）

- 承接 49086d27 实操发现的两项问题，不更改原业务窗口、布局、字段、删除规则或云端权限。React 规范仅用于将草稿读取放在点击查看时；保留原确认窗口、依赖顺序和提交前快照一致性保护。
- 实际确认处理函数测试先失败（旧列表显示旧课程/地址），修复为点击时读取当前 outbox 列表、同时解析最新关联地址；已移除/已完成或读取失败时不开过期确认窗。测试验证打开窗口不提交，点击确认提交的快照等于显示的当前内容。旧 ID-only 删除草稿先读取云端对象；查不到时保留草稿并提示，不展示盲目删除确认。
- 原 12 个业务删除方法测试先失败（删除缓存后名称丢失）。现在只把被删除记录传入既有草稿适配器，以字段白名单保存本地展示快照；云端删除 payload 仍严格只有 id/expectedVersion，不包含姓名、手机号或展示内容。批量课表删除也保留原记录用于展示；原备份/删除/保存时序不变。财务/成绩按实际 student_id 展示关联学生，不虚构 name 字段。
- 实际处理函数/React 确认渲染、12 方法缓存→适配器→展示、适配器原测试、browserDatabaseSyncCapture（含课程地址、学生学校、排课比较、机构草稿）、authority client/runtime、uiRegression、desktop-layout 均已退出 0。新类型检查会话 65366 退出 0。构建会话 29032 退出 0，`main.5b5d6f0e.js`、`249.1b8dc5ae.chunk.js`；主 CSS `main.5792781d.css`、日历块 `450.eed1888f.chunk.js` 不变。
- 资源实操脚本去掉手动刷新步骤；修改确认断言当前名称，删除确认新增对象名称断言。实际运行结果另记，函数测试和构建不代表多端/整页验收。没有打包、NAS 更新或部署。（UTF-8）
- 无手动刷新实操会话 25837 退出 0，目录 `gewu-business-parity-e8zmu214`；副本 `gewu_ui_shadow_46ecd3a97179cfad` 清理成功。三类编辑/删除全部经过真实原控件：学生成功提交；教师与地址按既有权限被拒绝，原草稿内容保留，云记录未变。所有修改和删除确认名称断言均通过，仍保持 uiVerified/businessFlowComplete=false。
- 已查看学生删除、教师修改、教师删除、地址删除四张截图。学生删除窗显示“林小禾待删除”和学校，地址删除窗显示名称和地址；教师修改窗口显示“角色测试老师复核”，同时背景列表仍显示“初改”，直接证明确认读取没有依赖旧列表。教师删除截图捕获了过渡状态、确认窗口不可见，不能作该状态视觉通过证据；其名称断言和权限检查通过不等于截图已合格。
- 学生删除截图 SHA256 `89edbb1776bcd762d86d8e4940a795447576919587573fec83b6e8674b59963c`，教师修改截图 SHA256 `84415daf71f64493a6201cd3b4adb1e8a1f57f15e6acda7ef07fb206be8f1614`。Electron 测试后 Node 恢复会话 48778 退出 0，root/backend ABI137。项目总测试另行运行，不以此前专项结果冒称总测试通过。（UTF-8）
- 当前完整根 `npm test` 会话 49065 已退出 0，包含前置原业务 144 批量 + 108 出勤对比、云业务完整前后置 PostgreSQL 测试、桌面同步/权限、题库/导出、后端和发布配置检查，最终输出 miniapp release config / backend runtime port / deploy env checks passed。这是本次总测试成功证据，不追溯覆盖此前失败会话，也不表示 Word 实际导出、小程序逐页面或生产部署已验收。版本文件哈希仍保持用户原值，发布继续暂停。（UTF-8）

## 2026-09-08 学生草稿实操暴露的微秒版本冲突（UTF-8）

- 资源专项首轮会话 56543 退出 1，证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-28_lrgnw`：学生离线编辑、联网再次编辑均保留草稿，但用户确认后得到 `CLOUD_BUSINESS_STUDENT_CONFLICT`。联系人基线为 `2026-09-08T14:21:02.254223+08:00`；副本回执 cleanupComplete=true，未写生产库。
- 代码定位：桌面序列化只归一化毫秒版本；云 API 的 instant 只接受毫秒规范时间。联系人验证把解析失败的 null 当作新增联系人所用的空基线，造成错误冲突。新增真实客户端到 HTTP 接口测试先得到 `CLOUD_BUSINESS_INPUT_INVALID`，修复后通过。
- 修复仅涉及并发版本：expectedUpdatedAt 保留 PostgreSQL 六位小数精度并归一化 UTC；所有业务版本验证统一使用保精度检查，排课起止时间等事件时间仍使用原验证。联系人无效版本返回 400，不再变为空基线。没有改数据库时间戳、权限、表单、窗口、布局或业务规则。
- 新测试覆盖毫秒/微秒、正负时区、非法日期/时间/过高精度、单独联系人接口，以及相差一微秒仍冲突。原排课 REST 回归也增加微秒校验。完整 `npm --prefix cloud-business-api test` 会话 58138 退出 0（含前后置 PostgreSQL 检查）；桌面 identity client 与 HTTP 冷启动检查退出 0，类型检查 64800 退出 0，副本保护测试 2 项通过。
- `npx craco build` 会话 53507 退出 0，主包 `main.23088ccf.js`；主 CSS `main.5792781d.css`、日历块 `450.eed1888f.chunk.js` 未变。新增资源专项只覆盖学生、教师、地址的原编辑/删除及草稿确认，不能代表全部业务或多端验收；修复后的实际桌面复测结果另记。未打包或部署。（UTF-8）
- 修复后实操会话 52753：学生编辑和删除均实际 completed，联网再次编辑未夹带草稿、删除也等待确认，已读取云端结果。随后教师确认失败，专项整体退出 1；目录 `gewu-business-parity-7ok9bji0`，副本 `gewu_ui_shadow_cfa0b57250bcec9c` 已清理。截图显示确认窗为旧的“角色测试老师初改”，实际草稿为“角色测试老师复核”，仍 awaiting_confirmation 且 confirmation/receipt=null。确认保护没有错误提交旧内容；面板每 5 秒刷新导致旧显示的问题待修，不能冒称教师权限检查通过。
- 已查看上述学生修改确认、学生删除确认和失败截图；删除确认截图仅截到过渡动画的细小区域，不能作为视觉证据。后续脚本使用现有面板刷新按钮、断言确认名称等于本次草稿，并用关闭动画的整窗截图取证；该测试操作不代表旧内容显示问题已修复。（UTF-8）
- 最终专项会话 32345 退出 0：目录 `gewu-business-parity-qn1x50na`，副本 `gewu_ui_shadow_6656bbc8273af5d4` 已清理，ok/cleanupComplete=true，productionWrite/uiVerified/businessFlowComplete=false。学生更新、删除 completed；教师及地址更新、删除 conflict，回执为 CLOUD_BUSINESS_ACCESS_DENIED，草稿原文完整保留、云端记录未变。三类再次编辑及删除均未夹带离线更改。读取证据 `resource-confirmation-readback.json` SHA256 `b2c22ced26c74cf4e8acd45fb53a9f5acd0dcaffd110cf7bce5d3e551ca4deca`。
- 已查看最终学生删除确认、教师更新确认/拒绝结果、地址删除拒绝结果截图。教师确认显示本次“复核”名称，拒绝后提示草稿保留；学生删除确认只写“删除学生”没有对象名称，此显示缺口仍待修。保留本地删除后的空列表是草稿覆盖缓存，不是云端删除成功。顶部过渡通知可能同时显示旧离线提示，不宣称整页体验完成。测试没有新增教师权限，不能将按现有规则拒绝视为用户角色方案验收。
- 按 Electron ABI 技能恢复 Node，会话 46592 退出 0，root/backend 均为 137。保护的版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`；未修改用户 NAS 脚本，未打包、更新 NAS 或发布 OSS。（UTF-8）

## 2026-09-08 学生、教师、地址的同类草稿边界（UTF-8）

- 承接已实操通过的课程检查，检查原 StudentList/TeacherList/RoomManager 的编辑和删除路径。新增真实处理函数测试先失败：StudentList 存在 awaiting_confirmation 的新建草稿时，删除实际调用云端而非继续保存本地更改。
- 新增只读 `businessDraftSubmissionGuard.mjs`，在三页六个入口检查同实体/同记录的未完成草稿，再沿用原本地草稿路径；不自动确认、不引入新云协议。完成草稿、其他记录或其他实体不阻止现有在线操作。原编辑表单、字段、按钮、弹窗和业务规则未变；没有顺带重构课程页面已验证的检查。
- `ResourceDraftConfirmation.test.js` 执行三页真实编辑/删除函数，覆盖 3 实体 × 2 操作 × 3 草稿类型 × 5 状态 × 同/不同记录，共 180 情况，已通过；另检查跨实体同 ID 不阻断、读取失败不被解释为空队列。这里云写入/本地保存端点为调用记录桩，不冒称本轮已实操完这三页或证明真实草稿合并。
- 学生/教师/地址各自 cloud-write 检查、课程检查（含原 90 组合和新 180 组合）、desktop-layout（含六页 Form AST 保真）、uiRegression 均退出 0。类型检查会话 6005 退出 0；构建会话 67959 退出 0，`main.8ce53127.js`，主 CSS `main.5792781d.css` 与日历块 `450.eed1888f.chunk.js` 不变。
- 仍需三页实际编辑/删除与角色权限验收，其他适用类型也不能据此直接签字。未改用户三处脏文件，版本文件哈希保持原值；没有新部署、NAS 更新或 OSS 发布。

## 2026-09-08 原桌面课程草稿实际确认与可见内容（UTF-8）

- 使用真实源桌面、独立密码测试账号、已校验完整云库副本，不安装新版本、不写生产业务。Playwright Interactive 技能所需交互工具不可用，复用既有 Playwright/Electron 脚本；Electron ABI 技能要求测试前切到 119，结束后恢复 Node。新增 `business-parity-course-confirmation.cjs` 只通过原控件修改、筛选、结课、确认和恢复，读取接口只用于核验。
- 首轮会话 26370 退出 1：离线时长变更、联网结课、拒绝/接受确认以及云端字段断言已走过，但确认后页面重新挂载回默认未结课筛选，脚本没有重新选已结课便试图编辑，因此找不到行。保留失败证据；仅修正测试筛选步骤，未改变产品筛选规则。副本清理成功。
- 第二轮完整脚本会话 24498 退出 0。新路径实际将默认时长 90→120 分钟离线保存，重连保持云端原值；联网点击结课后仍为一个待确认草稿，保留草稿不写云端；确认后 active=false、时长 120，原名称、教师、地址、计费单位/模式、价格与学生定价一致。随后通过原表单恢复 active=true、时长 90，完成草稿不再阻挡正常在线保存。完整脚本后续原排课、调课、批量、出勤和六页弹窗步骤也运行通过，但不替代全部业务/多端验收。
- 本机证据目录 `gewu-business-parity-e3r10hyr`，`course-pending-confirmation-readback.json` SHA256 `3c6a7c5363b326f55967a776b3bfb8d2ba289710105159e69103ae16a4fc2539`。副本 `gewu_ui_shadow_1053b5d4a2c5ebe4` 回执 ok/cleanupComplete=true，productionWrite/uiVerified=false。此轮构建为 `main.72f08a27.js`，不能据此认证后来新增的确认内容。
- 人工查看 09a/09b/09c 截图发现：确认窗口没有列出真正变更的课程状态和默认时长。因此没有把功能断言通过当作确认体验合格。先补测试复现失败，再仅给现有确认信息增加这两项；课程编辑窗、课程列表布局与按钮不变。确认数据展示、确认面板和 uiRegression 均退出 0。
- 新构建会话 18301 退出 0，`main.1364e63f.js`，日历块和全部 CSS 未变。新增 `--course-confirmation-only` 用于这类修复的专项实操，回执明确 scope，仍保持 uiVerified/businessFlowComplete=false，不把专项当全流程。
- 新构建专项会话 79615 退出 0，目录 `gewu-business-parity-aax96pac`，副本 `gewu_ui_shadow_86204e0c17ce59be` 已清理；回执 ok/cleanupComplete=true、productionWrite/uiVerified=false、scope=course-confirmation-only。已亲自查看新 09b 确认截图和 09c 恢复截图；09b 显示“课程状态：已结课”“默认时长：2小时”，内容和两个确认/保留按钮完整可见。原大表水平滚动、测试教师长名称和右上既有 Cloud account 仍在，不据此声称整页无缺陷。
- 新专项 `course-pending-confirmation-readback.json` SHA256 `bd0aa70a89979e05ae1fc5ff889d11bd21b30ada8ebff50ffa417df8016072cf`；09b 截图 SHA256 `88184b6fca5f5cdadf11d005b35f1fe23caf95959ee77bfdb5caa5b415cb79aa`。新增 UI 实操只覆盖保存/结课/保留/确认/恢复，不把函数测试的 90 种组合都称为实操，也未新增删除和所有角色验收。
- 最后 rebuild:node 会话 60785 退出 0，根/backend 均为 Node ABI 137；确认面板、展示、课程完整检查（含 90 组合）、副本保护测试再次退出 0。用户版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。无打包、部署、OSS/NAS 更新，长期全量门禁仍开放。（UTF-8）

## 2026-09-08 课程未确认草稿防夹带修复（UTF-8）

- 数据映射检查发现：当前课程缓存可能叠加未确认的离线修改，而 `submitCourseToAuthority` / `handleToggleActive` 仍将整条缓存直接提交，删除也绕开同课程的未完成草稿。先扩展真实处理函数测试，旧代码明确失败：期望仅保存草稿，实际调用云端接口。该证据证明调用边界错误，不冒称已经在生产库发生误写。
- 仅在这三个原操作入口增加同课程未完成草稿检查；匹配 create/update/delete 的原记录 ID，任何非 completed 状态都继续保留本地更改，仍由现有确认面板提交。检查本身不自动确认、不重写已有草稿；后续修改沿用既有本地草稿保存机制，其合并和回执另有测试边界，不能以本轮入口桩测试代替。已完成和其他课程草稿不阻止正常在线操作。原弹窗、字段、按钮、结课含义、原地址联动和计费逻辑均未改。（UTF-8）
- 原函数测试覆盖 3 种草稿 × 5 种状态 × 3 个操作 × 同/不同课程，共 90 种情况，并检查已有草稿原文不变；课程完整检查退出 0。地址联动测试首次因新增函数未注入测试上下文而失败，接入真实检查函数后通过，没有替换为始终放行的假函数。
- 当前类型检查退出 0；生产源码构建会话 45701 退出 0，主包 `main.72f08a27.js`，课程块 `124.668c8dd4.chunk.js`。日历块 `450.eed1888f.chunk.js` 与样式 `main.5792781d.css` 未变。课程检查、desktop-layout 和日历云写入/卡片/列表检查组合退出 0。
- 尚未新增这 90 种情况的真实桌面点击/云端副本验收，不把函数测试称为跨端实操。其他资源页是否存在同类旧草稿夹带仍须检查；本修复不代表所有业务类型已收口。发布门禁仍未满足，没有递增版本、打包、更新 OSS 或 NAS。

## 2026-09-08 收紧范围后的当前复验（UTF-8）

- 本轮只新增未结课程列表回归、接入现有日历检查入口并记录结果；没有改动任何产品组件、业务规则、样式、版本或部署脚本。用户现有三处跟踪修改保持原样。
- `test:desktop-layout` 当前退出 0：六页 Modal 容器/原宽度/表单语法树检查、导航规则检查、1200/1536 两尺寸 Chrome CSS 几何检查通过。浏览器专项使用实际 CSS 和测试结构，不冒称本轮重新打开了完整安装版。
- `ScheduleCalendar.card-content.test.js` 当前退出 0：真实组件片段只输出完整课程名、地址、起止时间，不额外输出内部 ID、科目或教师。
- 新增 `ScheduleCalendar.sidebar-parity.test.js` 从当前及 `8118419f` 各自提取真实 Sidebar、命名函数和宽度，使用真实 React/Ant Design 渲染，再比较相同数据经过云端派生缓存后的列表。覆盖两位教师、无匹配课程、未选教师、无教师、空课程，以及未确认结课草稿；验证顺序、课程名、年份/学期、地址/类型、220px 宽度、可拖动属性与不改写输入。注入内部编号替代课程名的负面对照能被检测。此基线只证明这段迁移前后代码同数据一致，不代表用户认可整版历史 UI。
- 新测试首次退出 1 是测试错误地预期 `2026提高班-A01` 的年份显示为 `-`；旧版和当前实际都按原正则显示 `2026`。依据原行为修正测试预期，没有修改产品代码。随后独立测试与完整日历云写入检查均退出 0。React 技能仅用于检查真实组件并避免改写组件结构，没有性能或视觉重构。
- `test:business-parity` 当前退出 0，含 144 批量排课与 108 出勤计费组合。以上均不是当前生产数据逐记录验收、安装版实操、全量根测试或多端发布证明；这些门禁仍未完成。测试和文档变更不触发桌面/NAS 更新。

## 2026-09-08 用户现场指出的窗口和导航差异（UTF-8，优先于后文旧结论）

- 刚才真实自动化所开的学生窗口是右侧抽屉，不能称为已经保留用户记忆中的原弹窗。追溯 `716d060c`（2026-06-12，`feat: migrate core resource pages to drawers`）：其父版本 StudentList 使用 Modal、宽 700；该提交改用 DataPageLayout 的 Drawer、宽 560。此前使用的 `8118419f` 基线已晚于这次修改，故该基线不足以证明窗口形式保真。教师原弹窗宽 600；其他资源页 `fe404f8a` 也有抽屉迁移历史，须逐项核查，不能仅还原学生便声称全部恢复。
- 导航压缩主区域已定位到 `ee5d8c58`（2026-09-05）：新增 `.app-shell--nav-pinned .app-shell__main { margin-left: 236px; width: calc(100% - 236px); }`。`8118419f` 没有这条规则；刚才测试点击 `.app-shell__collapse-button` 固定导航后触发。现有 pinned-layout 测试反而要求压缩宽度，不能把该测试通过作为符合用户原行为的证据。
- 用户再次要求不要改动原界面。本轮截至此记录仅检查代码历史、扩展验收脚本，没有改 StudentList、DataPageLayout、AppShell 或 index.css；不声称抽屉和导航已恢复。后续需以用户指出的原独立弹窗、导航不挤压主界面为恢复标准，并分别留下实际尺寸和交互证据，不再对原界面做设计调整。
- 真实源桌面实操会话 40908 退出 0，完整云库副本 `gewu_ui_shadow_50cc61949cdfcb9c`：创建学生林小禾及学校，确认离线草稿；原课程表单创建初二物理、自有/一对一/90分钟，选入该学生，内联输入东湖上课点，180/120 每小时费用自动汇总；课程与地址确认前均未写云库，确认后均 completed。刷新、重新进入课程信息并打开编辑，姓名、地址和费用保留。随后在既有课程表日期标题双击打开排课窗口，创建 12:00–13:30 排课；离线及重连均无静默写入，确认后云端排课金额为 270/180，计费单位与教师计费模式保留，学生关系正确。这里的“原”仅指既有课程/排课实现，不作为学生窗口或全部界面形式保真的签字。
- 本机证据位于 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-lgyvqh30`：course-readback.json、schedule-readback.json、09-course-reopened.png、13-calendar-card.png 和 receipt.json；课程卡片截图已查看，仅见“初二物理”和“东湖上课点 12:00-13:30”。测试副本、容器及辅助目录清理完成，生产数据和服务、NAS均未修改。调课、刷新后排课金额、多角色小程序、全部业务组合与 Word 可编辑公式仍未通过完整验收，不发布。
- 前三轮失败分别是学期可见项与读屏镜像重复、已选时长文字遮挡内部搜索输入、编辑按钮名称包含图标；均为自动化定位问题，原失败证据保留，脚本修正为可见选择面和实际可访问名称后同链路通过。没有据此改业务控件。

### 2026-09-08 恢复执行记录（UTF-8）

- 已实删导航固定时缩减主区域的 CSS；不改变原悬停、固定、取消固定和菜单跳转事件。AppShell 源码未改。旧 pinned-layout 测试中强制要求缩减宽度的断言已删除，替换为保持工作区尺寸；实际 Chrome 几何检查在 1200/1536 下验证主区域开关前后矩形一致。
- DataPageLayout 删除 Drawer 容器及旧 drawer 参数，不留兼容分支；学生恢复宽 700 的 Modal，老师/机构/缴费 600，学校/地址使用原 Modal 默认 520。各页保留原表单内容、按钮事件和 destroyOnClose 设置；没有把页面接口改写成另一条保存路径。课程已是 Modal，仅同步 footer 类名；排课列表和操作日志删除无效的空抽屉参数，不新增编辑能力。
- 新增 DataPageLayout.modal 测试先在原 Drawer 下失败，恢复后通过；它检查 Modal 取消/可见性/内容传递、六页宽度，并逐页比较恢复前 `46f148bd` 的 Form 语法树，证明此次容器恢复没有修改表单字段、校验或字段事件。该提交只用作“本轮未改表单”的比较点，不再把它当作全部原业务基线。React 技能用于保留现有组件和表单状态，不进行风格调整。
- 类型检查会话 40363 退出 0，uiRegression 与 desktop-layout 均通过；源码构建会话 85750 退出 0，主包 `main.035ec7bd.js`、CSS `main.5792781d.css`。未触碰用户的版本文件；没有递增任何端版本、打包安装程序或发布。
- 真实阿里云副本会话 3123 退出 0，学生→学校→课程→地址→排课的显式确认和读取仍通过。六页均实际打开/取消，草稿列表未发生变化，1536 宽截图已逐个查看：学生长表单可滚动至原取消/确定按钮，其余五窗可完整显示。证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-6_b_tuk8`；副本 `gewu_ui_shadow_383d244a1466e63c` 已清理。该轮测试请求缩至 1200，但 Windows 最大化状态忽略 setContentSize，实际仍为 1536；因此这轮**不能算双尺寸验收**。已修改测试为先取消最大化并断言真实 innerWidth，再补验 1280/1200。
- 实操诊断中另修正了脚本等待动画准备帧、读屏重复节点、滚动条可用宽度和鼠标离开悬停导航的条件；这些修正均未通过禁用动画、强制点击或改业务控件使测试通过。早先动画截图/最大化伪双尺寸证据不用于最终签字。
- UTF-8 最终补验：真实源桌面会话 **92308 退出 0**，完整云库副本 `gewu_ui_shadow_b5eee07e43f60255`。1280 和 1200 两个实际 innerWidth 下，六页分别打开/取消，共 12 项；原宽度、水平居中和无可见 Drawer 断言通过，取消前后草稿列表一致。12 张顶部截图已逐张查看，学生长表单另检查滚动到底部后的按钮。实际桌面 1536 宽导航打开前后主区域矩形均为 x=0、width=1536；1200/1536 的 CSS 浏览器专项亦通过，不把真实导航检查误记为两个尺寸。
- 同一会话经真实密码登录和原界面完成学生/学校、课程/内联地址、排课的离线草稿及明确确认；重连不静默写入。课程刷新重开后保留学生、地址和 180/120 每小时费用；12:00–13:30 排课云端读回学费 270、教师费 180，学生关系与计费模式正确。未做调课或刷新后排课金额验收，`businessFlowComplete`/`uiVerified` 仍为 false，不据此关闭全部业务清单。
- 本机证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-p25aqsku`：`resource-modal-checks.json` SHA256 `b943a91ef5f8d6bfe13b3d3599f37262bb616977bd3ee5445d47a03d4c2038ea`；`modal-1200-学生-top.png` SHA256 `2c0a6a93c783ce6786bd70b455c623649cd20d0c499cb3adc7312794a0d0fbbb`；`navigation-geometry.json` SHA256 `264347af534914ab30926eae51ac1944b1366e9f246c44fbb5502662f8299c30`。回执 `cleanupComplete:true`，临时副本、服务容器和辅助目录已清理，原完整备份保留；没有写生产业务或更新 NAS。
- 截图复查另发现既有 `Cloud account` 浮层仍高于弹窗遮罩，学生窗口滚动时有局部遮挡；列为未处理问题，不为修容器擅自重排表单。1280 学生截图同步提示短暂显示待同步，未单独证明其刷新原因；不能把本轮界面称为无缺陷。
- 最终 desktop-layout（含真实 Chrome 几何检查）、uiRegression、脚本语法检查均通过。Electron 实操后已执行 rebuild:node（会话 58976 退出 0），根和 backend 原生依赖均恢复 Node ABI 137。用户 `src/generated/version.ts` SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。
- 完整根测试会话 **46642 退出 1**，此次输出止于 cloud-business-api 的 posttest 教师课程 PostgreSQL 用例，npm 日志仅记录 exit 1，无断言诊断；没有把之前的原生退出码当成本次原因。该 PostgreSQL 用例独立执行退出 0，整个云端 posttest 再执行会话 **67766 退出 0**，但不以分段成功替代完整根测试成功。保留全量入口未通过状态，未部署、打包或发布。

编码：UTF-8。核查起点：301d6ad2。界面对照版本：6e1c5ef36bc4e432e8935272fc3840623c732124（新增简化排课编辑窗之前）。该版本用于定位具体差异，不等同于已经确认所有模块的最初业务基线。

### 2026-09-08 原排课窗口调课检查（UTF-8，未作全业务签字）

- 接续已提交的弹窗/导航恢复，只扩展 `business-parity-desktop.cjs` 的原日历操作。先创建并确认 12:00–13:30 的隔离测试排课，刷新后双击原卡片打开原排课窗口，再检查调课、计费、版本基线、冲突拒绝与确认后的重开。没有新增调课窗口或保存接口。
- 第一轮会话 1951 因脚本刷新后漏展开“教务”菜单失败；副本 `gewu_ui_shadow_7a216e403cc38405` 清理完成。第二轮会话 58985 想调到 14:00–16:00，但完整备份中该教师已有 14:00–15:30 的排课，原冲突保护阻止保存；副本 `gewu_ui_shadow_6de6b837aa85fd2d` 清理完成。没有删除旧排课或绕过冲突，后续保留该拒绝用例，并另选相邻而不重叠的 16:00–18:00。
- 对原窗口的冲突提示进行实际模板回归，先复现云端 UTC 时间被截为 06:00–07:30 而不是本地 14:00–15:30。生产源码只修改 `handleSave` 的冲突提示和修改日志两处时间格式化，复用已有 dayjs；冲突条件、费用计算、表单、状态事件未改。该回归通过，类型检查会话 73707 退出 0，源码构建会话 23867 退出 0（`main.1eb0d45b.js`，CSS 不变）。React 技能未引发组件重构；Electron 原生依赖技能用于测试运行环境切换，不打包安装程序。
- 调课真实副本重跑结果另记，不能把上述模板用例当作完整实操成功。此前全量根测试未通过的状态仍保留。
- UTF-8 补充实操结果：会话 **41709 退出 0**，完整副本 `gewu_ui_shadow_fcfa4f240e1de817`。14:00 冲突提示的实际可见 DOM 匹配本地 14:00–15:30，窗口保留、草稿列表完全不变；之后 16:00–18:00 离线保存成功，草稿保存修改前云端 updated_at 基线。重连后整个云端原排课对象未变；原确认窗口发送后草稿 completed，同一排课 ID 的时间更新，学费 270→360、教师费 180→240，原学生明细、出勤、地址、课程、教师、计费单位/模式均一致。刷新后双击同一原卡片，窗口读回 16:00、2小时、18:00 和原地址。此次是“原窗口修改时段”验收，不等于拖动、拉伸、复制、撤销重做或全业务完成。
- 证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-r4_0pnol`；`reschedule-readback.json` SHA256 `50a5142194c2094eb6428408c202448aeea644f26c5de76abf58cc25915b1cc5`，`16-reschedule-reopened.png` SHA256 `5642ba446b015b309fd87b23e91f9da8372686180eaa927b9c5d935d405606fb`。重开窗口与卡片截图已查看。冲突截图拍在提示动画早期、文字未完整入画，不作为该提示文字的视觉签字，提示内容依据实际 DOM 断言及真实源码模板回归。原有账户浮层及选择框收起时序未在本轮重设计。六资源弹窗双尺寸检查继续通过，但整体 `businessFlowComplete:false`。
- 回执 `cleanupComplete:true`：仅本次副本、临时服务容器及辅助目录已清理，验证备份保留，生产与 NAS 未改。实操后 rebuild:node 会话 **2650 退出 0**，root/backend ABI 137；业务保真专项、调课时间模板与卡片、草稿适配、uiRegression 均通过。用户版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。未再宣称全量根测试成功，未打包、部署或发布。

### 2026-09-08 拖动、拉伸与历史操作复查（UTF-8，初轮撤销结果不放行）

- 增加原卡片鼠标拖动到次日、下边缘拉长 30 分钟、原撤销/重做控件与键盘 Ctrl+Z、明确确认及云端读回。坐标取自实际卡片和日期列矩形，不直接调用页面保存函数。测试仍只使用完整云库隔离副本和测试教师。
- 会话 83847（`gewu_ui_shadow_52414f291fab6610`）和 2099（`gewu_ui_shadow_f421959835d0a989`）退出 0，但复查第二轮 `19-undone-draft.json` 仍是 18:30/450，而预期撤销为 18:00/360，因此两轮 **不能证明撤销正确持久化**。`gesture-readback.json` 能证明最终跨日、2.5 小时、450/300 的确认写入，不可替代中间历史状态验收。两副本及临时服务已清理，生产不变。
- 最初源码级回归要求一个内部 dirty 标记，未覆盖完整事件链；不能据此直接推断真实故障。其临时两行生产修改已撤回，先保留原状态转换回归。现已将实操等待改为同一份实际读取连续稳定 600ms 后返回，并直接保存该对象、核对卡片；失败时记录期望与实际草稿。后续以这条更严格实操决定是否修业务代码，不能为了绿色检查改动原规则。
- UTF-8 更严格实操会话 **12004 退出 1**，真实复现：`failure.json` 的卡片为次日 16:00–18:00；`unstable-history-draft.json` 期望 18:00/360，但连续读取仍为 18:30/450，抛出 `SCHEDULE_DRAFT_DID_NOT_STABILIZE`。这次使用未增加标记的旧构建 `main.1eb0d45b.js`/`450.db467b39.chunk.js`，不是凭新单测推断。证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-q0m5z_y5`；副本 `gewu_ui_shadow_02095897badb9136` 已清理，未确认错误草稿进入云端。
- 在真实失败证据后，仅给原 undo/redo 非空历史分支补齐 `schedulesDirtyRef.current = true`，复用现有 `persistScheduleCalendarState`、加密草稿合并和用户确认路径；空历史不保存、不新建历史、不改按钮/快捷键、不引入自动云提交。原状态转换测试与保存门禁断言通过，已纳入 calendar cloud-write 入口。React 技能用于保留原函数式状态更新；最终严格实操另记。
- UTF-8 最终严格实操 **93617 退出 0**，副本 `gewu_ui_shadow_8a6316328992fb3c`。实际拖动到次日保持两小时 360/240，下边缘延长半小时得到 450/300；Ctrl+Z 后同一草稿稳定读回 18:00/360/240，原“重做”按钮后读回 18:30/450/300，两次均保留修改前云端版本。重新联网时整个云端排课对象仍未改变；原确认窗口发送后云端读回次日 16:00–18:30、450/300，学生明细、课程、地址、教师、计费模式不变；刷新后仍显示相同卡片。没有以“脚本退出 0”代替回执内容复查。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-h1ja4nt5`：`19-undone-draft.json` SHA256 `0d7c43dc86b6b94288fcebe28a8920c148404e1b833aee836eab17b67625bb7c`，`gesture-readback.json` SHA256 `ebfd0152abee878329a62f5b75a0b33fcf0d8692e20f91e79f663d75c40f6564`。撤销及重做回执字段已读回核实，刷新卡片截图已查看（SHA256 `0d07b03ec11746c8dd1f8651cd85a11ffb53b9e82f7dd624e5a91d041a87786f`）。临时副本、服务和辅助目录清理完成，备份保留；生产业务、NAS 均未修改。
- 构建会话 29471 退出 0（`main.2ce12de7.js`、`450.0255ae7a.chunk.js`；CSS 不变），类型检查 23538 退出 0。本次只验证单课跨日移动、下边缘延长、一次撤销/重做和确认闭环；上边缘缩放、Ctrl 拖动复制、批量操作、已确认后的历史操作等仍未放行，不称全部排课业务完成。
- UTF-8 收尾：rebuild:node 会话 55099 退出 0，root/backend ABI 恢复 137；业务保真、日历历史/卡片/云写入、草稿比较、uiRegression、desktop-layout 均通过。用户版本文件哈希保持 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。未更新版本或任何部署产物，旧全量根测试未通过的记录不撤销，整体发布门禁仍保留。

### 2026-09-08 原框选与批量时间适配（UTF-8，实操结果另记）

- 源码回归先复现两个云端时间问题：`batchSelectionGeometry` 对 ISO 排课返回 success 但 changedIds 为空；实际 `getCoursesInRect` 回调按空格拆时间时报错。生产改动只统一四个操作分支及批量移动/复制的时间解析，复用 dayjs，以本地日历时间处理位置、以 ISO 传输结果；冲突比较改按同一时刻比较，兼容旧文本与云端时间混用。未改框选范围、鼠标事件、窗口、费用计算或学生明细。
- 增补原 24:00 端点回归时，先复现 RangeError。结束时间按开始日期计算日偏移，保留原 24:00 格子；旧文本和云端午夜均验证向前移动一小时后仍为一小时课程。单课复制源不变、两课移动源不变、混合时间冲突拒绝、跨 UTC 日期的本地框选及取消状态过滤均通过。两项回归已加入 `test:business-parity`。
- 初次补参构建/类型检查因 JS 默认 undefined 被推断为仅 undefined 而失败（42535/10521）；改为可选空字符串后，类型检查 **57718 退出 0**，源码构建 **50984 退出 0**（`main.748335f2.js`、`450.eed1888f.chunk.js`、CSS 不变）。没有改版本文件、生成安装包或发布。
- 现有源桌面实操脚本增加原矩形框选、Ctrl 拖动复制一课、框选两课再移动两天；从实际 DOM 矩形取鼠标坐标，保留稳定草稿读取、确认前云端对象不变、修改版本基线、费用和学生明细、未选课不变及刷新后的原卡片检查。未直接调用 React 保存函数。此处只记录新增验收路径，不将脚本存在当作实操通过。
- 首次实操 **89867 退出 1**：框选复制已 completed，两条批量移动草稿时间及 450/300 金额、原版本基线均通过；第一条确认成功后同步面板随日历刷新收起，脚本没有重新打开，第二条保持 awaiting_confirmation。失败 DOM 仅显示“1 条本机更改待同步”，不是第二条业务提交失败。修正脚本按可见状态重开原同步入口，不改面板行为；证据 `gewu-business-parity-alb4tzei`，副本 `gewu_ui_shadow_c012a3f5f970d636` 与临时服务已清理。这轮不算完整批量闭环通过。
- 第二轮 **20227 退出 1**，复制草稿仍 awaiting_confirmation：脚本外层刚点击打开面板，内层在动画准备帧判断按钮不可见，又点击一次关闭，导致点击等待超时。删除外层重复打开，每条由一个助手步骤打开一次，并等待上一次面板关闭；未加 force 点击、禁用动画或改变产品事件。证据 `gewu-business-parity-2mtwx30s`，副本 `gewu_ui_shadow_915336180461788b` 已清理；此轮亦不放行。
- 最终实操 **29548 退出 0**：副本 `gewu_ui_shadow_b89ecfb14a3f4635`。真实框选周二 16:00–18:30 课程并 Ctrl 拖至周三，确认后新 ID 在云端创建，原课整个对象未变；刷新后框选两张卡片一起移至周四/周五。两份草稿各保留修改前 updated_at，重新联网后云端排课集合完全未变，逐条原确认窗口发送后云端读回正确日期、相同 16:00–18:30、每课学费 450/教师费 300，学生单价 180/120、出勤、课程、地址、教师和计费模式保留；未选排课整个对象未变。刷新后的两张原卡片均核对通过。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-0bkyjcq7`：`batch-readback.json` SHA256 `4cf18520ba1abe7590538cb9e50e881ba048cb09bcb0c4db777ab907c9339573`；`23-batch-selected-two.png` SHA256 `020262b892e14197475a4736938e1e68cde779b0c6bc13ae12ac6954b9445eb4`。已实际查看两课框选及刷新后两张卡片截图，并读回最终两课金额与时间。此前学生/课程/原窗口调课/单课拖动拉伸/撤销重做以及 12 项资源弹窗尺寸检查也在同一脚本通过，但不能据此宣布全部业务和逐页面视觉审计完成。
- 回执 cleanupComplete=true，仅删除本次隔离副本、临时服务与辅助目录，备份和本机证据保留。本轮没有修改生产业务、NAS、部署版本或安装程序。Electron 实操后 **64084 退出 0**，根与 backend 依赖恢复 Node ABI 137；用户版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。整体 `businessFlowComplete`/`uiVerified` 仍为 false；批量日期排课、多学生/多计费模式、批量删除及原版其他差异继续核查，全量根测试旧失败记录保留。
- 恢复 Node 后收尾验证 **61847 退出 0**：test:business-parity（含新框选/批量回归）、原日历云写入/三字段卡片/撤销重做、排课草稿比较、scheduleStorage、uiRegression、desktop-layout（含浏览器导航遮挡检查）均通过。仅对本轮范围签字，不替代全量根测试、多角色小程序、Word 原生公式或发布验收。

### 2026-09-08 多日期与双学生费用保真（UTF-8，实操结果另记）

- 当前 `financialDetails.ts` 与 `8118419f` 的文件差异为空。新增回归分别加载该历史版本和当前版本的实际 `handleSave`、`buildFinancialFieldsForSchedule` 与各自费用模块，同输入比较结果，未另写一套替代业务计算。该历史版本是具名函数的比较依据，不将它升级为用户确认的全软件最终基线。
- 144 组源码/草稿比较通过：3 种来源 × 4 种课程类型 × 2 种计费单位 × 2 种教师费模式 × 3 组两学生出勤状态。三个日期中间一日模拟原冲突，原版与当前均只保存其余两日；费用、学生明细、出勤、计费方式、地址、一次历史写入一致；逐条转换确认命令后，REST 客户端收到相同费用和学生状态。真实云响应没有在该单测伪装，单测不证明所有表单组合合法或全部运行时场景完成。
- 扩展现有桌面脚本：原学生窗口创建周清，原课程窗口创建双人讨论课（林小禾/周清，180/120 与 130/80，按次计费、按学生分摊，90 分钟）；原排课窗口使用间隔 2 天和“添加日期”形成三节，离线保存后逐条确认并检查云端、刷新卡片。仅新增测试与门禁，不改生产表单、日期规则、费用函数或部署；运行时结论另记。
- 首次实操 **84618 退出 1**：学生、双人课程和三节排课均已确认，三日期/时长/310 学费/200 教师费断言已执行到首条学生明细；测试错误读取云端 pricing.status，实际云端契约为 attendance_status（桌面缓存适配后才是 status）。修正严格检查 attendance_status，不使用默认正常状态掩盖缺失；提前保存实际云端回执方便失败诊断。原课程表单和三日期窗口截图已查看。证据 `gewu-business-parity-y88xtpcp`，副本 `gewu_ui_shadow_41feca9666c1465f` 已清理；不把该轮视为完整通过，生产未改。
- 最终实操 **98199 退出 0**：副本 `gewu_ui_shadow_cd80a26156bb1649`，按原窗口创建第二名学生及双人课程，三日期为 2026-09-07/09/11、12:00–13:30。逐条确认前云端 schedules 集合与保存前完全相同；确认后恰好三条新排课，每条 90 分钟、按次收费 310/200（不是按小时放大后的 465/300），两学生明细为 180/120 与 130/80、attendance_status=1，billing_unit=2、teacher_fee_mode=2、地址保留。刷新后三张卡片均显示双人讨论课、东湖上课点及 12:00–13:30，截图已逐张查看；原有完整单学生/调课/拖动/框选/双尺寸弹窗检查也在该会话通过。
- 证据 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-rdyewe7p`：`multistudent-batch-readback.json` SHA256 `525d4e48c0431e478e6e2bd0f9faf075a85694ea6632a13285a5f2e7ff06e45e`，`27-multidate-form.png` SHA256 `0c844d0edbc1dff7de2ec29e7514c1e0e23bb2172a87096c3abdcf7fe5965014`。读回三条云端时间、金额、学生明细，与界面证据一致。回执 cleanupComplete=true，隔离副本、服务与辅助目录已清理，备份和本机证据保留。
- 本轮只有测试、测试入口与证据文档改动；生产业务源码未改，沿用上一轮已验证构建 `main.748335f2.js`/`450.eed1888f.chunk.js`。没有更新生产数据库、NAS、安装包或版本。此轮真实签字范围为自有一对二/按次/按学生分摊/正常出勤/三日期新增，不将 144 组源码检查说成全部表单和多角色实操完成；整体业务/小程序/Word/发布门禁仍保留。
- 收尾 **3908 退出 0**，root/backend 恢复 Node ABI 137；验证 **62241 退出 0**，business-parity、financialDetails（含出勤云端往返）、coursePricingRules、scheduleBatchDates、原日历云写入/卡片/历史、uiRegression、desktop-layout 均通过。用户版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`，未暂存 NAS 用户改动或任何 output 文件；本次未重跑全量根测试，旧未通过记录保持。

### 2026-09-08 双学生出勤组合与零费用恢复（UTF-8，实操结果另记）

- 在已有批量保真测试中，分别执行历史 `8118419f` 与当前的实际 `getSchedulePricingsForEdit`、`buildFinancialFieldsForSchedule`、`handleSaveStudentEdit`，使用各自费用模块。新增 36 组：2 种计费单位 × 2 种教师费模式 × 两名学生各正常/取消/请假，覆盖全员非出勤产生零费用。原版与当前输出、原学生单价、未选排课、一次历史写入、云端命令状态和 expectedUpdatedAt 均一致；与原 144 组批量组合一起通过。该回归仍不代表纯机构分支或全部实际表单验收。
- 真实桌面脚本扩展五个组合：[请假,正常]、[取消,正常]、[正常,请假]、[请假,请假]、[正常,正常]。每次从原卡片右键打开“学生出勤和费用”，离线保存两名学生状态，稳定读取草稿、核对旧版本、重连确认云端未变、原确认窗口发送，然后重载重开检查状态与原 180/120、130/80 单价。逐轮断言课程默认对象及其他排课未改变。只改测试，不改窗口或生产收费逻辑；实操结果另记。
- 初轮 **20576 退出 1**，停在双人课程第二名学生的下拉选项点击，未进入出勤矩阵。日志为选项先不稳定、随后不可见；失败截图仍见周清选项，未据此认定产品选择功能失败。改用原有 showSearch 输入姓名并回车选择，同时断言已选姓名，不修改 AutoCloseSelect 或禁用动画。证据 `gewu-business-parity-6nkiie3o`，副本 `gewu_ui_shadow_d873802baca52bac` 已清理，业务未部署。
- 第二轮 **4203 退出 1**：新的筛选定位把 dialog 根定位器放进 filter(has)，等价于在选择框内部再查弹窗，因而找不到第一名学生输入框。改为相对筛选，后续学生卡片也修正同类测试定位；未改产品 DOM。证据 `gewu-business-parity-_8mf_67j`，副本 `gewu_ui_shadow_05498cdad53d29ef` 已清理；仍未进入出勤验收，不算完成。
- 第三轮 **52464 退出 1**：已进入原出勤窗口，在重复点击第二名学生原本不变的“正常出勤”时，再次遇到下拉选项稳定性/可见性等待失败。依据本机 rc-select OptionList 源码的方向键及 aria-activedescendant 支持，测试改为只操作变化的状态，读实际高亮标签后方向键/回车，并断言选中标签；不强制点击、不改选项顺序或业务规则。证据 `gewu-business-parity-4fkc2yr8`，副本 `gewu_ui_shadow_3df5919cef7aa44b` 已清理，未将此轮计为出勤通过。

- UTF-8 最终实操回执核验：`gewu-business-parity-gfdbml3k/receipt.json` 为 ok=true、cleanupComplete=true，副本 `gewu_ui_shadow_fc59f19858ec670f`，productionWrite=false。原进程已结束，因续转未保留该轮终端会话编号，不编造退出码。desktop-receipt 中 five-state 数量为 5、attendanceZeroAndRestore/courseDefaultsPreserved=true，businessFlowComplete=false。五轮云端读回费用依次为 130/80、130/80、180/120、0/0、310/200；原学生单价、课程默认对象和其他排课保持，逐轮确认前云端未变。五张刷新后重开窗口截图已逐张查看，与状态和原单价一致。
- 本机证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-business-parity-gfdbml3k`：`attendance-matrix-readback.json` SHA256 `637e891ccae9a28cbe6a48ae584b4a49e502f3bdd8fa48624368fe7c42d17f29`；全员请假 `33-attendance-reopened-3.png` 为 `8d93d98fdac7faa8cb184bd894ad4c1afdea277ad6514cd6469101c2b06b8039`；恢复出勤 `33-attendance-reopened-4.png` 为 `efd4c1be7e0bd7eca0b380d1066f859c6d3a3a322b159cf9146afece2141e40e`。此证据仅证明自有一对二、按次、按学生分摊的这五种实操状态，不覆盖纯机构课、混合课或小程序。
- 收尾依赖恢复会话 18960 退出 0，root/backend Node ABI 137；重新验证会话 69066 退出 0，覆盖 business-parity、financialDetails、coursePricingRules、scheduleBatchDates、ScheduleCalendar.cloud-write、uiRegression、desktop-layout。未改生产源码，未重建安装包或发布；全量根测试旧未通过状态不变。用户版本文件哈希仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`，NAS 用户改动和 output 未动。

### 2026-09-08 机构与混合课：先核对历史规则（UTF-8）

- `git show 8118419f` 证明：2026-07-13 的该提交已将 `isPureInstitutionSchedule` 改为恒 false，并停用无学生机构费回退；当前并非本轮迁移才删除它。当前 `coursePricingRules.mjs`、`financialDetails.ts` 与该版本差异为空。不能因为旧分支函数仍在，就擅自恢复更早的无学生收费规则；机构课程按既有规则绑定本机构学生（含机构费用专用学生），混合课可选自有与本机构学生。
- 出勤回归不再注入恒 false 替身，而是分别加载历史和当前的实际判断函数。扩展自有/机构/混合三类，共 108 组；课程默认单价故意设为不同值，确保本节学生价格快照不被默认值覆盖，课程默认对象也不被本节编辑改写。与 144 组批量场景合计 252 组通过。此处仍是具名函数和草稿 REST 转换检查，不冒充机构/混合课真实窗口或权限验收。
- 最终会话 50980 退出 0：business-parity 全入口、institution-student 原生命周期/选人/费用专项及 Node ABI 检查通过。原生命周期使用临时 SQLite，只证明历史行为基准；不能证明云端机构自动建学生、重命名和删除联动已完成。下一步须验证这些云端事务及原窗口操作，本轮不改生产规则或界面，不发布。

### 2026-09-08 机构联动前置：普通学生与费用专用学生分离（UTF-8）

- 真实缓存方法及草稿适配器的无网络诊断已确认：一次 createInstitution 生成 institution.create.v1 与 student.create.v1 两条独立命令；专用学生的本地标记未作为学生 REST 参数传递。进一步核对 SQL，旧 vnext_create_student_record_v1 将所有 source_type=2 的学生标成 legacy_is_institution_student=true。这不是依据字段缺失猜测的“标记丢失”，而是普通机构学生也被误分类；直接补机构改名联动有误改普通学生的风险。
- 新 PostgreSQL 回归先在原创建函数复现 true !== false（真实断言失败，前置测试的时间戳格式/联系人表与角色依赖问题修正后才计为业务复现）。新增迁移 20260908-student-source-classification.sql 仅将普通学生创建时的专用标记固定为 false；参数、返回值、来源字段、联系人写入事务、SECURITY DEFINER/search_path 和已有权限不变。迁移前后历史学生全列比较一致，不批量重分类已有云记录。
- 最终专项退出 0：自有/机构普通学生都保持普通标记；联系人正常保存，非法联系人整条学生创建回滚，显式事务回滚无记录；原专用学生不改写，函数 ACL/owner/security 属性不变；实际教师作用域创建仍登记 created_by_teacher_id，访客与 verifier 拒绝。已加入云服务 posttest；完整 npm --prefix cloud-business-api test 会话 50758 退出 0。部署脚本 46 项、252 组原业务对照和 Node ABI 137 检查亦通过。
- PostgreSQL 技能用于保持最小权限与原子事务边界。本轮仅完成机构联动的分类前置，不宣称机构创建/改名闭环完成：云端机构函数仍未原子创建专用学生，旧桌面仍生成两条草稿，机构写入仍限 super_admin，读取投影的专用标记和历史误分类还需核验。新迁移不得单独部署放行；需要与后续原子联动、旧草稿处理及原窗口实操一起通过门禁。没有改学生/机构窗口、没有写生产数据库或更新任何端版本；用户原有 NAS/版本改动和 output 保留。

### 2026-09-08 机构与专用学生：云端事务及确认接口链路（UTF-8）

- 新 PostgreSQL 用例首先在现有机构函数复现：机构创建返回成功，但关联学生数为 0。新迁移 20260908-z-institution-billing-student.sql 保留机构 create/update REST 参数和 SQL 函数接口，通过已获授权的机构写入事务维护专用学生；不是额外提交学生命令。新记录使用稳定的 institution-student-<机构ID>，余额初始化为 0，身份冲突拒绝且不覆盖已有学生。
- 新 institution_billing_students 表明确保存机构与专用学生的归属，禁止 PUBLIC、业务 writer/verifier 直接读写。先锁机构记录，再锁归属及学生；已有归属在备注修改后仍保持。无归属的历史机构只在明确确认改名时识别唯一“专用标记＋原专用备注”的候选；普通来源或误设标记本身不作为归属证据，多候选/不明确记录拒绝，不任选一条。安装迁移不回填或重写历史数据，owner 导入/恢复不触发自动创建。
- 隔离 PostgreSQL 验证：新机构原子创建、改名、旧 managed ID 保持、普通学生全列不变、过期版本零修改、同版本并发仅一条成功、学生写入故障与归属写入故障整笔回滚、显式回滚、重复历史候选拒绝、生成 ID 碰撞不覆盖、非法角色与直接归属表写入拒绝。历史夹具 initially 使用带微秒的 now()、测试传入毫秒版本导致改名未执行；已改夹具精度并断言实际更新返回一条，不把该失败归咎于业务。
- 实际 localhost HTTP 链路复用 outbox、确认客户端、业务草稿适配器、身份客户端、Express 路由、正式 mutation service 与 writer PostgreSQL：未确认零请求，确认新增一个请求同时创建机构/学生，确认改名再一个请求同时更新两者；两草稿 completed。访客/学生请求拒绝且无记录，令牌不进入 outbox。会话使用测试身份验证器、内存测试 codec，不代表真实登录、加密验收、原窗口实操或生产部署。
- 关联歧义/失效原先返回 503，路由回归先失败，现精确映射成 409 冲突，避免误当离线重试；未扩大机构权限，仍受既有 super_admin 限制。专项与 46 项部署脚本测试退出 0；完整云服务测试结果另记。
- 本轮是云端部分，不放行整体机构业务：桌面 createInstitution/updateInstitution 仍有旧独立学生草稿，启动派生缓存、读取投影的专用标记/归属、旧待确认草稿及其课程引用、机构删除与专用学生引用、教师权限、完整云副本和原窗口实操必须继续处理。新迁移不得单独部署，不能因为 HTTP 用例通过就宣布原窗口一次确认已完成。未改变任何业务窗口，未更新云端/NAS/小程序/OSS版本或写生产业务数据。

- UTF-8 收尾：完整云服务测试会话 71133 退出 0（含 pretest/test/posttest 与新机构用例）。随后将新机构专项的夹具扩展为同时加载联系人、手机号约束、学校登记、教师学生作用域与本轮分类迁移，再次退出 0；这仍是隔离 PostgreSQL，不是完整生产副本迁移。252 组原业务对照和 root/backend Node ABI 137 检查通过，版本文件 SHA256 仍为 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。PostgreSQL 技能用于归属表最小权限和父记录先行的锁顺序；全量根测试旧未通过记录不由云服务测试替代。

### 2026-09-08 桌面机构单草稿与云端归属读回（UTF-8）

- 实际缓存方法测试先复现 institutions＋students 两条草稿。现 createInstitution/updateInstitution 只记录机构命令，实删 ensureInstitutionStudents 及启动时自动补学生调用；新 helper 仅覆盖该机构的本地派生学生，ID 与云端稳定规则一致。待确认投影重载也复用该覆盖，不发请求、不改原草稿、不覆盖其他学生。明确云端归属优先于可变备注；缺失已关联学生、歧义或 ID 冲突拒绝猜测，既有学生 observed updated_at 不被本地改名伪造。
- 云端两个读取投影原本缺失专用标记/明确归属，新增 source 回归先失败后通过。现读回 is_institution_student 与 billing_student_id；新增 20260908-zz-institution-billing-projection.sql 仅授予现有只读角色归属表 SELECT。隔离 PostgreSQL 实际执行当前桌面学生/机构投影片段，验证所返回的专用学生 ID/标记一致，并验证该读角色 UPDATE 仍被拒绝。不是通过假响应证明读回。
- 新缓存用例进入 browserDatabaseSyncCapture 门禁；缓存单草稿/重载/歧义/普通学生保护、business-parity、uiRegression 通过。类型检查会话 70274 退出 0；npx craco build 会话 12098 退出 0，主包 main.4fea04c5.js，原日历 450.eed1888f.chunk.js 与 CSS main.5792781d.css 未变。原机构窗口未修改，没有递增版本、打包安装程序或发布。
- 仍未放行：旧版机构伴随的独立学生草稿及课程引用需要安全对账，不能直接重放造成重复学生，也不能静默删除/重写用户草稿；本轮未改旧草稿提交器或宣称该兼容门禁完成。机构删除、教师权限、完整云副本和原窗口一次确认实操继续待验。当前构建不部署到生产，完整云服务测试结果另记。

- UTF-8 收尾：完整云服务测试会话 25911 退出 0（含新只读投影实查）；root/backend Node ABI 137 校验通过。用户版本文件 SHA256 保持 `bd068aa29ebce184ddfe3383c17987bec984c3c71a33a409ef9aeb4643cfc173`，NAS 用户改动及 output 未动。全量根测试、真实原窗口与旧草稿对账仍不由这些结果代替。

## 不可变边界

用户要求迁移，不是重做。原有排课、调课、复制、批量排课、课程设置、学生添加、出勤、学费和教师课时费的业务含义、字段、联动和操作流程必须保留。

允许改变的是数据权威、账号与权限执行位置、多设备冲突处理、离线草稿确认提交，以及用户另行明确要求的账户角色和界面调整。不得为迁就云接口而删字段、取消联动、增加绕路步骤或另造简化业务窗口。

接口成功、源码按钮存在、类型检查通过均不等于业务行为一致。必须从原窗口实际操作，再验证云端记录、重新打开后的状态、关联数据及金额。

## 已确认差异和修复顺序

| 编号 | 范围 | 当前代码证据 | 状态与下一步 |
|---|---|---|---|
| BP-01 | 排课列表另造编辑窗 | `ScheduleList.tsx`；323862ef 新增 Modal、Form 和独立 updateCloudSchedule 路径，原列表只查询/筛选/导出 | 本轮删除额外窗口、按钮、状态、提交函数和专用权限 helper；保留完整云投影读取。原课程表窗口未替换。新增 AST 回归先失败后通过；尚未新构建实测 |
| BP-02 | 课程内输入新地址 | 已恢复原表单内创建地址和课程的草稿关联；确认窗口展示关联地址，并按已确认快照先提交地址、再提交课程 | 部分恢复；本地原子保存、依赖确认与失败重试测试通过。真实教师云端闭环和重名冲突恢复未完成，不关闭 |
| BP-03 | 课程设置联动排课地址 | 在线和草稿分支都已调用原 `syncSchedulesRoomName`，保持全可见课程的原遍历范围 | 已恢复派生缓存及待确认草稿生成；关联排课仍需确认，未验证云端完整等效联动，不关闭 |
| BP-04 | 排课计费快照字段 | 已补齐四字段的草稿、客户端、REST、SQL 调用、数据库列与完整读取投影，新增隔离 PostgreSQL 往返测试 | 代码与隔离数据库验证通过；新增迁移未部署，旧数据缺失快照还需从原始来源验证和恢复，原桌面跨端实测未完成，不关闭 |
| BP-05 | 教师课程维护权限 | 教师本人课程增删改、地址创建的服务与 SQL 已提交；迁移 `20260907-teacher-course-write-scope.sql` 尚未部署 | 部分实现；云端专项测试通过不等于生产已可用。新学生首次归属及教师地址维护仍待补齐，不关闭 |
| BP-06 | 学生添加/修改/删除 | `StudentList.tsx` 原表单仍在，但写入、联系人、引用删除约束已改变 | 未完成逐行为核验。新增三组手机号/微信属于用户明确要求；其余字段、学校录入、机构归属、费用余额、删除及关联行为必须对照，不得统称“已保留” |
| BP-07 | 原课程表全部动作 | `ScheduleCalendar.tsx` 的 handleSave、handleDragSchedule、handleResizeSchedule、handleSaveStudentEdit、handleDeleteSchedule、handleRefreshCourseInfo 仍存在 | 源码存在不代表实际完成。需要原窗口逐项验证新建/修改/调课/复制/拉伸/批量/删除/撤销重做/出勤与费用；含刷新与重开后的持久化 |

BP-02～BP-07 未关闭前，不得以 BP-01 测试通过宣称业务迁移保真完成。

## 本轮按旧版逐项对照（2026-09-07，尚未恢复完成）

本轮先停止提交接口修复支线，回到用户指定的原版业务核查。新增只读 `scripts/audit-business-source-parity.js`，已执行，逐项比较 26 个具名处理函数/组件的去注释语法树，保留旧版及当前行号、摘要。它只定位源码变化，不是界面验收或业务通过证明。

对照提交为 `8118419fe1110ce55f68af114760d41631fdfae7`（2026-07-12）。课程表在此后至 2026-09-05 无该文件提交；仍不能把这个对照点冒称为用户确认的全部原版基线。D 盘所指目录顶层是应用数据目录，本轮未重复盘点其业务数据，也没有把它当作可直接运行的旧源码。

| 对照项 | 旧版行为/内容 | 当前行为/内容 | 判定及证据 |
|---|---|---|---|
| 左侧待排课程范围 | `active === true`，按所选教师筛选 | 同一过滤表达式 | `Sidebar` 整个组件去注释摘要相同；云端返回哪些课程、结课状态及字段仍须同数据实测，不判定显示一致 |
| 左侧卡片第一行 | 优先 display_name，否则 name 去掉年份学期前缀 | 同一函数和表达式 | `getCourseDisplayName`、`stripCourseSystemPrefix` 摘要相同；测试课程名不同不能证明或否定真实业务卡片差异 |
| 左侧卡片第二行 | 年份、学期；缺少独立字段时从 name 提取 | 同上 | `Sidebar` 摘要相同，仍需核对实际投影字段 |
| 左侧卡片第三行 | 地址 + 一对一/一对二/班课 | 同上 | `Sidebar` 摘要相同，不将其当作截图对照通过 |
| 课表课程框内容 | 第一行课程名，第二行地址和起止时间 | 同样的字段排列；时间由字符串截取变为日期格式化 | `git diff 8118419f HEAD -- src/pages/ScheduleCalendar.tsx`；尚未用相同记录、尺寸做新旧渲染对照 |
| 课表加载后的地址 | 课程 room_name 非空时，将排课显示所用 room 更新为课程最新地址 | 301d6ad2 删除此处理，现有 resolver 优先排课 room | **确认变化，未恢复**。不能为避免误生成草稿而改变原显示语义；恢复时需把显示派生与业务提交分开验证 |
| 课程内新增地址 | 保存课程时直接 addOrUpdateRoom | 提示先去地址页并终止保存 | **确认回归，未恢复**，BP-02 |
| 保存课程的地址联动 | 无论新建/修改均调用 syncSchedulesRoomName；原函数遍历全部课程对应排课，不只是本次课程 | 只有本地草稿分支调用；在线分支刷新投影 | **确认调用差异，未恢复**。必须按原联动范围核验，不能擅自缩小成仅修改当前课程，BP-03 |
| 编辑课程缺失年份时 | 使用该课程原 year，否则才使用当前年份 | 该兜底被删除，courseCloudPayload 直接 Number(value.year) | **新增确认回归，未恢复**。旧 handleSubmit 的年份保护与当前差异已查 |
| 刷新课程信息截止日 | 从排课开始时间取日期后比较所选起止日期 | 用完整开始时刻比较，截止日午夜后的课程可能被排除 | **新增确认回归，未恢复**。normalizeRefreshDateRange 不把结束日扩为全天；不能将其当作必要的时区迁移改变 |
| 学生内录入学校 | 保存学生前 addOrUpdateSchool | 页面此调用被删除；已查学生 SQL 仅写 school_legacy | **新增关联行为核查项，未完成**。仍须检查服务层/其他 SQL 是否有等效学校登记，不能仅凭页面调用删除就声称整个链路缺失 |
| 出勤窗口及费用计算 | 原表单打开/保存函数、financialDetails 计算 | 这两个函数及 financialDetails.ts 与对照点一致；云读字段名不同 | **页面未改不等于业务保留**。云字段映射修复仍在工作区；真实提交测试失败，不得关闭 BP-07 |
| 教师、地址维护 | 原新增/修改/删除与同名校验 | 编辑回填函数相同；保存和删除调用路径已换 | **待逐行为核查**，不能以表单仍在作为保真结论 |

本轮未修改上述业务页面。时间格式修复与出勤字段修复保留为未发布工作区改动，不算完成原版恢复。下一步先完成这张旧版行为清单及同数据卡片对照，再按清单恢复，而不是继续横向修接口并跳过业务核查。

### 用户补充后确认的卡片编码来源

用户明确原卡片只有课程名、时间、地址，所见异常为“编码＋物理＋地址＋时间”。本轮读取已留存真实投影 `desktop-course-real-projection.json` 的 courses 项确认：测试课程的 `name` 与 `display_name` 都是 `E2E-20260905-物理课程`，`room_name` 为 `E2E-20260905-桌面验收教室`。因此这串编码来自测试数据命名，不是卡片新增“课程编号”或“科目”字段。此前未先核实这条数据便扩大到显示结构变化，判断顺序有误。

不要为去除测试编码而改写课程名称显示规则、截断用户课程名称或增加隐藏字段过滤。后续测试用可读课程名，追踪标记留在内部 ID/测试记录；现存测试记录尚未改名。该结论只解释用户这次看到的编码，不撤销已独立确认的其他业务回归。

### 本轮恢复及证据边界

- 地址显示：`resolveCalendarRoomDisplay` 恢复旧课程表加载后优先展示课程最新地址的语义，用于卡片、拖拽提示和复制；不修改其他模块的排课地址快照解释。仅派生显示，不改写源记录。新用例先失败后通过。
- 同数据渲染：`scripts/audit-calendar-card-render.js` 提取真实旧版/当前 Sidebar、卡片 JSX 和加载函数，用相同合成数据对照。先复现旧版新地址/当前旧地址差异；修复后左右卡片渲染输出一致，源记录地址未重写。这是局部渲染证据，未做新旧整页截图验收。
- 课程年份：原 handleSubmit 年份保护已恢复；执行原表单提交函数的用例覆盖验证结果缺失、null、明确新年份，先失败后通过。未做生产课程保存验收。
- 截止日期：刷新课程信息恢复按本地日判断；用例覆盖首日、末日午夜/白天/23:55、前后一天，先失败后通过。
- `scheduleDraftComparison`、课程表源检查、地址解析、日期范围、课程表单测试通过；`tsc --noEmit` 最终通过。第一次类型检查指出两个拖拽提示仍引用旧 helper，已补齐并重新检查。`uiRegression`、`test:cloud-schedule` 和完整 `npm test`（会话 34608，退出 0，最终 deploy env checks passed）通过。尚未构建或发布本轮恢复。

以上更新优先于前表中的“未恢复”状态；关联地址云端保存、全范围地址联动、计费快照、权限、学生学校登记等仍未恢复完成。

### 课程内新增地址：下一完整恢复闭环（UTF-8）

已沿原 handleSubmit、addOrUpdateRoom、createCourse/updateCourse 追查。当前页面提前 return 阻止旧流程；浏览器草稿库自身仍可创建地址，但它生成新 ID，原输入的地址文字不会自动变成课程引用的这个 ID。只删除提示会留下错误关联，不能作为修复。

恢复门禁须覆盖：原课程窗口直接录入地址；地址与课程引用同一 ID；在线保存与离线确认后的结果等效；教师在本人业务范围内能实际保存（当前 rooms POST 和 courses POST/PUT 均仍要求 super_admin）；离线地址先于引用它的课程提交且均经用户确认；部分失败、重试、同名地址不制造重复或悬空引用。现有 outbox 按单条确认，已检查的提交入口无此关联编排，尚未新增实现。

下一步测试和实现以这个完整旧流程为单位；管理员专用成功或单独地址创建成功不作为闭环完成。

### 教师课程与地址创建权限：后端部分进展（UTF-8）

- 课程新增、修改、删除以及地址新增不再仅允许超级管理员；身份范围由桌面会话推导，不信任请求正文中的角色。教师仅能操作本人课程，学生关联沿用既有教师可读范围。小程序仍不能写核心教务。
- 新迁移 `20260907-teacher-course-write-scope.sql` 通过受限函数执行，未授予业务表直接写权限；教师绑定、课程归属、学生关联在事务内加锁验证。实际 PostgreSQL 测试覆盖跨教师、跨租户、失效教师、过期版本、直接写表拒绝和并发修改阻断。
- 实际数据库测试发现原课程软删除函数存在输出变量 `id` 与查询列歧义；本次限定列所属表，保留原引用检查与版本条件。
- 完整 `npm --prefix cloud-business-api test` 会话 1316 已确认退出 0，含教师排课及教师课程 PostgreSQL 测试；最新教师档案绑定和超级管理员路由用例单独再次通过。
- **未部署，未关闭 BP-02/BP-05。** 课程窗口新增地址、离线依赖确认、全范围地址同步仍未接回；新教师首次新增学生的归属链路、教师地址修改/删除也未完成，不能称为完整教师业务恢复。

### 课程地址草稿关联：缓存部分进展（UTF-8）

- `createCourse` / `updateCourse` 现在将已有地址名称解析为真实 ID；新地址与课程引用同一生成 ID。已有地址复用不增加地址更新草稿、不改其版本或计数。只修改课程其他字段不会顺带改写地址。
- 同次课程保存新增的地址与课程使用既有 `appendDraftBatchSync` 一次持久化，之后才保存派生缓存。没有调用网络提交，也没有自动确认。
- `courseRoomDraft.test.js` 执行源码中的实际方法，先复现地址文字错误地充当 ID，再验证新增/修改、已有地址按 ID/名称复用、仅改地址名称、旧名称与有效 ID 冲突、缺失课程以及持久化异常后双记录回滚。异常用例使用实际批量记录方法和检查点实现，存储桥接为故障注入，**不是生产端到端证据**。
- 新测试、草稿捕获、数据库安全、outbox、桌面云提交客户端测试通过；最终 `tsc --noEmit` 会话 8123 退出 0。顺带将原来漏接默认测试链的浏览器数据库安全测试接入。
- **BP-02 仍未完成。** 地址输入框预先创建的草稿、课程保存入口、依赖草稿确认和联网提交次序尚未接回。本次原子批量写入只证明本地保存边界，不代表云端两条 REST 写入是原子事务；生产业务数据、页面布局和发布均未变更。

### 课程内新增地址：窗口与确认链路已接入，待真实验收（UTF-8）

- 原课程表单不再要求跳转地址页。输入地址名称时重新读取地址缓存，创建或复用地址后将其真实 ID 写入课程；保持原字段和布局。实际 `handleSubmit` 测试覆盖新地址、已有名称及年份兜底。
- 保存课程发现关联地址创建草稿尚未完成时，保留课程草稿，不绕过地址确认直接发课程 REST。实际 `submitCourseToAuthority` 测试覆盖新建/编辑、待确认地址/已存在地址四种组合。
- 待提交更改的原确认弹窗显示“一并新增地址”；点击确认时传递用户刚查看的课程、地址内容快照。主进程重新核对依赖，批量确认时再次检查当前内容；内容变化、额外草稿、地址冲突均不能扩大原确认范围。
- 提交器仅在明确确认后按地址、课程顺序调用既有 REST；地址失败不发送课程，重试沿用已保存命令，不自动确认其他草稿。只处理本轮课程→新增地址依赖，不宣称所有业务依赖已完备。
- 实际弹窗 JSX 渲染及事件测试通过，证明显示两项且打开弹窗不发请求；真实 runtime + 本地加密存储 + REST 客户端的受控请求测试通过，证明地址先于课程且二者最终完成。HTTP 响应为测试替身，**不等于已在生产云端或安装后窗口验收**。
- 客户端依赖测试、outbox 测试、课程原函数测试、弹窗测试、runtime 测试、`uiRegression`、`test:cloud-schedule` 通过；最终类型检查会话 84659 退出 0（之前两个隐式类型错误已修正）。
- 完整 `npm test` 两次未通过：会话 20617 在角色申请原子档案 PostgreSQL 检查前后退出 1，会话 97870 在题库分类 PostgreSQL 检查前后退出 1，均未输出具体错误；对应单项分别复跑通过，原因未定位。不得用单项结果宣称完整测试通过。
- **BP-02 尚不关闭**：生产真实操作、同名地址冲突恢复、在线与离线最终结果对照待验；课程保存后的全范围排课地址联动仍是 BP-03 未完成项。未部署、未上传 OSS。
- 页面生产构建 `npx craco build` 会话 92700 退出 0，主包 `main.5e346d82.js`；这是源码页面构建，不是安装包验证。新模块已补入桌面打包文件清单，相应缺失断言先失败后通过；`test:desktop-build-flavor` 通过。用户版本文件 SHA256 仍为 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`。本次没有切换 ABI、递增版本或发布。

### 保存课程后的排课地址联动：已恢复草稿生成（UTF-8）

- 在线新建和修改课程成功、刷新投影后，重新调用原 `syncSchedulesRoomName`；离线分支原调用保留。没有将范围缩成刚保存的课程，也没有加排课日期、状态或结课过滤。
- 新增 `CourseList.room-sync.test.js` 执行真实保存函数与原联动函数：先复现在线只保存/刷新、未捕获地址变化，再验证新建/编辑均产生联动。涵盖其他课程、已结课课程、空地址、找不到课程的排课；除地址外，出勤、金额、备注、时间和观察到的版本均不改写，源对象不提前突变。
- 这一步恢复的是原联动结果在派生缓存和待确认草稿中的形成；**不是已同步全部云端排课**。关联排课仍需逐条确认，完整联动的真实云端验收尚未完成，BP-03 不关闭。
- 专项、草稿捕获和最终类型检查会话 60746 通过；页面构建会话 81261 退出 0，主包 `main.28b6ad49.js`。
- 完整测试会话 81024 已跑过云端全部测试和教师权限 PostgreSQL 测试，随后在 `SyncSettingsAuthorization.test.js` 失败：仍断言旧两参数确认调用。本次更新为包含内容快照的调用检查，并补快照生成断言；`test:identity-device-center` 与 `uiRegression` 重跑通过。此次有明确原因，不用于解释前两次无错误输出的退出。

### 隔离桌面启动实测发现的双目录原生依赖缺口（UTF-8）

- `desktop-attendance-parity-9OVEUC` 通过真实配对确认请求后未进入主界面，停回密码登录页；尚未触及测试排课，不能算出勤通过。
- 该隔离目录的 `profile/logs/electron-main.log` 明确记录缓存服务启动失败：实际加载 `backend/node_modules/better-sqlite3`，模块 ABI 137，而 Electron 要求 119。根目录与 backend 下是两份独立依赖，旧准备脚本只处理根目录。
- 原生模块准备和恢复脚本已扩展到 backend；验证器也分别从根目录和 backend 的模块解析入口打开内存数据库，不能再用根目录成功掩盖后端失败。测试先复现旧验证器漏检，再验证故障注入可拦截；修改前两份 Node ABI 137 实际检查通过。完整 Electron 重跑结果另记，不提前宣称登录修复。

### 失败实测记录（不计通过，UTF-8）

隔离测试 `desktop-attendance-parity-x9K1NC` 在原“学生出勤和费用”窗口将正常出勤改为请假，本地草稿形成、联网未静默提交；人工确认后状态为 conflict。读取该隔离目录加密草稿得到具体拒绝 `CLOUD_BUSINESS_INPUT_INVALID`，不是已证实的并发版本冲突。提交基线为 `2026-09-07T03:29:18.983+00:00`，云接口校验要求规范 ISO Z 格式。新 REST 往返回归先复现同样拒绝，传输格式修复后通过；尚未重新验证生产真实出勤操作。该记录不得写成真实操作通过、已恢复出勤、已完成发布。

## 当前证据边界

### 2026-09-07 排课计费快照链路（UTF-8，未部署）

- 原 `financialDetails.ts`、排课/调课/出勤/刷新表单均未改写。`billing_unit`、`teacher_fee_mode`、`teacher_id`、`teacher_name` 现在进入草稿白名单与命令，使用现有 REST 路径的四个附加字段传递；请求不能只带半组快照，非法值在进入 writer 前拒绝。
- 新迁移 `20260907-zz-schedule-financial-snapshot.sql` 增加可空列，并更新既有 scoped create/update 数据库函数；仍先执行原教师课程/学生范围检查。快照与排课、学生费用在同一事务提交，版本冲突不写快照，非法快照导致整条写入回滚，writer 不能直接更新表或调用内部快照函数。
- 不把“历史教师”当成“当前操作权限”的替代物：课程换教师后，新课程负责人复制旧排课可以保留原教师快照，旧负责人不能借快照继续写该课程。引用的教师必须属于同一租户，可保留已经归档的教师信息。
- 迁移不会用今天的课程设置填充历史空值。旧客户端不携带快照时保留已有值，不清空；明确携带快照的原“刷新课程信息”流程才更新。**这不代表历史数据快照已经恢复**：已迁移数据原先缺失的字段，需要继续从原始来源作可恢复影子核验，不能推测填充。
- 完整业务投影返回四字段；学生侧不返回教师计费方式和课时费，学生个人金额计算优先采用排课计费单位。小程序投影缓存原本保留这些字段，不改其页面布局。
- `desktopScheduleSnapshotContract.test.js` 执行真实草稿→客户端→本地真实 REST→服务 SQL 参数链路；`schedule-financial-snapshot.postgres.test.js` 使用隔离 PostgreSQL 验证落库、旧请求保持、旧课程改价不覆盖、明确刷新、版本冲突、跨租户拒绝、原子回滚、教师更换后复制，以及执行应用源码中的真实投影 SQL。`student-schedule-tuition.postgres.test.js` 增加历史排课按次、当前课程按小时的金额对照。均已通过，不能冒称真实生产部署验收。
- 确认窗口增加可读的计费单位、教师计费方式及教师姓名，便于确认仅快照变化的草稿；不显示内部编号、不改变原课程框。
- 云端完整测试会话 45196 退出 0；页面构建会话 43329 退出 0，主包 `main.50e554f3.js`；类型检查会话 11423 退出 0。桌面草稿、字段适配、差异比较、费用及小程序投影专项均通过。
- 项目完整 `npm test` 会话 68620 已执行到最终 `deploy env checks passed`，退出 0，包含本轮新增快照专项。这是当前整套通过证据；不据此反推前几次异常退出已经找到根因，也不把测试里的模拟上传输出当作真实发布。
- 兼容顺序：先备份云数据库，再应用新迁移和云服务，核验后才能发布携带新字段的桌面。新桌面不能直接配旧云接口；当前不递增版本、不上传 OSS、不更新 NAS。统一发布与 BP-04 门禁仍未完成。

### 2026-09-07 本轮真实出勤闭环（UTF-8）

- 新页面构建会话 69177 退出 0，主包 `main.83658e2f.js`；类型检查会话 4512 退出 0。没有生成安装包、上传 OSS 或部署云服务。
- 前次 `desktop-attendance-parity-niANAK` 已将隔离排课恢复正常并验证云端金额，但重载后回到登录页，因此整条测试失败。定位为 `DesktopIdentityGate` 初始化只恢复 sealed，遗漏主进程仍 unlocked、页面会话已丢失的情况；新测试执行真实初始化分支，先在 unlocked 场景失败，修复后 sealed/unlocked 的成功与云端拒绝分支均通过。恢复仍要求云端签发会话，不凭本地已解锁状态直接进入业务。
- 实测会话 47968 退出 0：源码 Electron 在隔离本地目录启动，以既有测试教师登录配对，连接真实 `physicsedu.xyz` 云服务，在原“学生出勤和费用”窗口执行正常→请假→取消→正常。
- 每一步均验证只生成 awaiting_confirmation 草稿、联网不提交、用户点击确认后完成云写入；请假和取消的学费/课时费合计均为 0，恢复正常后分别为 270/180。每次重载后重新打开原窗口核验对应出勤状态；最后测试排课 `d664f976-4ae4-4bcd-8dc8-bb9cfb5a17a1` 已恢复正常。没有改角色授权或真实学生档案。
- 证据目录：`C:/Users/83423/AppData/Local/Temp/gewu-question-pagination-20260906/desktop-attendance-parity-IGzqnj`。`receipt.json` SHA256 为 `AFAF33AA5820E94FAB0EBFB4ED1E43059D76EC177F7289D204CC2F8D4971D3AF`；`reopened-4.png`、`reopened-3.png`、`reopened-1.png` 已实际查看，状态、原单价 180/120 和原窗口控件均可见。课程和学生名称仍含测试标记，不能用这些截图宣称命名与全部页面视觉验收完成。
- 同一实测同时验证排课列表没有后来另造的“编辑云端排课”按钮。仅这一读取检查不等于列表筛选、导出全部验收。
- Electron 根目录和 backend 两份模块均实际校验为 ABI 119；实测结束后已恢复并校验 Node 两份 ABI 137。此前漏检 backend 的原生依赖问题已纳入准备、恢复和验证脚本。
- 此证据限于一个既有测试教师、一节 1.5 小时课程、一个学生的出勤完整循环。不是微信手机扫码登录验收、安装包验收、全部排课动作、多教师/机构/多人计费、所有角色或统一发布完成；BP-04、BP-07 仍不关闭。
- 本轮完整 `npm test` 仍未通过：会话 82413 在角色申请原子档案 PostgreSQL 单项前后异常终止，npm 记录退出码 `-1073740791`；该单项独立重跑退出 0。随后完整重跑在另一处（跨安装锁测试前后）退出 1，该单项独立运行也通过。20 次最小 `node -e 'process.exit(0)'` 未复现异常；尚无证据确定根因，不将随机重跑成功或当前专项通过冒称整套通过。
- 本轮已重新通过：`DesktopIdentityGate.test.js`（含真实初始化分支测试）、`desktopIdentityClient.test.js`（含真实本地 REST 版本往返）、`authorityProjectionCacheAdapter.test.js`、`financialDetails.test.js`、`test:cloud-schedule`、`SyncSettingsAuthorization.test.js`、`desktopAuthorityClient.test.js`、`desktopAuthorityRuntime.test.js`、原生模块验证器测试。用户版本文件 SHA256 保持 `BD068AA29EBCE184DDFE3383C17987BEC984C3C71A33A409EF9AEB4643CFC173`。

- 8.9.8 构建会话 34379 成功，Electron ABI 119 检查通过，Node 原生依赖恢复并通过数据库测试；这是本轮删除窗口**之前**的构建，不能证明当前源码的渲染结果。
- 旧构建真实教师测试 `desktop-teacher-schedule-QqtSFB`：原课程表新建一节课只产生一个 awaiting_confirmation 草稿；重新联网不提交；人工确认后云端创建 d664f976-4ae4-4bcd-8dc8-bb9cfb5a17a1。其后的简化编辑窗保存失败，整条测试未通过。
- 8.9.8 没有上传 OSS。此前 release matrix 的构建/版本回执不构成本轮业务保真验收；发布暂停。
- 本轮仅删除后来新增的简化编辑实现及其独有 helper，历史提交可恢复；未回退整个仓库、未改用户原有三个工作区文件、未修改或删除生产业务数据。
- 无子 agent。

## 回到原工作流程的条件

### 2026-09-07 历史计费快照来源核对（UTF-8，只读，尚未恢复）

- 原 D 盘 SQLite 排课表没有 `billing_unit`、`teacher_fee_mode`、`teacher_id`、`teacher_name` 四列；原 Electron 的 `schedules` 本地存储键却有 598 条记录，其中 318 条明确保存四字段。不能从 SQLite 或当前课程价格推测历史快照。
- 只复制原 Local Storage 到新建临时目录，复制前后和副本逐文件校验和一致。隔离隐藏 Electron 只读副本排课键，禁网络、不加载业务应用或数据库原生模块。SQLite 只读事务读取排课及课程名单，主库/WAL/SHM 前后校验和一致，原文件未修改。
- 通过生产 HTTPS 完整管理员业务投影读取当前云端 576 条排课；未调用业务写入接口。既有短期会话令牌不进入证据文件，远端临时会话辅助文件已清理。
- 新增 `scheduleSnapshotRecoveryPlan.js` 和 `planCapturedScheduleSnapshotRecovery.js`，只生成候选，没有 apply/SQL/网络写入入口。核对租户、唯一编号、课程、时间、出勤、名单、单价、总费用及源/云版本；四字段只能来自明确缓存。版本保留微秒精度，不覆盖不同快照，相同快照标记已恢复。原数据库未存排课学生覆盖时，按既有迁移规则使用原数据库课程名单，仍须与缓存和云端一致。
- 实际结果：**158 条候选；93 条缓存与原数据库业务不同；67 条缓存记录在原数据库中不存在；280 条无明确快照**。93 条包含最初 13 条时间不同，以及进一步检查的 80 条继承课程名单/费用后仍不一致；不能把这 80 条归为纯格式差异。不一致和缺失记录保留待核查，不插入、不覆盖、不推断废弃。
- 临时证据目录 `gewu-schedule-cache-copy-Rq3J0C`；`snapshot-recovery-proposal-r4.json` SHA256：`242ace11886361fe8d6b070b9e9660e31861ded7c471b77258ca8ec757fab44c`。云读取证据 SHA256：`b9a88204796d9e4fbcddc768a06f74bbf34556f336d61d25700824b2bcc4e365`。不提交用户业务明细或会话令牌。
- 新检查先失败后通过，覆盖来源变更拒绝、输出不可覆盖、名单继承、空值/重复/跨租户、非法时间、精确版本冲突、快照冲突和无副作用；已接入迁移测试入口。完整 `npm run test:vnext-migration` 会话 45036 退出 0。
- **BP-04 仍未关闭**：生产投影尚无快照字段，158 条只是候选，不是已恢复。下一步在隔离数据库验证仅四字段恢复、事务冲突/重跑/回滚，再按云备份和兼容部署门禁推进；93+67 条继续追溯。本轮没有发布任何端，没有更改原业务窗口。

以下原流程门禁保持不变：

### 2026-09-07 恢复事务与 158 条候选隔离重放（UTF-8）

- `scheduleSnapshotRecoveryTransaction.js` 提供维护用事务函数，不是桌面/小程序接口，不打开数据库连接、不自行提交事务、不提供生产执行命令。要求已经进入 `vnext_pg17_business_owner` 的 SERIALIZABLE 显式事务，并用连续查询的事务编号检查拒绝自动提交模式；普通 writer 不能执行恢复或回滚。
- 先按编号排序锁定所有目标记录，检查精确版本、同租户教师、未删除状态和四字段仍为空，再只写四字段及新的 `updated_at`。已恢复且相同的快照不重复写，部分/不同快照或版本冲突拒绝整批。不依赖 JavaScript Date 截断数据库微秒版本。
- 恢复返回前后字段与版本回执；提交后回滚仅在记录仍与恢复后的字段和版本一致时允许。回滚生成新版本，不恢复旧版本号，避免旧草稿重新获得提交资格；后续合法修改受到保护。重复回滚不写入。
- PostgreSQL 专项覆盖：恢复第二次 UPDATE 人为失败后整批回滚、微秒级冲突、跨租户教师拒绝、显式回滚、提交后回滚、回滚第二次写入失败后整批撤销、重复恢复/回滚无写入、后续修改保护和普通账号拒绝。已接入原迁移测试入口；完整 `npm run test:vnext-migration` 会话 2027 退出 0。
- 使用前节校验和一致的实际候选文件，向一次性本地 PostgreSQL 重放 **158 条**候选的课程关联、时间、状态、出勤、单价、总费用和精确版本，再恢复四字段。关联教师/学生/课程名称使用占位值，地址/备注使用保留值：**不是生产全库克隆或完整影子导入**。158 条均恢复成功、重复执行零写入、显式回滚还原、提交后可回滚；逐列断言其他排课字段不变，学生出勤与费用表完全不变。
- 本轮最终重放退出 0；证据为临时目录 `gewu-schedule-cache-copy-Rq3J0C/snapshot-recovery-postgres-1788763961024.json`，绑定候选 SHA256 `242ace11886361fe8d6b070b9e9660e31861ded7c471b77258ca8ec757fab44c`。只输出聚合结果，无用户明细和密钥。
- PostgreSQL 最小权限及一致锁顺序技能影响了事务边界和测试设计。没有修改生产数据库或运行任何发布。真实恢复仍须云备份验证、快照迁移/云服务兼容部署、恢复回执持久化以及执行前重新核验来源与当前云版本；93 条差异和 67 条缺失记录仍待追溯，BP-04 不关闭。用户三个原有工作区改动保持不变。

### 2026-09-07 学生学校登记：确认链路与事务边界（UTF-8）

- 对照原 `StudentList.handleSubmit` 和现 `browserDatabase`：原页面曾调用 `addOrUpdateSchool`；迁移后在线路径仅提交学生 `school` 字段，云端没有学校登记。离线路径则先生成并保存独立学校草稿，再生成学生草稿。这不是等效迁移。
- 新迁移 `20260907-student-school-registration.sql` 在获授权的 writer 学生插入/学校字段更新事务中登记缺失学校，既有学生 REST 契约不变。联系方式校验失败、学生版本冲突不会留下孤立学校；按租户和未删除名称唯一索引防止并发重复。已有学校内容、历史 count 不回算，owner 导入/恢复无此写入副作用。若历史存在重复名称，迁移拒绝安装，不擅自合并。
- 桌面离线创建/修改学生现在只记录一条学生确认草稿；学校选项从云端学校加本地学生草稿派生，不提前生成学校 ID、改学校版本或单独提交学校。旧版本已经持久化的学校草稿没有删除或自动确认，需按实际存在的草稿另行验证升级后的提交顺序。
- 两个初始行为测试分别复现“离线两条独立草稿”和“云端学生已保存但学校未登记”，修复后通过。新增真实 localhost HTTP 链路使用原 outbox、业务适配器、身份客户端、Express 路由和 writer PostgreSQL 服务：未确认零请求，确认后一次 REST 同时保存学生和学校，随后修改学校可读回；测试使用隔离数据库、测试身份验证器和内存 outbox，不是生产账号/UI验收或加密验收。
- 此链路另发现新增学生接口将小程序票据的权限拒绝转成 503，客户端误当断网。仅修正该接口的权限错误映射为 403；访客、学生及小程序票据均拒绝且学校无写入。教师创建学生仍受当前旧管理员限制，属于 BP-05/06 待恢复范围，未放宽权限冒充完成。
- 真实云库完整备份副本 `gewu_snapshot_shadow_b29634f7cd1d85b1` 通过正式入口应用包括学校登记在内的四项迁移；69 张表、645 条原始排课的 158 条快照恢复/回滚和故障检查继续通过。临时维护容器、数据库及辅助目录已清理，原备份保留。证据 `cloud-shadow-recovery-b29634f7cd1d85b1.json` SHA256：`f62951bd627eed776dda8743ea51657d1d8d779d8da04a6e4e1d8d6dc8b6b926`。生产库未写入、云服务/NAS未重启。
- 边界：这轮只补“学校目录登记”缺失，不关闭整个 BP-06。原 `addOrUpdateSchool` 在保存时累增 count，而页面称其为“关联学生数”，与实际人数不是一回事；本轮未将历史 count 擅自改成人数，也不能将统计行为称为已经保真。学校管理修改/删除联动、旧草稿升级、教师新增学生归属和原窗口实际操作仍须继续检查。
- PostgreSQL 技能用于限定 writer 副作用、权限和原子性；未改学生/课程窗口布局。此修改尚未发布，原业务与 Word 公式门禁保持。
- 当前验证证据：最终云服务完整测试会话 80859（含新增 HTTP/PostgreSQL 用例）退出 0；根 `npm test` 会话 42317 退出 0（权限错误映射追加前，追加后的云端全部测试已另行重跑）；类型检查及学生页面/草稿回归会话 74013 退出 0；`npx craco build` 会话 61954 退出 0，主包 `main.3d06355b.js`。用户原有 `src/generated/version.ts` SHA256 仍为 `bd068aa29ebce184ddf3383c17987bec984c3c71a33a409ef9aeb4643cfc173`。没有生成或发布 OSS 安装包，也没有更新任何端版本号。

原流程门禁：

### 2026-09-07 真实云库备份与完整副本迁移验证（UTF-8）

- 只读追查旧 `scheduling.before-restore.db` 中 93 条差异及 67 条缺失编号，共 160 个编号均未找到。该检查不证明记录应废弃，也不支持改用缓存整批覆盖；差异继续保留。查询限定这批编号，读取前后数据库文件校验和一致。临时证据 `snapshot-exceptions-pre-restore.json` SHA256：`0cfa2dc07eca82ce8101ad429bb375daec1744c98e45a097203f23121c62a30f`。
- 通过项目现有备份流程创建真实云库备份 `/root/scheduling-backups/postgres/20260907-065915/gewu_cloud.dump`，SHA256：`38d8ff34f060d12182c1b9f71885dd35d09e78729edcf587466976519575b61b`；已完整恢复到新建临时数据库并通过验证。随后独立核验备份校验和正确、验证库不存在。备份与元数据保留，生产业务数据未修改。
- 备份清理流程加固：只有 createdb 成功后才设置本次所有权标记，失败清理不能删除未由本次创建的同名数据库；备份目录使用独占 mkdir，避免同秒并发覆盖。新增测试先失败后通过，真实备份成功验证正常路径。
- 再从这份备份创建完整云端临时副本，保留数据库对象所有权；不是之前的合成关联档案重放。副本包括 69 张业务/控制表、645 条原始排课（含已删除等不进入日常投影的记录；不能与此前 576 条投影数混用）。
- 第一轮通过正式部署迁移入口时，真实暴露 `migration_sql()` 拒绝开头注释的问题：新快照迁移先写了 UTF-8 注释，旧入口却只接受直接 BEGIN。失败临时副本已清理，生产不受影响。补实际快照迁移文件回归后，入口改为识别首条 SQL 前的行/嵌套块注释；保留文件内容和原哈希，仍要求第一条 SQL 为 BEGIN。注释中的角色切换文字不当成真正授权语句。
- 修复后使用新完整副本执行正式迁移入口成功，实际应用 `20260907-teacher-course-write-scope.sql`、`20260907-zz-schedule-financial-snapshot.sql`。再次执行零新增迁移。69 张业务/控制表逐表行数和内容指纹保持一致（排除迁移台账及新增四个可空列），新增快照列全部为空，未推测补值。
- 成功会话 70166 退出 0；完整副本 `gewu_snapshot_shadow_a13dbd42bcec059e` 已清理，备份仍保留。临时证据 `cloud-shadow-migrations-a13dbd42bcec059e.json` SHA256：`3fa1bb94670a7103526c1f2359f5eb55fe7c8289a8237f75e35e87673b0e276c`。公网云服务仍健康，版本仍为 8.11.9、业务权威仍为 cloud。
- 当前验证：备份测试 4 项、迁移入口测试 8 项、部署脚本测试 46 项全部通过。没有部署或发布任何端，没有修改用户三个原有工作区文件。
- 这次完整副本只验证了备份还原和两项迁移，**尚未在完整副本执行 158 条快照恢复**，不能把此前局部 PostgreSQL 重放当作该步骤完成。下一步仍需完整副本恢复、持久回执及其故障验证，然后才能按原业务保真门禁推进生产恢复。

原流程门禁保持不变：

### 2026-09-07 完整云库副本恢复、持久回执与故障回滚（UTF-8）

- 新迁移 `20260907-zzz-schedule-snapshot-recovery-receipts.sql` 增加仅维护角色可读写的回执表，绑定租户、来源计划哈希、备份哈希、恢复前后字段及精确版本。普通 writer 和业务读取角色无该表权限；不增加桌面或小程序写入入口。
- `scheduleSnapshotRecoveryReceipts.js` 将快照恢复和回执插入放在同一 SERIALIZABLE 事务中，提交后重试先读取持久回执并核对当前记录，而不是依赖内存。回滚从已保存回执读取原值，与回执状态更新同事务；来源计划/备份不匹配、后续记录变化、缺失回执或已回滚的旧计划均拒绝，不能覆盖新业务编辑。
- 隔离 PostgreSQL 新增实际回执写入失败及回滚回执失败用例：即使前面的快照 UPDATE 已执行，整批业务数据和回执仍一起撤销。恢复/回滚重试无再次写入；普通 writer 读取回执被拒绝。测试先失败后通过。
- `verify_cloud_schedule_snapshot_recovery.py` 只允许新生成的 `gewu_snapshot_shadow_<随机值>` 副本，复核备份文件哈希和重新生成的候选计划。使用现有云服务镜像启动一次性只读、无额外能力的独立维护容器；管理凭据仅通过标准输入进入维护进程，不写环境文件、不写日志、不交给现有服务进程。现有云服务和 NAS 不重启、不替换。
- 真实云端测试从已验证完整备份还原，应用三项待发布迁移后，在 **645 条原始排课、69 张业务/控制表**的副本中恢复 **158 条**候选，保存 1 条批次回执。重新建立连接后确认已提交结果；注入恢复回执和回滚回执写入故障均整批撤销；随后提交回滚 158 条，再次回滚无写入。除四个快照字段和应更新的版本外，所有业务/控制内容指纹不变；最终回滚后只留下正确的新版本号及维护回执。
- 真实会话 62986 退出 0。临时证据 `cloud-shadow-recovery-c7f1f946222199bc.json` SHA256 为 `69125882bbc80a458e4bcee7b2694c7827502af46cb085d0c052c87fbe3789f1`，绑定此前计划哈希 `242ace11886361fe8d6b070b9e9660e31861ded7c471b77258ca8ec757fab44c` 与备份哈希 `38d8ff34f060d12182c1b9f71885dd35d09e78729edcf587466976519575b61b`。测试维护容器、完整数据库副本和带候选数据的临时辅助目录均已清理，原始备份及本地聚合证据保留。
- 完整迁移测试会话 18838 退出 0；完整云服务测试会话 36975（含 posttest 教师排课/课程）退出 0；生产拒绝与输入脱敏检查、Python 副本回执校验和迁移入口专项通过。公网生产服务仍健康且仍为 8.11.9。
- PostgreSQL 技能影响了角色限制、固定加锁顺序和数据/回执事务边界。**这不是生产快照恢复或统一发布完成**：生产未应用三项迁移，93 条业务差异和 67 条缺失缓存记录仍不放行；原业务行为保真及 Word 公式等剩余门禁不撤销。下一阶段可使用完整隔离副本继续原桌面业务操作和多角色验收，再决定符合门禁的生产推广步骤。

原流程门禁：

1. 从旧版本提取每个模块的操作和联动清单，逐项检查实现与云端持久化，而非重设计窗口。
2. 修复上述差异，验证原版单教师、自营/机构/混合来源、一对一/一对二/班课、按小时/按次、单人/多人定价及出勤组合；旧数据和新增隔离测试数据分开。
3. 在原桌面窗口实际操作，再用小程序教师/学生/家庭成员/访客/超级管理员检查可见范围和有限写入，恢复全部 18 页逐页审计。
4. 继续试卷、讲义真实导入与题库/组卷/导出测试。用户本轮再次明确：Word 公式不能有一部分是图片。必须逐个公式确认可编辑，不能用图片兜底后仍标记验收成功；题目本身的插图不属于公式。
5. 业务行为保真和 Word 公式验收未完成前，不进入最终统一发布。独立审计仍属于总目标，但当前用户禁止子 agent，不自行启动。

### 2026-09-07 教师新增学生的归属、读取和选课范围（UTF-8）

- 本轮起点：学生新增、普通资料修改、含联系人修改、删除四条 REST 入口仅允许超级管理员；教师的学生读取只根据课程/排课关联，新学生尚无课程时没有可持久读取的归属。已先用实际 Express 路由用例复现四条入口全部 403。
- 新增独立迁移 `20260907-z-teacher-student-write-scope.sql`，给学生增加可空的创建教师字段和同租户外键；历史学生不猜测、不回填、不改归属。教师创建学生时，云端从已核验桌面会话取得教师 ID，学生、联系人、学校登记与创建教师归属在同事务内完成。请求正文不接受 actorScope 或创建教师字段；缺少会话范围时服务层拒绝，不能默认提升为管理员。
- 四条学生写入入口均调用带教师范围校验的 SQL；已有普通资料、联系人、版本冲突、引用删除限制保留。学生创建者或原本具有课程/排课关系的教师可在其范围内维护，其他教师、跨租户教师、已删除教师、未绑定教师、学生、访客、家庭成员与小程序票据均不能获得这些写入权限。管理员切换为教师角色时按教师范围处理。
- 教师读取投影增加“本人创建”这一来源；课程选人、排课选人和学生维护共用数据库关系检查。新学生不需要先伪造一门课程才能进入课程选择范围。已有共同授课学生仍按原课程/排课关系可见和可维护，不会因最后一次编辑而改变创建教师。页面字段、排课卡片和原学生窗口未改。
- 隔离 PostgreSQL 实测：教师创建/修改自己的学生、随后建课选入及为另一个新学生安排试听均成功；另一位教师建课冒选或修改/删除被拒绝；旧版本、失效联系方式、已有课程引用均保持原拒绝/回滚；删除自己的未引用学生成功。持有写入事务时，外部删除教师或改写创建教师被锁阻止；跨租户归属外键拒绝。原课程、排课和计费快照测试现均先加载新迁移，再验证原有关系与锁定行为。
- 原学生学校确认测试现使用教师上下文，串联原 outbox、业务适配器、身份客户端、实际 HTTP 路由和 writer PostgreSQL 服务，确认后可读回创建教师；未确认仍零请求。测试身份验证器、内存 outbox 和数据库均为隔离测试资源，不能说成真实生产登录、加密验收或原窗口截图验收。
- 真实阿里云完整备份副本 `gewu_snapshot_shadow_4fb756cfe8587957` 已通过正式迁移入口应用五项待发布迁移，158 条历史快照恢复/回滚及故障检查继续通过（69 表、645 条原始排课）。回执 `cloud-shadow-recovery-4fb756cfe8587957.json` SHA256 为 `080b83b0af6d8491fe6226106df67f941068e3f928d3041af566796cefecc46f`。维护容器、数据库副本和辅助目录已清理；原完整备份保留。生产库、现有云服务及 NAS 未改动。
- PostgreSQL 技能用于最小权限、同事务身份归属和关系证据锁定。部署时必须先应用此迁移再启动新云服务；现有桌面 REST 正文不变，不要求随此项更新桌面或 NAS 版本。当前仍暂停发布，待原桌面窗口与多角色操作验收，不将本项视为 BP-05/BP-06 或整体架构全部完成。
- 验证：教师学生、原课程、原排课和金融快照专项均通过；真实云库副本会话 41719 退出 0。默认入口的完整测试会话 69404、53350 退出失败，其中 npm 日志 `2026-09-07T08_11_49_501Z-debug-0.log` 记录原生退出码 `-1073740791`，没有对应断言信息，未据此声称通过；相同接口用例连续 8 次单独执行通过。随后只在本次命令指定现有 PowerShell 为 npm script shell，执行完整根测试，会话 59604 最终退出 0（包含全部云端 pretest/test/posttest、桌面权限/草稿、小程序页面契约、后端及发布边界检查）。没有修改 npm/system 配置或依赖，也没有将崩溃根因标记为已经修复。用户版本文件哈希保持不变。

### 2026-09-07 真实应用还原查出的备份权限缺失（UTF-8）

- 原桌面连接完整云库副本时，真实 `gewu_cloud_schedule_reader` 读取测试教师账户返回 PostgreSQL `42501`。根因是旧备份使用 `pg_dump --no-privileges`，验证恢复又使用 `--no-owner --no-privileges`。旧备份仍保存业务数据，但没有保存 ACL；旧副本维护角色的恢复/回滚通过，不能证明应用账号能正常运行。此前 158 条快照的数据恢复证据仍限于该范围，不作为应用权限或完整灾备验收。
- `backup_cloud_postgres.py` 已保留归属和权限；发布前备份门禁拒绝没有归属/权限验证的新回执。仅还原到新空库，不再给出覆盖生产库的清理式恢复命令。源库与还原库比较两业务 schema、表/视图/序列和函数的属主与规范化 ACL、函数安全模式/search_path、默认权限及表 RLS 开关指纹；这不是新集群角色/密钥恢复验证，角色仍须另行受控配置。PostgreSQL 最小权限技能用于保持真实 reader/writer/verifier 分工，没有通过给业务账号提权使测试通过。
- 新追加备份 `/root/scheduling-backups/postgres/20260907-083758/gewu_cloud.dump` SHA256 `391d0629d65c9c81c6fc6f5698b2c2ca4b68915cc31681845eb11f7a5b7591f4`，归属/权限指纹 `046cf160303ba80ae0835241a3ccc659`，真实还原比对通过。旧备份未删除或覆盖。临时验证库已删除。第一次新指纹 SQL 因 char/text 拼接歧义失败，修正显式转换后重跑成功；失败时间戳目录不视为可用备份。
- 新 `run_business_parity_shadow.py` 验收入口每次只创建随机 `gewu_ui_shadow_*` 数据库、还原并核对新备份权限指纹、应用待发布迁移，再使用现有镜像依赖加载当前云服务源码。服务仅云主机 loopback 端口，经 SSH 转发；不重建生产镜像，不替换现有服务，不连接 NAS；导出 worker/storage agent 关闭。测试票据签名独立，测试教师密码只在副本设置并经进程输入传递，不持久化密码或票据。
- 桌面测试入口只在自身进程通过已有配置参数覆盖 loopback 服务地址，原 `public/electron.js`、原 IPC/保险库/草稿/runtime 和原 React 界面均执行，不修改正式固定云地址。QA 清单覆盖原窗口创建学生、建课、排课/调课、离线确认、卡片和窗口尺寸；没有将只完成登录记成完整页面验收。
- 首个原桌面密码登录与教师投影读回会话 29692 退出 0，证明保留 ACL 后业务账号实际可用。随后实际创建学生“林小禾”、学校“春禾中学”，未确认与重连均未写入副本；点击确认被固定导航遮挡，真实截图及 Playwright 点击失败保留在该轮临时证据。同步快捷面板弹出锚点从 bottomRight 改为 bottomLeft，避免向导航下方展开；只改该新增面板，不改学生抽屉、原课程表单或排课规则。源码构建会话 4851 退出 0，`main.524780ff.js`；后续确认提交结果另记，不提前标记成功。
- 当前没有部署生产云服务、上传小程序或更新 OSS/NAS。原业务逐项保真、小程序全页多角色、Word 公式可编辑和统一发布门禁继续保留。

- 补充：新权限备份在 `gewu_snapshot_shadow_107aec2ccab2441e` 上重新完成 158 条快照的持久回执恢复、故障回滚和显式回滚；会话 43883 退出 0，69 表、645 条排课保持原范围。回执 SHA256 `dd3d5f1853d37f330e219045c72d81a697b7c8d94e45f2a1be11a98384dc618d`，新增 `ownershipAndPrivilegesVerified:true`。副本和维护容器均已清理。某轮 UI 辅助源码上传中 SSH 断开，精确临时库 `gewu_ui_shadow_7215add073d477c9` 与对应辅助目录另行核实并清理；未操作其他数据库或备份。验收入口随后增加断连后的清理重连。
- 原窗口会话 26105 退出 0，完成教师密码登录、空表单必填检查、原学生抽屉离线新增、重连不推送、用户点击确认提交、云端学生和学校读回、刷新后经原菜单重新进入学生列表。仅名称/学校验证通过；截图人工复查发现联系电话消失、生源从自有显示为机构，故**本次整体学生资料保真不通过**，不能以脚本退出 0 宣称完成。此前重跑中的错误学生 ID 路径和刷新默认返回首页属于验收脚本问题，均已修正。
- 该视觉差异的代码根因：两个云端学生读取投影遗漏 `legacy_source_type` 与 `student_source_legacy`；联系方式已进入云端独立联系人表，但原列表仍读取旧平铺电话字段。修复仅补齐原字段投影，并在本地派生缓存复用原学生编辑窗口已有的联系人映射；联系人草稿覆盖仍优先，不写回旧平铺字段、不改数据库联系人权威。新增回归先复现旧电话保留/新电话丢失，再验证三组联系人、原来源、联系方式草稿和云端源对象不被改写。同步面板锚点、备份权限和该回归已加入 `test:business-parity`，由根 `pretest` 调用。
- 后续原窗口测试增加真实联系人值、来源值和重新打开后的表格单元格断言。最新源码构建会话 10692 退出 0，`main.31ce1e54.js`；修复后的真实 UI 重跑和完整测试结果待下文，不提前认定通过。截图另见账户标签 `Cloud account`，留待账户文案/全页面审计核对，不擅自改业务窗口。

- 修复后的真实原窗口会话 **47345 退出 0**：阿里云副本 `gewu_ui_shadow_eb4cd4c7f4b390a4` 使用当前 cloud-business-api 与 shared 源码、原运行镜像依赖。原学生抽屉离线新增“林小禾/春禾中学”，重连后按精确学生 ID 确认未写入；点击确认后读回 `source_type=1`、slot 1 电话，并刷新、通过原菜单重新打开列表，逐单元格确认电话和“自有”。实际重新打开截图 SHA256 `26e0d922018b679a58f897b2d456b0f16ea6953f1d90a7ed57f9d95f206dd8b8`（本机临时证据 `gewu-business-parity-nsvayf6q/05-student-reloaded.png`）已查看，电话和绿色自有标签均存在。数据库副本与容器已清理，生产业务没有写入。本轮只验收这一条学生新增/学校登记闭环；课程、调课、较小窗口及其他字段组合不据此放行。
- `test:business-parity` 会话 74252 退出 0；原缓存草稿、学生联系人、教师学生会话权限、云端投影及原卡片专项均通过；原导航遮挡浏览器检查通过。完整根测试会话 96489 退出 1，输出止于 cloud-business-api 的 supplemental lifecycle 前后，没有断言诊断；对应接口测试单独重跑通过，但**不把单测通过替代完整根测试通过**，仍需继续核验全量入口。当前未证实这次中断根因，不复用上轮原生退出码作为本轮结论。
