# NAS 解析器实际验证（2026-09-19）

本记录是进行中的发布门禁，不代表多端验收完成。NAS 待发布版本继续为 8.8.3；不因重试或补丁增加版本号。

## 当前实测

- 581ad4eb 已推送 gewu/master；Alpine 官方源在构建环境反复 TLS 失败，普通容器可访问。增加构建期 HTTPS 镜像源参数（默认仍是官方源），改用 Alpine 官方列表中的阿里云镜像后实际安装成功，保留 TLS 与包签名验证。
- 固定提交镜像 `gewu-storage-agent:8.8.3-581ad4eb` 已构建，摘要 `27fd141d619a850de4a7c49246a9ce97f672183b9a16a2c3b4a0af8cf941a924`。MathType 加载成功；试卷 20 题一致。讲义 88 题中第 56 题 `a-x` 已恢复，但第 67 题四个选项是 WMF，Linux 原先不转换、Windows 转为 PNG，所以严格对照未通过，未保存 tar、未上传或切换 NAS。
- 实际渲染四张 WMF 后确认它们是公式，而非示意图：`v_1=v_2`、`2v_1=v_2`、`3v_1=2v_2`、`5v_1=3v_2`。仅补 PNG 转换不能满足用户的 Word 可编辑公式要求。WMF 的 AppsMFCC 注释保留 MathType 数据，转换库已支持读取，缺口在导入适配器只读取 OLE、不读取独立 WMF。
- 已补独立 WMF 原生公式导入，按格式隔离缓存、批量转换，已有 OLE 的预览不重复导入。失败转换保留原图和待审核公式元数据，不伪造 LaTeX、不静默当作完整公式。普通非公式 WMF 才使用受限的 libwmf PNG 转换（20 秒、1600×1600 上限、原件不改）。
- 回归先失败：0b6e8c 缺少转换模块、bdef1a 缺少 WMF 导入、30618e 失败公式未进入审核清单、0bec25 Ruby 异常处理语法错误；修复后 af7fcf 全部 63 项解析测试通过。c8995f 存储代理全量测试通过。
- 实际 Windows 与 Linux 隔离容器按两份 D 盘原件对照：20+88 题的题干、答案、解析、选项、小问、富文本、公式元数据一致；第 67 题恢复上述四个原生公式，四个选项均不再是图片。70f2a6 退出 0。证据目录 `C:/Users/83423/AppData/Local/Temp/gewu-native-wmf-parity-20260919-zqnhpiw2`。这次验证使用源码只读挂载的诊断镜像，正式源码固定镜像仍须另行构建验证。

## 线上状态与未完成项

- 2026-09-19T15:40:48Z 生产数据库只读检查：NAS 最新心跳为 15:40:10Z，版本 8.8.2；储存租约、排队储存、图片/成品传输租约、导出、导入的活动计数均为 0。证据目录 `gewu-nas-cutover-readonly-20260913-k3yoreuj`。切换前必须重新检查，不能沿用旧空闲结论。
- 本次没有修改生产讲义第 67 题。须核对其当前版本/结构、执行备份恢复门禁、通过正式 REST 修正后，再生成新 Word/PDF，验证这四个公式是原生 Word 公式。此前 473 个原生公式的产物证据不能用来声称所有公式图片已消除。
- 浏览器控制入口两次在初始化阶段返回 `Unable to load browser request-header policy`，未读到当前浏览器清单，不能据此声称 Chrome/Edge 扩展未安装。未修改浏览器授权、未启动远程调试，未动线上容器。
- 微信开发者工具 skill 0.3.2 equal、登录未失效、无需令牌。原项目窗口未开，已打开；初次运行时尚无页面，随后截图实际显示登录页，currentPage 确认为 `pages/login/index`（1e2b2c）。截图 `gewu-startup-current-20260919.png`。没有让用户重新扫码，未测试真实手机号登录；这不代表之前间歇性启动/导航问题已解决，也不代表多角色页面验收通过。

下一步：固定源码镜像验证及可用管理入口切换；生产第 67 题原生公式修正与新产物验证；继续既有桌面/小程序多角色、全页面、课程业务操作清单。保留原业务界面和排课逻辑，整体仍为部分发布。

## 2026-09-20 构建检查修正

- 固定源码 50f1473d 的构建实际失败于 `wmf2gd --version`：libwmf 已安装，且输出 `libwmf: version 0.2.13`，但该工具在版本展示时退出 2。没有产出或上传 tar。改为检查可执行文件存在且可执行；实际转换和两份原件内容对照仍是成品镜像门禁，不用忽略错误掩盖失败。
- 新回归 243f59 先失败，修复后 6ec653 存储代理全部 12 组测试通过。NAS 仍为待发布 8.8.3，其余端不升版。
- 2026-09-20 00:08（北京时间）公网健康接口返回云端 8.11.14、PostgreSQL、业务权威 cloud；SSH 与生产数据库只读查询成功。NAS 心跳仍为 8.8.2，活动任务计数均为 0。证据 `gewu-nas-cutover-readonly-20260913-sov9tpqx/receipt.json`，不能代替后续切换前检查。
- 浏览器重试后已能读取 Chrome、Edge 现有标签页；Chrome 阿里云控制台没有验证码弹窗，实例显示运行中。管理终端页的 VNC 断开不代表业务不可用。未因此重启服务器或更改登录信息。

## 2026-09-20 NAS 切换与后续修正门禁

- 4a4af821 固定源码镜像完成构建；真实镜像禁网、原件只读运行，试卷 20 题与讲义 88 题全部结构和公式元数据对照一致。产物 74,342,912 字节，SHA-256 `8ed4b74e45023b8a13d49c82cfd935621ed8a4af692e7e4891cf26d8a4ea4cb2`。构建证据目录 `gewu-nas-8_8_3-20260920-cy7k2dmt`。
- 通过现有 Edge NAS 标签页上传到原有存储空间根目录，选择保留已有文件；导入镜像 `gewu-storage-agent:8.8.3-4a4af821`，新建 `gewu-storage-agent-8.8.3-candidate`。先保持未启动并核对挂载 `GewuStorageAgent → /nas-storage` 读写、bridge、无端口映射、`node src/launch.js /nas-storage/agent.env`、unless-stopped。
- 切换前 16:22:03Z 只读检查所有活动存储/导入/导出/传输计数为 0（`gewu-nas-cutover-readonly-20260913-kuq0xfkl`）。停止 8.8.2 后启动 8.8.3；旧容器和镜像全部保留，旧容器策略只读确认也是 unless-stopped，无需修改。
- 云端 16:24:02Z 收到 8.8.3 心跳，协议为导出 3、传输 3、解析证明 1。16:26:05Z 实测线上解析器包指纹与最终镜像一致：`8d3a16cd92f5d01a9bbf8a746dc9115acb6dfb087ec854bc32b9d82e482cf689`。证据 `gewu-nas-runtime-proof-20260920-7fx8k_2m/receipt.json`。镜像 ID `sha256:93b1f6a444c3a53c69dc46fe2fba32c34796cf9750434ab1f0c56a79a3d79ae8`。
- 存量讲义第 67 题仍为内容版本 2，四个选项仍是图片；代理更新不自动修改已入库内容。新增受限维护准备器只允许整项单图选项变为来自相同 WMF payload hash 的完整原生公式，保留选项 ID、标签、答案及其余富文本，使用既有 REST、备份、CAS、持久执行日志和重试读回门禁。832026、ab89e8 先失败，aeb1e2 四组相关测试通过。维护脚本不改变客户端或服务协议，不触发其他端升版。
- 当前真实修正计划仅包含这一题的四个选项，计划 SHA-256 `9173919be8c301de1c9edc9c501081e6b0e2f645986be4c7618f796c691932d1`；此记录时尚未提交。后续仍需实际修正及新 Word/PDF、多端真实操作和全页面验收。NAS 此项切换完成不等于多端发布完成。

## 2026-09-20 存量公式正式修正及新导出检查

- 上述单题修正随后已通过正式 REST 提交，内容版本由 2 变为 3。提交前新建数据库备份，SHA-256 `a2c8846949932b812e39cda8d30cc1593789a31f745c8e29d2705774b7f847de`，实际恢复验证与所有权/权限验证均通过。执行记录为 `gewu-option-formula-rest-20260920-l52a23q6`，维护用临时登录已撤销，活动会话为 0。
- 独立只读查询再次核对四项均为原生公式、原选项 ID 保留，题干、答案、解析及其他元数据未改。读回 SHA-256 `8d1f94c7defd71f1a53be741cc9a9c0c69207d379aaf086635fd0d96c00d3b03`，验证记录 `gewu-lecture67-readonly-20260920-qu_ag7ty/verification.json`。这是云端内容修正证据，不是 Word/PDF 成品已验收的证据。
- 使用同一份已批准的 108 题选择发起真实教师权限云端 Word 导出，任务 `paper_task_c09d0953-0242-41c0-bb75-2057348f9f65`，云端 8.11.14、NAS 8.8.3。截至 16:46:07Z，115 份素材已有 39 份 ready、76 份 queued，传输有进展；导出仍为 processing/media_pending。保留同一任务，不因等待重复创建。证据 `gewu-export-status-20260920-b9ffma8k/receipt.json`。
- 后续门禁仍为：下载并实际验证新 Word/PDF、全量公式及版面检查、桌面与小程序多角色真实操作、原业务逐项差异恢复和全页面验收。此次仅维护数据与验证记录，不触发 NAS/桌面重复打包或其他端升版。

## 2026-09-20 新成品已取得但排版验收未通过

- 真实 Word/PDF 导出、NAS 成品归档与云端下载均完成（0aaea7 退出 0）。Word 为上文同一任务，没有重建；十分钟测试会话过期后重新取得同账号短时会话继续读同一任务。PDF 任务为 `paper_task_72fd2d30-a9a9-4528-8b10-9b26520e2765`。
- Word：11,289,770 字节，SHA-256 `37f544fe2062f1ea63a72533072efc45d2d25865663de4807d867ba650aeb59e`。ZIP 校验通过，含 452 个原生 OMML 公式和 115 张内嵌图；第 87 题（讲义第 67 题）的四个选项逐项确认是 `v1=v2`、`2v1=v2`、`3v1=2v2`、`5v1=3v2` 的 OMML，原四张 WMF 的内容哈希不在内嵌图片中。此项证明四处修正生效，不据公式总数断言其余所有数学排版正确。
- PDF：17,920,064 字节，SHA-256 `249ff2c6bb75ce8e4dcbc261249a8b20064d5cf2f44df1ead0304dbe3185d298`。严格解析成功、未加密、83 页全部由 Poppler 成功渲染（f63ad8 退出 0）；已实际查看第 1、5、13、14、69 页。第 69 页四项公式显示完整。没有声称逐张查看全部 83 页。
- **排版门禁失败**：第 5、13、14 页等示意图过大，出现整页只放图、题干与选项和图分离。与原试卷 Word 的 23 张同哈希图片对照发现，源宽 161.25pt 的图在导出 Word 变成 315pt、源宽 171pt 的图也变成 315pt。渲染器当前使用像素尺寸和固定上限，快照部分图片节点 width 为 null；必须继续按原件显示尺寸和原业务导出规则核对，不能用任意缩图阈值或修改原件掩盖问题。该检查不写云数据库。
- Word 整页渲染尝试失败，明确错误为 `LibreOffice soffice.exe was not found on PATH`；运行时包和两处标准安装位置没有 LibreOffice，文档连接器也没有连接会话。未改文件冒充成功，Word 全页视觉验收仍缺失。
- 本机证据目录 `gewu-cloud-corrected-108-8_11_14-20260920`，含 `report.json`、两份成品、`structure-review.json`、`source-image-size-review.json` 和 `pdf-render`。新任务快照 SHA-256 `d89026853c68ccf38a40498fcda74dd922e23dcfcc17293dac881c25d9888646`。成品属于保留的失败排版样本，不作为已验收交付。
- 后续继续：修复已证实的原图显示尺寸丢失/图片位置问题并重验，恢复原桌面业务差异，补齐课程与题库多角色操作和 18 页审计。当前只是部分发布，不是完成投入使用。
