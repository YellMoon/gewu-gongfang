# 题库字段与真实渲染验证（2026-09-13）

## 本次修复范围

- 云端此前接收但没有保存/返回桌面已有的来源、年份、年级、学期、考试类型、地区、学校、科目编号、章节编号、编辑状态及含图标记。本次使用已有数据库列补齐创建、更新和读取，未新增数据库迁移。旧草稿未提供字段时保留原值，显式清空与版本冲突分别验证。
- 小程序 RichText 内部图片无法依赖外层图片选择器获得尺寸限制。真实题库截图中导入图越出题卡，修复为与桌面一致的最大宽度、自适应高度；小图不强制拉伸。题干、选项、答案与组卷共用此转换函数，不更改原业务或页面布局。
- 实机检查脚本补充逐角色进度和安全的启动失败诊断，只报告路由、身份是否匹配、桥接失败次数，不输出账户或令牌。

## 测试证据

- PostgreSQL 字段测试先失败（387220），修复后通过（66d708）；完整字段专项包含导入绑定、来源默认/覆盖/清空、分页及既有服务测试，19128/a64aff 退出 0。
- 图片测试先失败（a83293），修复后显示、题库云交付及组卷云交付测试通过（a855e7）。小程序类型检查 31137/8838a6、构建 70381/7407ee 均退出 0。
- 实际云测试会话的五角色检查 46919/ada5a8 退出 0：超级管理员、教师、学生、家庭成员各 40 张题卡/答案入口/试题篮入口，访客各 20 个；合计 12 组角色与页面组合。这是运行时数据和入口检查，不是全部按钮交互验收。
- 教师课程、课表、题库三张截图 19209/e6172f 成功取得并逐张查看。题库图片溢出是在查看真实截图后发现，不以元素存在替代视觉检查。
- 修复后再次取得教师题库截图并实际查看：图完整限制在题卡内，选项已可见。截图 SHA256：`9f5d47f35eab27ae5c7f9f6c58efe161ca211ade3372e00700c4421b8d8b34c4`。
- 截图位置：`C:/Users/83423/AppData/Local/Temp/gewu-miniapp-native-word-20260913/image-bounds-fixed/teacher-pages-question-bank-index.png`。修复前对照位于同一目录的 `foreground-teacher-pages/teacher-pages-question-bank-index.png`，SHA256 `b8d8cd6863599b056bbe3419c439e440071a28435512af2f7337bacbf672428a`。
- 最后补验 3556b2：图片单元测试、启动等待 3 项测试、差异空白检查通过。

## 全量入口的未通过记录

根 `npm test` 日志化运行 34123/ec45b1 经过 701.36 秒，以原始退出码 `3221226505`（0xC0000409）终止，未取得全量通过。最后输出在发布矩阵中的生产题目修复 PostgreSQL 测试附近，原生进程异常原因未确定，不能归因于紧随其后的脚本。

日志：`C:/Users/83423/AppData/Local/Temp/gewu-full-regression-20260913-bzxw44_y/npm-test.log`，SHA256 `58f0e70e1e0c9c7ec340361f15f6a066ce081598f3360035f6a2c19ca62d679a`。随后发布矩阵独立运行 72220/2feb08 退出 0，根入口尾部三项检查 62669/7254aa 退出 0，但不能据此称根入口完整通过。

## 仍需完成

1. 全部 18 个注册页面及适用角色、离线/无权限/有限写入状态；目前运行时角色检查仅覆盖 4 个不同内容路由。
2. 组卷编辑、移入移出、下载及 Word/PDF 实际打开；修正题的公式可编辑性复验。
3. 导入器的来源与标签映射及既有 108 题标签回读。此次云字段修复尚未部署，也没有凭空补知识点或重复导入。
4. 原有桌面课程与排课操作完整闭环、全量回归异常定位、按兼容矩阵备份后发布与健康验证。

本次尚未部署云端、上传小程序或发布 OSS；NAS 接口和结构未变，不需要随这两项修复更新容器。不能宣布全部发布或投入使用完成。
