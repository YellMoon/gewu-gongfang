{
  "schema": "gewu.protocol-data-compatibility.v1",
  "contracts": {
    "desktopQuestionMetadata": {
      "version": "1",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "existing question source/year/grade/semester/exam_type/region/school/subject_id/chapter_id/edit_status/has_image fields persist in existing business.questions columns and round-trip through desktop reads; omitted update fields preserve prior values, explicit clears remain explicit and metadata edits use the existing content-version CAS; absent import source defaults to the verified original filename; cloud-only implementation correction, no new schema or NAS change"
    },
    "desktopQuestionProjection": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "desktop question pages use stable ID cursors and explicit nextCursor null at the end; desktop replaces its derived cache only after every page succeeds; deploy the additive cloud cursor response before the desktop consumer"
    },
    "desktopQuestionMutation": {
      "version": "2",
      "participants": ["desktop", "cloud_business"],
      "rule": "question update/delete requires the observed integer content version; taxonomy changes also advance it; legacy drafts without a valid baseline are rejected and must be recreated after refreshing cloud data"
    },
    "cloudBusinessRest": {
      "version": "1",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "cloud_business is the only business-data writer; desktop submits confirmed drafts and miniapp has limited task writes"
    },
    "desktopScheduleRestoration": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "schedule PUT with restoreDeleted true and full lifecycle/pricings requires the confirmed deletion expectedUpdatedAt; restore is atomic, permission-scoped and explicitly confirmed; ordinary PUT/POST never resurrect a tombstone; deploy 20260908-schedule-confirmed-restore.sql and cloud support before desktop; old cloud rejects restoration without a legacy fallback"
    },
    "desktopStudentLedger": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "desktop teacher projection supplies existing payments/consumptions arrays for its authorized students, in the same database snapshot; preserve original student-wide balance calculation and tenant/deleted boundaries; missing arrays fail closed; cloud-only correction using existing schema and client fields, no miniapp or storage_proxy change required"
    },
    "studentDeletionReferences": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "20260908-student-delete-original-behavior.sql restores original desktop deletion regardless of course/schedule references; soft-delete only the authorized student, retain actor/version locks and all related records; apply forward migration before cloud deployment; existing REST/client contract is unchanged, no storage_proxy or miniapp update required"
    },
    "courseDeletionHistory": {
      "version": "1",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "apply 20260909-course-delete-original-behavior.sql before cloud deployment; original course deletion retains lessons, rates, attendance and finances; scoped projections retain authorized lessons and original display labels but never resurrect courses in selectors; miniapp retains supplied course_name/course_type; no storage_proxy change required"
    },
    "retainedCourseScheduleWrites": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "apply 20260909-retained-course-schedule-write.sql before cloud deployment; original move/copy/resize/delete and confirmed restoration retain tenant/teacher ownership, original lesson snapshots and version checks even after course deletion; only roster links of the checked course extend schedule permission, not student CRUD; course selectors and attendance editor are unchanged; no new REST fields, miniapp or storage_proxy update required"
    },
    "retainedStudentScheduleWrites": {
      "version": "1",
      "participants": ["desktop", "cloud_business"],
      "rule": "apply 20260909-retained-student-schedule-write.sql before cloud deployment; original move/copy/resize/delete and explicit restoration preserve deleted students already linked to the checked course by enrolment or a retained active lesson; keep original rates, tenant/teacher ownership, version checks and locked reference evidence; student CRUD and selectors remain unchanged; existing REST fields only, no miniapp or storage_proxy update required"
    },
    "questionPaperExport": {
      "version": "3",
      "participants": ["desktop", "cloud_business", "miniapp", "storage_proxy"],
      "rule": "structured question text is cloud-authoritative; storage_proxy keeps only media and export artifacts"
    },
    "storageAgentTransport": {
      "version": "3",
      "participants": ["cloud_business", "storage_proxy"],
      "rule": "storage_proxy never decides identity, permissions, or business transactions; v3 carries an exact parser revision proof with question-import candidates"
    },
    "questionImportParserProof": {
      "version": "1",
      "participants": ["cloud_business", "storage_proxy"],
      "rule": "storage_proxy reports the exact parser SHA-256 and cloud_business matches it to the import task before accepting candidates"
    },
    "businessDataSchema": {
      "version": "1",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "desktop offline changes are local drafts until confirmed; miniapp does not directly mutate core teaching data"
    },
    "desktopCloudSession": {
      "version": "2",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "desktop login verifies the cloud account before silently registering the device and installation; an authenticated miniapp session can confirm pairing without a second phone proof; restart challenges and role switching are cloud-authoritative"
    },
    "identityRoleModel": {
      "version": "2",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "formal grants are exactly super_admin, teacher, student, and family_member; visitor has no formal grant; family_member keeps guardian-scoped linked-student data access"
    },
    "questionBankBrowse": {
      "version": "1",
      "participants": ["desktop", "cloud_business", "miniapp"],
      "rule": "desktop and miniapp render the same cloud-authoritative question structure, option semantics, filters, basket identities, answers, explanations, and source metadata"
    }
  },
  "runtimeReceipts": {
    "storage_proxy": {
      "approvedRuntimeVersions": ["8.8.2"],
      "contracts": {
        "questionPaperExport": "3",
        "storageAgentTransport": "3",
        "questionImportParserProof": "1"
      }
    }
  }
}
